# Tooling & environment — skill pack

> Lane pack. Load `_core.md` first, then this file. Denser than the full articles by design;
> for a matching bullet's full write-up, follow that gotcha's article link in `coverage.md`
> (full articles live on the Field Guide website, not in this pack). Sanitized public
> advice; unconfirmed details marked `(needs verification)`. The sync appends new bullets here.

## Engine environment

- **`new System.Random` (time-seeded) IS whitelisted** — compiles + runs headless. Useful
 for per-LOAD procedural variation (verified: one project arched-roof open-pane picker uses
 `new System.Random; rng.Next(n)`). Note the earlier "no crypto RNG" gotcha is specifically
 about `HashCode.GenerateGlobalSeed`/`RandomNumberGenerator`, NOT `System.Random`.
- **Arch/slope tilt axis: rotate about `Vector3.Forward` (+X), not `Rotation.From`'s pitch.**
 For a slab whose up-face must slope ALONG y (an arch rising across y), `Rotation.From(pitch,..)`
 rotates about Y and slopes along x (wrong). Use `Rotation.FromAxis(Vector3.Forward, deg)` to roll
 +Z toward ±Y. Sign still needs one visual confirm (rotation-sign gotcha above): one project's
 arch uses `-pitchDeg` so +Z leans DOWNHILL — flip if it reads as a valley/twist.

- **A `FacingYawOffset` (mesh-facing correction) SWAPS which `_baseRot` local axis is the
 flip axis vs the cartwheel axis — a front flip is `_baseRot.Forward`, NOT `.Right`, when
 the offset is ±90.** one project's hero character uses `FacingYawOffset = -90` (AI-generated (Tripo-style)
 meshes face mesh-local −Y). The rotation returned by `ComputeRotation` bakes the −90 into
 its yaw, so for a character traveling world +X: `_baseRot.Forward = -Y`, `_baseRot.Right = -X`,
 `_baseRot.Left = +X` (the visual NOSE/travel dir). A front flip pitches the nose over its
 own LEFT-RIGHT axis = world ±Y = **`_baseRot.Forward`** here; `_baseRot.Right` (world -X) is
 PARALLEL to travel → an aileron **CARTWHEEL** ("goes sideways, all the way around"). Both the
 charged somersault and the air-flap barrel roll had copied `_baseRot.Right` (naive "Right =
 pitch axis" from the no-offset convention) and were latent cartwheels. Fix:
 `Rotation.FromAxis( _baseRot.Forward, t * -360f ) * _baseRot` (NEGATIVE sign: at −90° the
 nose(+X) pitches to −Z = dives forward/down = front flip; +90° backflips). DON'T hand-guess
 the axis — PROVE it: a scratch net10.0 console referencing the real `Sandbox.System.dll`
 (`bin/managed/`, net8 crashes on Vector3 binding) evaluating `Rotation.From(0,yaw,0).{Forward,
 Right,Up}` classifies each axis against the travel dir in seconds.

- **Deriving a visual's FACING from horizontal velocity flips 180° on any momentum reversal —
 lock facing to an attach-time azimuth for pendulums/oscillators, don't re-derive it each tick.**
 A rope-swing visual that set `fwd = horiz.Normal` snapped the body around every time the
 pendulum reversed (owner: "I go forward then start going backwards and I turn the other way —
 weird; a kid on a swing keeps facing and swings BACKWARD"). Root cause: horizontal velocity's
 SIGN reverses at every arc endpoint, so a velocity-heading facing is discontinuous. Fix: pick a
 facing azimuth ONCE at attach (from the initial swing impulse), hold it constant, and only
 re-aim it on DELIBERATE steering input (A/D past a threshold rotates it a fixed step) — never
 from the velocity sign. Feed it to the visual through a dedicated continuous channel
 (`Character.SwingForward`), the same fix the BAR spin already used (the spin tangent is continuous
 over the top; the pendulum azimuth is continuous through a reversal). BARS keep their spin-sense
 facing; only the POINT/tire pendulum needed the azimuth lock. General rule: any body whose
 velocity oscillates (pendulum, ping-pong platform, reversing patrol) must NOT face its velocity
 heading — face a locked/steered heading, or it strobes.

- **A camera that switches FOCUS TARGET or FOLLOW DISTANCE on a state change jerks even when the
 camera POSITION is already exponentially smoothed — smooth the target, not just the follower.**
 one project's chase-cam focused the swing ANCHOR (×1.5 distance) while swinging and the BODY
 (×1.0) otherwise; the position lerp (`Lerp(pos, desired, 1−e^−kt)`) hid steady motion but on
 dismount the `desired` TARGET jumped in one tick, and lerping toward a snapped target reads as a
 velocity kink ("jerky when I dismount"). Fix: ease the focus point + follow distance toward
 their raw per-state values through their OWN exponential (a separate `_smoothFocus`/`_smoothDist`
 at a rate SLOWER than the position lerp, ~5 vs 12), so the target glides across the Swing↔Air
 framing switch and the position lerp never chases a discontinuity. Seed the smoothed values on
 the first valid frame or they glide in from the origin.

- **An input press consumed at the bottom of the fixed tick is LOST across a state transition
 that `return`s early — buffer it.** Character's swing RELEASE branch flips to Air and `return`s;
 the shared `_jumpPressed` edge is then cleared at the end of OnFixedUpdate before TickAir ever
 runs, so a Jump pressed ON the release frame silently ate the dismount→jump. Fix: handle the
 press inline in the release branch (launch immediately), AND arm a short buffer timer
 (`TimeSince`, ~0.2 s) that TickAir checks so a press landing a frame or two LATER still fires the
 same release-jump. Buffering both the on-frame and just-after cases makes the dismount→jump feel
 continuous regardless of exact press timing.

- **Pure-nearest target selection re-grabs the anchor you just left — score by DIRECTION OF
 TRAVEL and exclude the just-released target briefly.** one project's `NearestGrabpoint` was
 distance-only, so dismounting a swing and grabbing again yanked the character BACK to the same
 anchor even after flinging toward a closer new one (owner: "pulls me back to the thing I just
 dismounted"). Fix: (a) remember the released anchor and EXCLUDE it from re-selection for a window
 (~0.8 s, longer than the same-anchor debounce cooldown), falling back to it only if nothing else
 is in reach (never a dead grab); (b) bias the score toward travel: `score = dist × (1 −
 Bias×align)` where `align = dot(toTarget.Normal, velocity.Normal)` ∈[−1,1], so an anchor ahead
 scores as if closer and one behind as if farther (a slightly-farther anchor in the fling
 direction beats a closer one behind) — gate the bias off below a min speed so a slow/hanging
 body has no false travel direction. Log the decision (chosen vs nearest-rejected + why) for
 tuning. Radii left as-is (GrabRadius 2.6 m hand / TailHookRadius 4.0 m airborne) — they weren't
 fighting chaining; the SELECTION was.

- **Whitelist:** `Http` may call any domain (not raw IPs). Vendored libs need patches:
 no `System.Console`, no crypto RNG (`HashCode.GenerateGlobalSeed`), no
 `Environment.NewLine`. `System.Text.Json`, `ArrayPool`, `GameTask.RunInThreadAsync`
 are all fine.
- **Hotload:** C# edits apply in ~ms on alt-tab. Network sessions reset on hotload;
 scene-file changes need a Play restart; new assets sometimes need an editor kick.
- **Fixing a source asset does NOT always trigger a recompile of a previously-FAILED
 asset** — the engine can keep serving the stale `_c` baked from the broken version
 (ERROR models / white materials persist across editor restarts). AND deleting the
 `_c` alone is NOT enough: with an unchanged source mtime the engine just reports
 `ERROR_FILEOPEN` on the missing `_c` without recompiling. The reliable sequence is
 BOTH: delete `.vmdl_c`/`.vmat_c`/`*generated*` artifacts AND `os.utime` the source
 files so the watcher marks them dirty, then kick the editor.
 A small fix-up script that deletes the stale `_c` and re-touches (`os.utime`) the sources automates this after any asset regeneration.
- **Engine console noise is normal**: missing `citizen_clothes/…`, `sfm` sounds,
 `menu-main.scene` sounds, wheely-bin gibs etc. are broken refs inside Facepunch's own
 base content — never ours. Vulkan `QueuePresentAndWait`/`VK_TIMEOUT` = driver-level
 present stall (alt-tab/shader compile), not game code.

## Tooling (see tooling.md for detail)

- **PowerShell 5.1 corrupts UTF-8 on bulk edits** (`Get-Content`/`Set-Content` reads
 BOM-less UTF-8 as ANSI → emoji/dash mojibake). Use python or byte-safe .NET file APIs.
- **Some C# source files in one project are CRLF, and a `\n`-based string-replace edit
 silently fails to match them** (the search string has `\n`, the file has `\r\n`, so the
 multi-line `old_string` never hits — the byte content otherwise looks identical under
 `od -c` per-line, which hides the `\r`). Symptom: an exact-looking edit reports "string
 not found" repeatedly on a block you can see in the file. Diagnose with
 `python -c "print(b'\r\n' in open(p,'rb').read)"`; fix by editing via a python
 byte-level `data.replace(old_bytes, new_bytes)` with `\r\n` in the literals (assert the
 match count is 1 first), then write back `'wb'`. Mixed in the same repo — ProjectileTuning.cs
 is CRLF, ProjectileSplat.cs is LF — so check per file.
- Verify compiles without the editor: `dotnet build Code/<proj>.csproj` — the csproj
 references the Steam s&box install directly. Do this after every batch of edits.
- **A stalled Steam update half-deletes the s&box install — close the launcher.** If
 `sbox-launcher.exe` stays running across an update (e.g. left open overnight), Steam
 downloads + stages the update but can't commit while files are in use, and the install
 ends up missing files: `sbox-dev.exe` gone (launcher's "open project" silently fails —
 no window, error only in `sbox/logs/sbox-launcher.log`), random dlls gone
 (`Sandbox.CodeUpgrader.dll` → all headless `dotnet build`s fail with CS0006 from
 `Base Library.csproj`). Diagnose: `steamapps/appmanifest_590830.acf` StateFlags ≠ 4
 with 100% BytesStaged. Fix: kill the launcher, run `steam://validate/590830`, wait for
 StateFlags 4. Don't "fix" the csprojs — they were never broken.
- **`dotnet build` warning counts lie under incremental builds** — warnings are only
 re-emitted for files that actually recompile, so a stale-output build happily prints
 `0 Warning(s)` over a tree that carries warnings. Any build-gate claim (docs,
 feature-matrix headers, agent verify clauses) must come from
 `dotnet build --no-incremental` (or a clean). Per-agent scope checks can stay
 incremental — it's the WHOLE-TREE 0/0 claims that need the rebuild.
- **A one-shot "commandeer + suspend competing drivers" sweep misses components created
 AFTER it runs** — one project's benchmark flythrough suspended everything on the camera
 GameObject at scene start, but Bootstrap.StartPlay creates ChaseCamera on that same
 GameObject ~1 s later (menu -> play transition), so it was never suspended and fought the
 flythrough every frame (its leash invariant hard-snapped the camera back to the player
 every ~2 s -> the visible "camera disjointed / sliding away"). Any commandeer pattern must
 either re-sweep for newborn drivers every tick (cheap on one GameObject) or hook the
 creation path; a suspension list built once is a scope-hole of the same class as a
 one-shot audit exemption. Diagnosis shortcut: if a misbehavior's telemetry warns cluster
 EXACTLY inside another system's active window, suspect a two-writers fight, not a broken
 writer.
- **The editor compiler can crash internally (NullReferenceException, "One or more errors
 occurred") and WEDGE: `compile_status` then reads `success=False` with 0 errors/0 warnings
 AND `needsBuild=False`** — it thinks nothing needs rebuilding while the last compile failed,
 so the editor silently keeps running the stale assembly forever. No code error exists to fix.
 Recovery: bump any watched source file's mtime (PowerShell `(Get-Item file.cs).LastWriteTime
 = Get-Date`) to dirty the compile; it rebuilds clean. Detect via the MCP `compile_status`
 (success=False + 0 diagnostics + needsBuild=False is the wedge signature).
- **Steam can update the s&box engine UNDERNEATH a running editor** — files swap mid-session
 and the symptoms look like impossible compiler bugs, in escalating order (observed 2026-07-10,
 26.07.08c->e): hotload "Failed to clean" reflection noise -> an internal Roslyn NRE
 (`GetExtensionContainers`) wedging the compiler (success=False, 0 diagnostics) -> on restart,
 `SerializationDeprecationException` + "Errors when loading 'Sandbox.Project'" (mixed-version
 DLLs), the scene deserializing with project components STRIPPED (MissingComponent — do NOT
 save the scene in that state, the on-disk file is still good), the package never mounting
 (type registry empty, nothing plays) -> "Could not find sbox-dev.exe" if relaunched while
 Steam stages that exact file. FIRST CHECK for any inexplicable editor-internal crash:
 `sbox/.version` mtime vs your session start (also `appmanifest_590830.acf` StateFlags —
 4 + all BytesStaged = update complete). Recovery once the update finishes: relaunch, discard
 scene changes, verify headless `dotnet build --no-incremental` against the NEW engine
 assemblies before trusting anything.
- **The s&box editor runs an MCP server on `http://127.0.0.1:7269/mcp`** — the
 headless verification loop for assets. Launch `sbox-dev.exe -project <sbproj>` in
 the background, then JSON-RPC `tools/call` → `call_tool` wrapping: `asset_compile`
 (returns Success/CompiledFile — THE way to verify a vmdl compiles), `asset_info`,
 `spawn_model` + `set_editor_camera` + `editor_camera_screenshot` (base64 PNG — eyes
 in the engine: verify orientation/scale against a known-good model side by side),
 `read_console`, `play_start/stop`, scene CRUD. `list_toolsets`/`describe_toolset`
 to discover schemas. Spawned objects live in the unsaved scene — don't save and
 they clean themselves up on exit.
- **Reusing another project's assets = copy with identical root-relative paths.** vmdl
 files reference their .obj and .vmat by project-root-relative path, so copying
 `models/tree_oak.*` + `materials/flat/*.vmat` +
 `materials/textures/white.png` into the same subfolders of the new project
 works with zero edits. Copy the source files only (.obj/.mtl/.vmdl/.vmat/.png), not
 the compiled `*_c`.
- **Kenney's own nature pack is an even
 easier zero-edit borrow than another project's**: every vmdl there references its mesh as
 `models/kenney/nature/<name>.fbx` and a SHARED `models/kenney/nature/materials/*.vmat`
 folder (one universal `DefaultMaterialGroup` remap table, identical across all 20
 vmdls), and every one of those vmats points only at the ENGINE's own
 `materials/default/default_{color,normal,rough}.tga` — no project-local textures at
 all, just a `g_vColorTint` per material. So copying the vmdl+fbx pair for each chosen
 species plus the whole `materials/` folder into the same `models/kenney/nature/`
 subpath in the new project needs LITERALLY zero path edits. Kenney's `nature/` folder is the source for trees/pines/bushes/rocks/flowers/cliffs. Kenney's pack has NO
 dedicated grass-tuft mesh — reuse a small-scaled `plant_bush.vmdl` as the "tuft" stand-in
 until a real grass model is generated.
- **BoxCollider.Center/.Scale are ALWAYS in the model's own native (unscaled) local
 units, never world units** — the engine multiplies by the GameObject's WorldScale
 automatically (this is exactly why BoxCollider, unlike Capsule/ModelCollider, "follows
 WorldScale": the follow-through happens because the collider's own numbers stay in
 local space and get scaled downstream). The established pattern everywhere in this repo
 is literally `c.Center = model.Bounds.Center; c.Scale = model.Bounds.Size;` — bounds are
 already native-unit, so no conversion is needed for a bounds-fit box. The mistake is
 reaching for a DESIRED WORLD-SPACE size (e.g. "a 24 cm trunk radius") and assigning it
 directly to `c.Scale`: on a GameObject scaled up 5-10× (a normalized 1 m Kenney mesh
 stretched to a 4 m tree) that world-space box would compile fine but end up 5-10× too
 BIG in-engine. Fix: divide the desired world size by `go.WorldScale` (or the relevant
 axis) before assigning, the mirror-image of the CapsuleCollider/ModelCollider bake-scale
 fix already in this file — BoxCollider needs a divide-back, those need a baked-in scale.

 **Second confirmed instance + companion bug**: a component authored assuming
 `WorldScale==1` (a bounce-pad mushroom whose stem `BoxCollider` used raw world-meter
 constants like `0.34f*M` half-width) breaks the moment a caller sets a different
 WorldScale (e.g. spawning size-VARIANTS of the same prop) — the constant needs
 dividing by `WorldScale` before assignment, same as above. The COMPANION mistake in
 the same component: a `model.Bounds` field (e.g. `Bounds.Maxs.z`) used to compute an
 absolute WORLD-space value (a bounce plane's world Z) must be MULTIPLIED by
 `WorldScale` first — bounds are native/unscaled, so using them raw in world-space
 math is correct only at scale 1. Before scaling any existing component via
 `GameObject.WorldScale`, grep its OnStart/OnAwake for both directions of this bug.

- **s&box whitelist is only enforced by the IN-EDITOR compiler** - `dotnet build` passes code that the game runtime then rejects (e.g. `Environment.TickCount` -> SB1000 whitelist violation). Fix: engine surfaces only (`Time.Now`-derived entropy for seeds, `System.Random(int)` itself IS whitelisted). Sweep agent code for `Environment.*`, `System.IO`, `Process`, `Thread` before shipping.

- **Runtime reflection is ALSO whitelist-banned** in s&box game code - `Type.GetProperty`/`PropertyInfo.SetValue` -> SB1000, same as `Environment.*`. Never use reflection as a cross-file workaround for a property another agent owns; emit a HANDOFF instead.

- **Model.Load of a MISSING vmdl can return the error model with the REQUESTED path as its Name** - the classic `Name.Contains("error")` check passes and you spawn the engine's giant orange ERROR-text mesh. Robust gate: `model == null || model.IsError || model.Name.Contains("error")`.

- **A self-swept projectile + a separate "did it hit me" detector must NOT both collide with the same collider — the projectile bounces off before the detector's poll runs.** one project's sweep-traces its own ballistic path each fixed tick; BreakableGlass polls live props for a plane-crossing to shatter. Both saw the pane's collider, so (with no guaranteed OnFixedUpdate order between the two components) the prop's flight trace hit the glass and bounced BACK before the glass ever detected the crossing → the pane never broke. Fix: tag the breakable collider with a distinct tag (`"breakable_glass"`) and have the projectile's flight trace `.WithoutTags("breakable_glass")` so it PASSES THROUGH, while the detector (and the character's wall-run/collision, which don't exclude the tag) still treat it as solid. General rule: when object A is meant to pass a barrier that a poller B watches for the crossing of, A's trace must exclude B's collider tag — don't rely on tick order.

- **Free a penned AI by WIDENING its public wander rect, not by scripting an exit path.** one project's openable area frees pen animals by growing each AnimalNpc's public `[Property] PenMin/PenMax` rect to a box centred OUTSIDE the opening — its own wander AI (which reads PenMin/PenMax live each PickWanderTarget) then paths it out through the gap on its own, no bespoke escort code, no reach into AnimalNpc internals. The one catch: the opening must line up with an actual GAP in the containing fence, or the sphere-traced movers pile up against it. Put the openable door AT the pre-existing walk-path gate gap (FencePen already skips fence panels inside a walk corridor) so door-open == fence-open at the same x.
- **Blender `materials.clear()` + `materials.new(name)` = the `.001` checkerboard trap.** `mesh.data.materials.clear()` only detaches the datablock from the slot list — it stays alive in `bpy.data.materials`, so a follow-up `materials.new(same_name)` dedup-renames to `name.001`. That `.001` becomes the FBX material slot key, the vmdl remap (listing the clean name) can't match it, and the engine loads `name.001.vmat` → File not found → red checkerboard in-game. The `.001` lives in the FBX *binary* — grepping vmdls finds nothing; grep the `.fbx` bytes. Fix: purge stray `name`/`name.NNN` datablocks and force-rename the slot before export (`sanitize_material_slots` in the quadruped rig library/the NPC rig library). Hit 10 rigs (8 animal rigs plus humanoid rigs); the hero character escaped only because its OBJ used a hyphen where the rig used an underscore.
- **Coplanar overlay ribbons z-fight — give each layer its own lift.** Roads and walk paths both seated at `terrain + 0.06 m` produced camera-dependent tan streaks wherever a path's segment overlapped a road (every path starts on one). One shared lift constant for two overlay types = guaranteed z-fight at every junction. Fix: layer the lifts (roads 0.06, walk paths 0.04) so the intended winner always renders on top; both stay proud of terrain. General rule: any two procedurally-laid coplanar surfaces need an explicit z-order via distinct lifts.
- **Never publish live mutable static Lists to per-frame consumers — hotload's ListUpgrader reallocates them mid-tick.** NavData exposed `static readonly List<Vector3> StreetLoop` that the builder `Clear`+`Add`ed and every GuardNpc/Wanderer indexed each frame. A hotload (or rebuild) reallocates/shrinks the backing array BETWEEN a consumer's `Count` read and its `list[i]` — IndexOutOfRange every frame on every consumer at once, even ones with modulo guards (the race is on the list, not the index). The log smoking gun: `[hotload/GameMenu] Destination array is too small … ListUpgrader`. Fix: publish `IReadOnlyList<T>` and REBIND the field to a fresh list per rebuild (one atomic reference assignment); consumers capture `var loop = NavData.StreetLoop` once per tick and get an immutable snapshot. Belt-and-suspenders in consumers: empty → skip tick; wrap with modulo of the CURRENT count; rate-limit the warn.
- **Hysteresis lives in the CONSUMER, never in the stateless predicate tests read.** The guard's 15 Hz Spotted/LostSight flap: `TryCatchScan` read raw `CanSee(character)` per frame; on a "seen" frame the guard turned toward the character, on the next the patrol mover re-aimed his gaze at the waypoint, flipping the cone verdict — two facing authorities fighting at frame rate, so pursue never accumulated. Fix pattern: keep `CanSee` a pure stateless truth table (the test scenarios assert its exact per-gate reasons), add a `SeesSticky(target, dt)` wrapper that (a) holds sight until the lose-condition persists ~0.6 s and (b) widens the effective cone hugely under 3 m (nobody loses a character at their feet by turning their head), and make the tracking state own facing for the whole tick. Sight telemetry logs on rising/falling EDGES only.
- **Third-person camera occlusion: ease asymmetrically and tag-exclude thin structure — never pull in instantly.** An occlusion trace that snaps the boom to every hit turns any beam/rib thicket into per-frame distance jitter (the roof-swing jank). The fix trio: (1) ease the pull-IN too (fast k≈22), keep recovery slow (k≈6); (2) ignore transient slivers — a clamp shortening the boom <0.6 m while the prior clamp is clear is a rib ticking past, not a wall; (3) give thin structural members a `camstruct-ignore` tag excluded from the CAMERA trace only (they stay solid for movement) — a beam briefly crossing the lens beats a distance snap. Related: on play-start, SNAP the camera to its fully-resolved pose on first acquire, then ease — lerping out of the menu-camera's pose reads as "camera starts too close and crawls out".
- **Static input gates leak across play sessions — reset them in OnStart AND OnDestroy.** `static bool IsOpen` style gates (EscapeMenu, CeremonyScreen) survive Play→Stop→Play within one editor process. A session ending with the gate set freezes the next session (character wouldn't move until the menu was toggled, resyncing the static). Rule: any static a gameplay loop reads must be hard-reset when its owning component spawns and cleared when it's destroyed. Grep `static bool` near input checks when adding UI overlays.
- **"Everything broke at once" during agent waves = check for a stale assembly FIRST.** When the package compile fails (an agent mid-edit), the s&box editor silently keeps running the LAST GOOD hotloaded assembly — so a playtest can exhibit "regressions" from code that predates hours of committed work (audio dead, features missing). Before debugging any multi-symptom regression reported during concurrent agent edits, grep the log for `Compile of ... Failed` / `Broken Reference` in the session window. If present, the fix is "finish/fix the broken edit + restart Play", not the symptoms.
- **SoundHandle spin-up: freshly-played handles report IsPlaying=false for a frame or two.** A loop-retrigger keyed on `!IsPlaying` fires overlapping duplicate plays every spin-up frame — stacked copies read as a garbled rumble. Retrigger only on invalid-or-Finished, and Stop any lingering handle before replacing it.
- **Sounds/assets compiled AFTER a session started are invisible to that session — and a retry loop on them floods every frame.** The editor builds its asset registry at session start; .sound_c/.vsnd_c produced later (by another session, an agent's compile, or a git checkout) exist healthy on disk but "Couldn't find sound event" until an editor kick re-indexes. Two rules: (1) mtime staleness is a RED HERRING — prove it with a known-working control asset with identical mtimes before chasing it; (2) never per-frame-retry a failed loop: retry once after ~2s, then DISABLE with one warn (a permanently-invalid handle otherwise = one engine warning per frame per loop). A zero-volume canary probe of a few cross-bus events at first Sfx use turns "audio mysteriously dead" into one log line naming the editor kick.
- **.sbproj `Org` must be a valid lowercase package ident — a placeholder breaks engine BOOTSTRAP, not just publishing.** Setting Org to an uppercase TODO string made `Sandbox.PackageLoader.LoadAssemblyFromPackage` assert at editor startup ("File doesn't exist? Maybe a case sensitivity issue??") — the whole editor fails to boot, which reads as engine corruption, not a config typo. Keep `local` for dev; set the real org only when it exists, and never park a non-ident placeholder in that field (document the TODO in a doc instead).

- **Copied trace exclusion filters invert meaning across call sites - re-derive "who am I,
 who is the target, who is noise?" at every copy.** `.IgnoreGameObjectHierarchy(GameObject)`
 copied from SwingRope.ResolveRoofMount (correct: a rope raycasting UP for the roof must skip
 its own knot/segments) into ClimbNodeField.TraceInward (fatal: the field lives ON the tower
 it surface-fits, so it ignored the very collider it was fitting - every inward ray missed,
 0 nodes placed on every tower, the whole cylinder climb dead, silently). For surface-fitting
 prefer a POSITIVE filter: accept only hits on the intended hierarchy, stepping past foreign
 hits - also stops neighbouring geometry donating phantom nodes.
- **Visual "pin hands/feet to a world feature" welds = the DIFFERENCE of two measured lengths,
 never either length alone** - (physics offset of the root from the feature) minus (measured
 offset of the body part above the root in the ACTIVE clip). The bar-swing weld lifted the
 mesh by the full BarSwingRadius (0.7 m) when the bar_spin clip's hands already sit a measured
 0.664 m up the body - hands ended 0.66 m PAST the bar (owner: upside-down character, hands on
 nothing). Correct lift = 0.7 - 0.664 = ~0.04 m. Each swing surface plays its OWN clip
 (bar_spin vs hm_swing), so each gets its OWN measured constant (tools/measure_hang_pose.py
 measures both).
- **A [telemetry] boot line gated behind a debug/viz flag is not telemetry.** ClimbNodeField's
 only per-field boot line lived inside BuildDebugViz, so ShowDebug=false would have hidden
 the one line proving a field was dead (0 valid nodes). Emit per-system boot proof lines
 unconditionally in OnStart; gate only the visuals. Pair with a loud WARN on the dead state
 itself.
- **Nearest/Best pickers need an honest EMPTY answer** - ClimbNodeField.Nearest fell back to
 `_nodes[0]` when no valid node existed, which for a failed cylinder fit was an INVALID gap
 node at the trunk AXIS: climb entry teleported the body INSIDE the mesh and instantly
 ejected (Air->Climb->Air flicker). Return null and make the caller fall back explicitly
 (free climb + warn).
- **Build-time tag scans are blind to children built in a component's OnStart** - SwingRope
 builds its tagged knot child on its first enabled frame, AFTER the world Build returns, so
 any boot audit scanning for "grabpoint" tags undercounts every rope (fixed-web=15/26 with 4
 spurious gap-fillers patching holes that closed a frame later). Fix pattern: a single
 COMPONENT-DERIVED census - derive each future child's rest position from the component's own
 fields (pivot + Down*Length, exactly what OnStart will seed) under the name it will get, and
 name-dedupe so a post-frame-1 run counts identically; all consumers share the one census.

- **The editor ignores external edits to persisted settings.json** - GameSettings caches its
 file read once per editor-process lifetime (`static bool _loaded`), so editing
 `data/local/<ident>/settings.json` on disk while the editor runs is silently ignored - a
 benchmark launched after flipping QualityPreset 0->2 externally still printed `preset=Low`.
 No stock ConVar flips quality in-process either. Headless preset changes need an in-game
 Video-tab click or a project `[ConVar]`-backed setter (planned: gb_quality_preset).

- **The engine ships its own built-in `[McpTool]`s as readable C# source, not just compiled
 DLLs — read it before inventing a pattern.** `<path> Files (x86)\Steam\steamapps\common\
 sbox\addons\tools\Code\Mcp\*.cs` (`Scene.cs`, `EditorTools.cs`, `AssetSystem.cs`, `Play.cs`,
 `Packages.cs`, `Log.cs`, `Components.cs`) is real source, ProjectReference'd (not just
 Reference'd) into every project's `.editor.csproj` — that's why it's on disk in full.
 one project's `Editor/SfxProbeTool.cs` (the usual "how to write a project [McpTool]"
 reference) is a pure resource-lookup tool and never touches the scene, so it can't show you
 the canonical "get the active editor scene" pattern — that lives in `Scene.cs`:
 `SceneEditorSession.Active?.Scene ?? Game.ActiveScene` (see `SceneTools.ResolveScene` /
 `ActiveSession`). Same file is also the reference for viewport screenshots
 (`Application.Editor?.Camera` + `camera.RenderToBitmap(bitmap, false)`), moving the editor
 camera (`SceneViewWidget.Current?.LastSelectedViewportWidget`), and resolving game objects by
 guid across every open `SceneEditorSession`. Read this before SfxProbeTool.cs for any new
 project [McpTool] that needs the scene, camera, or selection. Two attribute forms both
 compile: the house convention (SfxProbeTool.cs) is
 `[McpTool( "name", Hints = McpToolHints.ReadOnly )]`; the engine's own tools use the shorthand
 `[McpTool.ReadOnly( "name" )]`.
- **`Angles` struct fields are lowercase `.pitch/.yaw/.roll`, `Vector3` fields are lowercase
 `.x/.y/.z` — do NOT trust capitalized `.Pitch/.Yaw/.Roll` sightings elsewhere in a codebase
 as proof of the struct's own casing.** Grepping this dev folder turns up BOTH casings: the
 lowercase form on `Angles`/`Vector3` themselves (`drive/Code/MapGen/AddressMapBuilder.cs`
 `heading.x, heading.y`; `addons/menu/Code/AvatarEditor/AvatarEditManager.Camera.cs`
 `_angle.yaw`/`_angle.pitch`/`_angle.roll`), and a capitalized form that belongs to unrelated
 component properties (e.g. `CitizenAnimationHelper.Pitch/Yaw/Roll` are the animation rig's
 own float properties, nothing to do with the `Angles` struct). Confirm field casing against a
 usage of the SPECIFIC type you're touching, not the first case-matching hit in a repo grep —
 it's a silent wrong-value bug (compiles fine, animation/camera math is just off), not a
 compile error.
- **`System.Text.Json.Nodes.JsonNode` has implicit conversions from primitives (int, long,
 float, string, bool, …) since .NET 8** — `new JsonObject { ["key"] = someInt }` and
 `new JsonArray { someFloat, someFloat }` just work, no `JsonValue.Create()` wrapping needed.
 Confirmed against the net10.0 TFM s&box projects use. Reach for this (not `Sandbox.Json.
 Serialize`) whenever an `[McpTool]`'s return contract mandates exact key names/casing —
 hand-building the `JsonObject` guarantees the keys match the contract regardless of whatever
 naming policy the engine's own serializer defaults to.
- **An aimed bot grabbing through a scored "best in reach" selector must hold grab ONLY while
 the intended target is inside hand reach** — holding at intent time catches whichever
 bystander enters the assist window first (11/11 wrong-grabs across three an agent suite
 runs had the wanted element ≥3.5 m away at the steal). Check where the hand-open sphere
 trips along the approach: a bystander equidistant at that point coin-flips the selector —
 shrink the window past the crossover.
- **A bar's release can only be aimed within its spin plane** — max velocity·target align =
 |target direction projected into the plane| (measured: a release at 0.56 vs computed cap
 0.57). Release gates above the geometric cap never fire; the pendulum creeps to the orbit
 top and stalls. Compute the cap per hop, gate under it, log best-seen align on every
 timeout.
- **Threshold-pair event detectors die on smooth physics: check the transition is producible
 in ONE fixed tick.** `prevVz > 0.5 && vz <= 0` can never fire under gravity at 50 Hz
 (~0.2 m/s per tick through zero); a bounce-pad impact IS one-tick detectable
 (≤−1.5 → ≥+1.5 flip).
- **The editor MCP server's ENABLE is a MANUAL per-editor-instance Preferences step — a running
 editor may serve NO MCP at all — and the PORT is a single GLOBAL cookie persisted in
 `sbox/config/tools.json` as `McpServerPort`.** The enable state is still not automatable from
 code, so a freshly-launched editor serves nothing until toggled via Edit → Preferences → MCP
 Server. The cookie holds the last-set value across ALL editors, so switching projects may show a
 stale port — re-check before enabling. Editors auto-increment past taken ports rather than
 staying on a hand-set per-project port. To find who's ACTUALLY serving, don't trust netstat's
 PID column (http.sys routes show PID 4): run `netsh http show servicestate view=requestq |
 findstr HTTP://` to list active listeners.
- **On a shared multi-agent working tree, ONE agent's uncompilable file reddens the WHOLE editor
 assembly and silently blocks every other agent's live MCP loop.** The s&box csproj is
 SDK-glob (no explicit `<Compile>` items), so it compiles every `.cs` under `Code/`; a concurrent
 UI agent's half-written `UiRig.cs` referencing a not-yet-created `SessionMenu` failed the whole
 `the vehicle project.csproj` (and, transitively, the `.editor.csproj`), which would leave the
 editor on a stale assembly / `compile_status` red for an unrelated harness agent. Judge only YOUR
 files with `dotnet build … | grep ": error" | grep -v "\\<other-scope>\\"` (empty = yours are
 clean); do NOT "fix" the other agent's file — the owning agent completes it (it did, and the tree
 went green).
- **Adding a `public` method to the GAME assembly and calling it from the EDITOR-tools
  assembly in the SAME hot-reload pass throws `MissingMethodException`** — even though
  `compile_status` is all-green and `dotnet build` is 0/0. The running editor assembly
  re-JIT'd against a stale copy of the game assembly that predated the new method. Fix:
  restart Play (a fresh Play session loads both assemblies together); the exception is
  transient to the cross-assembly hot-reload seam only.
- **`Array.Clone()` is NOT whitelisted in the game assembly** — `(short[])arr.Clone()`
  compiles headlessly but fails the in-editor compile with `SB1000 'System.Array.Clone()' is
  not allowed when whitelist is enabled`. The old assembly silently keeps running. Fix: use
  `var copy = new short[a.Length]; System.Array.Copy(a, copy, a.Length);` — `Array.Copy` IS
  whitelisted. Always check `compile_status` after a game-assembly edit.
- **The `FileSystem.Data` multi-file API surface is whitelist-clean and works at runtime for
  a named-slot save system:** `WriteAllText`, `ReadAllText`, `FileExists`, `DeleteFile`,
  `DirectoryExists`, `CreateDirectory`, `FindFile(dir, "*.json")` all compile in-editor AND
  execute. Paths are relative to the project data root; sub-dirs like `saves/<slug>.json`
  work. Sanitize any user-derived slug (drop `/ \ .`) to prevent path traversal.
- **`editor_status` over the editor MCP can transiently return `Project: null` while
  mid-hotload** (right after a source change compiles). An identity-probing harness then
  aborts with a false "wrong port / wrong project" even though the port and project are
  correct. Fix: treat a null Project as "editor busy", wait ~10–15 s and re-probe before
  concluding wrong-port; only a different non-null project name means wrong port.
- **On Windows, invoking `python3` from a project tool aborts with exit 49** ("Python
  was not found; run without arguments to install from the Microsoft Store") even though
  Python is installed and on PATH — `python3` resolves to the WindowsApps App Execution
  Alias shim, not the real interpreter. Use `python` or the launcher `py` instead.
- **Porting a `.cs` file into another s&box project with its doc comments intact: a
  `<see cref="TypeYouDidNotCopy"/>` in an XML `///` comment emits CS1574** ("XML comment
  has cref attribute that could not be resolved") — which quietly fails a "0 warnings"
  acceptance gate even though the code is correct. For a small port: grep for `cref=`
  and strip or redirect references to types you didn't copy. For a LARGE vendored
  slice you want to keep drift-diffable against upstream: prepend
  `#pragma warning disable 1574` to each vendored file instead of scrubbing the
  crefs — keeps bodies byte-identical to the source for drift-sync.
- **The editor MCP `camera_screenshot {includeUi:true}` captures base HUD panel elements fine
  but DROPS a centered full-screen modal card** — even one on the same ScreenPanel. The
  backdrop's dim/blur composites (the scene visibly dims) but the card and all its children
  never appear. This is a render-pass artifact of the modal sub-layer, not layout. Workaround:
  bind the asset-under-test to an always-visible base HUD element (which does composite),
  screenshot that, then revert.
- **Game code cannot subscribe to the engine log stream — `Sandbox.Diagnostics.Logging` is
  `internal`.** `Logging.OnMessage += handler` fails to compile (CS0234). Only
  `MenuUtility.AddLogger` / `EditorUtility.AddLogger` are public (menu/editor realm, not game).
  Workarounds: (1) derive events from observable side effects instead of log parsing; (2)
  bracket runs with grep-able START/DONE markers and have the outer tool grep the console
  between them.
- **When the whole HUD is gated behind a static flag on a static class, an MCP test harness
  that only pokes gameplay singletons can drive every gameplay verb yet capture ZERO HUD** —
  the flag never flips, the `@if` renders nothing, and screenshots show only the start menu.
  `set_component` reaches only component `[Property]` fields, never static class fields.
  Fix: expose the gate as a `[Property]` on a component, or skip HUD capture for automated
  runs and verify HUD separately.
- **Moving legacy assets out of a publish payload with `git mv` alone leaves the shipped bytes
  behind AND a source-only dependency closure misses shared textures.** Compiled artifacts
  (`*_c`, `*.generated.*`) are git-ignored, so `git mv` only relocates sources. AND a
  `.vmat`/`.vmdl`-only move misses shared `.vtex` files referenced by the material. Combine
  the `git mv` with a `git rm` of the compiled artifacts and trace the full texture closure.
- **The editor serializes its STALE in-memory `.sbproj` on any settings/wizard save, reverting
  on-disk hand-edits.** A `Resources` glob added by editing the file while the editor was open
  gets dropped without warning when the publish wizard runs. After any editor settings interaction,
  re-read the sbproj on disk and restore hand-edits (`git diff -- *.sbproj`). The publish wizard
  also PACKAGES from the stale in-memory copy — an editor restart (or editing through the Project
  Settings UI) is the only fix.
- **`editor_camera_screenshot` renders the EDITOR viewport (the edit scene), NOT the running play
  scene.** Anything that exists only at runtime (procedurally generated terrain, play-mode-spawned
  objects) is invisible to it. Capture the play scene with the `scene` toolset's
  `camera_screenshot` instead (arg = a CameraComponent/GameObject id, or omit for the scene's
  main camera).
- **A newly added Component type can be compiled-but-not-yet-registered in the TypeLibrary
  right after the editor loads the assembly.** `compile_status` is green but playing a scene that
  references it logs `TypeLibrary could not find <Type>` and silently skips the component (its
  `OnStart` never runs). Resolves on its own after a short window. Gate: re-query
  `get_component_type` before concluding the component is broken; if it now resolves, reboot play.
- **The editor MCP resolves only ONE component per GameObject.** `set_component {type}` fails on
  any secondary component with `has no <Type> component`. Workaround: drive behavior through the
  component you CAN reach, or put the thing you need to script on its own single-component
  GameObject.
- **There is NO scene/global time-scale API in the game-reachable engine surface (26.07).**
  `Scene.TimeScale` does not exist; the only `TimeScale` anywhere is
  `ParticleEffect.TimeScale`/`PerParticleTimeScale`. Slow-motion must be faked with a custom
  multiplier applied to delta-time in your own update loops.
- **The stock `PlayerController` exposes NO public speed property.** `WalkSpeed`/`RunSpeed`/
  `DuckedSpeed` are private serialized `[Property]` fields. Game code cannot cleanly scale
  player move speed (e.g. for a slow debuff). Write your own controller or reflection-hack
  the privates (fragile across updates).
- **Re-setting a `[ConVar]` to the value it already holds is a SILENT no-op** -- the property
  setter never runs. Because statics survive Play stop/start, a convar-triggered action from
  a prior session leaves the convar "already set," so a fresh session's identical command does
  nothing. Fix: toggle to a different value first, or reset the backing static on boot.

- **Headless Blender 5.2 drops the old compositor API silently** — `Scene.node_tree` / `CompositorNodeComposite` removed; use `scene.compositing_node_group` + `NodeGroupOutput`, or skip the compositor entirely with an opaque-floor render recipe.
- **Git worktree at a different directory depth breaks sbox csproj relative refs** — the generated `.csproj` uses deep `../` paths that only resolve at the main checkout's depth. Build from a worktree-local `.csproj` with absolute paths.
- **Headless Blender opaque-floor render recipe** sidesteps compositor API churn entirely — an emissive-grey world + albedo-matched diffuse floor + the subject's own cast shadow; no film transparency, no compositor graph needed. Use Standard view transform, not AgX.
- **`new System.Random` (time-seeded) IS whitelisted** — compiles + runs headless. Useful for per-LOAD procedural variation (verified: arched-roof open-pane picker uses `new System.Random; rng.Next(n)`). Note the earlier "no crypto RNG" gotcha is specifically about `HashCode.GenerateGlobalSeed`/`RandomNumberGenerator`, NOT `System.Random`.
- **Whitelist:** `Http` may call any domain (not raw IPs). Vendored libs need patches: no `System.Console`, no crypto RNG (`HashCode.GenerateGlobalSeed`), no `Environment.NewLine`. `System.Text.Json`, `ArrayPool`, `GameTask.RunInThreadAsync` are all fine. (drive)
- **Hotload:** C# edits apply in ~ms on alt-tab. Network sessions reset on hotload; scene-file changes need a Play restart; new assets sometimes need an editor kick. (drive)
- **Engine console noise is normal**: missing `citizen_clothes/…`, `sfm` sounds, `menu-main.scene` sounds, wheely-bin gibs etc. are broken refs inside Facepunch's own base content — never ours. Vulkan `QueuePresentAndWait`/`VK_TIMEOUT` = driver-level present stall (alt-tab/shader compile), not game code.
- **s&box DOES ship a first-class headless dedicated server, and it can run an UNPUBLISHED local `.sbproj` — no sbox.game publish required.** It's a separate Steam app (**`1892930`** — the client is `590830`), installed via SteamCMD with **anonymous login** and launched headless (no GPU/render window) with `sbox-server.exe +game <ident|C:/path/to/proj.sbproj> +hostname <name> [+net_game_server_token <tok>] [+port +net_query_port]`. Pointing `+game` at a **local `.sbproj` path** makes connecting clients stream+hotload the project's code/assets straight from the server — the sanctioned dev/iteration path (also the only supported way to run server-only code, since published sbox.game packages have server code stripped). Discovery is via **Steam lobbies / the multiplayer UI, NOT a paste-an-IP browser**; the dedicated server surfaces to clients when `Networking.QueryLobbies(filters, includeServers:true, ct)` is called with `includeServers = true` (the bool most lobby code leaves `false`), then `Connect(id)` joins it. TRAPS: (1) refresh SteamCMD's app metadata before choosing a branch: `+app_info_update 1 +app_info_print 1892930`. On 2026-07-12 the first public install attempt returned `Missing configuration`, while staging installed 3.28 GB and booted successfully as server `26.07.12`; after the metadata refresh, public installed successfully as `26.07.08e`, exactly matching the installed client. Prefer the matching branch — a staging server + public client discovered each other but the client then failed loading `local.`. (2) `+game <path.sbproj>` LOADS the project but does NOT activate `Networking`: the game must call `Networking.CreateLobby` itself (or it silently runs a headless single-player world). A dedicated process cannot call `Networking.QueryLobbies` (`unable to query lobbies on a dedicated server`), so its host path must skip short-code collision searches and create/advertise directly. (3) Steam drops custom `Networking.SetData` when aggregate game tags exceed **128 characters** — live log: `Steam game tags exceed 128 char limit ... Data dropped`; use tiny discovery metadata such as `g=gb,p=1,d=1`. (4) The older public build was documented STALE (Nov-2023 `.version`, didn't recognise `+game`) in Facepunch/sbox-public issue #3356, so always compare `.version` to clients. (5) `net_game_server_token` (persistent server Steam ID) is documented as available "once the game is publicly released" — whether a local/unpublished (`Org: local`) project gets a usable token + shows up in discovery is UNVERIFIED, test before relying on it. Do NOT trust nexus-games-style guides using `sv_tickrate`/`dm_crossfire`/ `workshop.txt` — those are Garry's Mod/Source-1 and don't apply to s&box.
- **Compiling AND loading hundreds of runtime-written vmdls in one warm editor session can exhaust the resource-stream VM and CRASH the editor: `CResourceStreamVM::Commit: Failed trying to Commit 65536 bytes, used 0 bytes, reserved 67108864 bytes`.** A world exporter baked ~900 land + ~418 water per-chunk vmdls, `AssetSystem.RegisterFile`'d them all (which compiled fine), then **cloned the full 1318-child prefab — loading every compiled model at once** — and the editor died on the resource commit. The crash is on the render/STREAM commit, not the file write or the compile. Mitigations: (1) spawn/load in SMALL batches (≤ 6–9 models) with brief waits — 6-at-a-time loaded 36 chunks with zero pressure; (2) for a visual check spawn only a contiguous central block (~25–36 chunks), never the whole world; (3) for a genuine full-world bring-up use a FRESH session (restart between the bake and the mass load) so pressure resets. Note the global MCP-server port moves on the crash/restart — re-identity-probe before the next call.
- **A green `compile_status` (and green headless `dotnet build`) can coexist with a STALE play-mode hotload that reports errors for symbols/lines which no longer exist in source.** After a burst of edits that removed a type member (deleted `CarDefinition.BodyModel`/`HatchFused`), `play_start` spewed recurring `Warn: Error | 'CarDefinition' does not contain a definition for 'BodyModel' - VehicleFactory.cs:402,41` and the default spawn silently failed — yet the editor's authoritative `compile_status` showed all compilers Success=true/0 errors AND `dotnet build` was green AND the on-disk file had none of those lines. The play-mode hotloader had compiled an intermediate/cached snapshot from mid-edit (the reported line numbers kept SHIFTING across boots as it caught different stale states). Two ways to tell it apart from a real failure: (1) the offending errors are BUFFERED in `read_console` with OLD timestamps — filter by time and look for the post-recompile SUCCESS log (`[vp] partkit '<kit>' body assembled: N/N`) to confirm the live assembly is current; (2) force a clean re-read by `os.utime`/`touch` on the edited `.cs` files, wait ~8-12 s for the `IsBuilding` cycle, re-check `compile_status`, THEN re-boot — the stale errors stop and the spawn works. Corollary: deleting compiled `_c` assets (sounds here) out from under a LIVE editor emits one-time `Detected Asset File Deleted` + `Cannot recompile: Source file not present` + `ERROR_FILEOPEN` warnings as the watcher drops them from the index; these SELF-HEAL and do not recur on later boots — not a regression.
- **`[GameResource]` and `NavMesh.GetSimplePath` are `[Obsolete]` (CS0618) on engine 26.07 — a zero-warnings build gate fails on both.** Modern replacements, each compiles clean: (1) mark a `GameResource` subclass with **`[AssetType(Name = "...", Extension = "npc", Category = "...")]`** instead of `[GameResource("title", "ext", "desc", Icon = "...")]` — `GameResourceAttribute` now derives from `AssetTypeAttribute` and is deprecated; `AssetType` exposes `Name`/`Extension`/ `Category`/`IconColor`/`Flags` (there is NO `Description` or `Icon` ctor arg — drop them). The base class stays `: GameResource`. (2) Replace `scene.NavMesh.GetSimplePath(from, to)` (returned a `List<Vector3>`) with **`scene.NavMesh.CalculatePath(new Sandbox.Navigation.CalculatePathRequest { Start = from, Target = to })`** → returns a `NavMeshPath` struct with `.IsValid` + `.Points`, an `IReadOnlyList<NavMeshPathPoint>` where each point exposes a `.Position` (Vector3) — it is NOT a `List<Vector3>`, so index `path.Points[i].Position`. Related surface that is NOT deprecated: `Scene.NavMesh.IsEnabled` (gates whether a baked navmesh exists — the scene default is disabled), and `GetRandomPoint(pos, radius)` / `GetClosestPoint(pos, radius)` returning `Vector3?`. Found via offline metadata reflection (MetadataLoadContext over bin/managed) when building headless.
- **CORRECTED (was: "the `.archetype` extension is engine-reserved" — that theory was wrong; the real rule is a hard 8-CHARACTER CAP on custom GameResource extensions).** A custom GameResource with `[AssetType(Extension = "archetype")]` (9 chars) can never compile its assets: every file fails with "can't be recompiled - it has no source file (cloud and compiled-only assets can't compile)" while an identically-formatted sibling GameResource type compiles fine. It is NOT about the word "archetype" — a second attempt at `.charclass` (also 9 chars) failed the SAME way, and renaming to `.charcls` (7 chars) fixed it immediately (`asset_compile` Success, a real `_c` produced). **Rule: custom GameResource file extensions must be ≤8 lowercase characters, or the type silently never registers as a compilable SOURCE type.** Mechanism (two enforcement points, both length-based, neither name-based): (1) the obsolete `[GameResource]` attribute actively enforces this — `engine/Sandbox.Engine/Resources/GameResourceAttribute.cs:120-124` logs an error and TRUNCATES the extension to 8 chars; (2) the modern `[AssetType]` attribute (see `g-ui-gameresource-attribute-obsolete-use-assettype`) sets `Extension` via a plain property with **no validation at all**, and passes it straight to the native `IAssetSystem.UpdateGameResourceType` (`Sandbox.Tools/Assets/AssetType.cs:355`) — an over-length extension there just silently never registers as a compilable source type, no error, no truncation. That's why the symptom is misleading: the asset shows the right TYPE in listings, but `asset_compile` fails with the "no source file" message above, and an on-demand recompile emits no `_c` at all (console shows a `recompile` Info line then `ERROR_FILEOPEN` loading the missing `_c`). A second 8-char check lives at `GameResourceProcessor.cs:34`, which rejects asset-REFERENCE paths (not just the type extension) longer than 8 chars. Facepunch's own docs (sbox-docs `docs/assets/resources/custom-assets.md`) state the filetype must be all-lowercase and ≤8 characters "otherwise it will fail to register/save" — every first-party extension complies by abbreviating (`sndscape`, `tmat`, `clthgrp`). "archetype" DOES appear in the engine, but only as `ModelArchetype`, an unrelated ModelDoc/Hammer enum (`engine/Sandbox.Engine/Editor/Hammer/HammerAttributes.cs:10`) — there is no file-extension reservation of the word anywhere; a shorter synonym containing "archetype"-adjacent meaning would have failed identically at 9+ chars. Fix: keep any custom GameResource extension to 8 lowercase chars or fewer (moved to `charcls`) — don't grep the engine for your candidate WORD, count its LENGTH.
- **Hotloading does NOT unregister a `[AssetType]`/`GameResource` type registration — renaming a resource's extension and hotloading leaves the OLD extension registered ALONGSIDE the new one until the editor fully restarts.** After moving a GameResource from `Extension = "archetype"` to `"charcls"` (see `g-tool-gameresource-archetype-extension-reserved`) and hotloading, the editor's asset-type listing showed a DUPLICATE type entry carrying both `["archetype", "charclass"]` — the stale registration wasn't replaced or removed, just shadowed. A full editor restart cleared it back to the single correct entry. This is a red-herring generator when debugging resource issues: after renaming/removing an `[AssetType]` extension, don't trust the live editor's registered-types list (or any tool that reads it) until you've restarted — a hotload-only check can show the old extension as still "registered" when it's actually dead weight from before the rename.
- **PowerShell 5.1 corrupts UTF-8 on bulk edits** (`Get-Content`/`Set-Content` reads BOM-less UTF-8 as ANSI → emoji/dash mojibake). Use python or byte-safe .NET file APIs.
- **Some C# source files in are CRLF, and a `\n`-based string-replace edit silently fails to match them** (the search string has `\n`, the file has `\r\n`, so the multi-line `old_string` never hits — the byte content otherwise looks identical under `od -c` per-line, which hides the `\r`). Symptom: an exact-looking edit reports "string not found" repeatedly on a block you can see in the file. Diagnose with `python -c "print(b'\r\n' in open(p,'rb').read)"`; fix by editing via a python byte-level `data.replace(old_bytes, new_bytes)` with `\r\n` in the literals (assert the match count is 1 first), then write back `'wb'`. Mixed in the same repo — PoopTuning.cs is CRLF, PoopSplat.cs is LF — so check per file.
- Verify compiles without the editor: `dotnet build proj` — the csproj
- **`dotnet build` warning counts lie under incremental builds** — warnings are only re-emitted for files that actually recompile, so a stale-output build happily prints `0 Warning(s)` over a tree that carries warnings ( shipped a "verified 0/0" gate twice while a CS0162 sat in an unchanged test file). Any build-gate claim (docs, feature-matrix headers, agent verify clauses) must come from `dotnet build --no-incremental` (or a clean). Per-agent scope checks can stay incremental — it's the WHOLE-TREE 0/0 claims that need the rebuild.
- **Steam can update the s&box engine UNDERNEATH a running editor** — files swap mid-session and the symptoms look like impossible compiler bugs, in escalating order (observed 2026-07-10, 26.07.08c->e): hotload "Failed to clean" reflection noise -> an internal Roslyn NRE (`GetExtensionContainers`) wedging the compiler (success=False, 0 diagnostics) -> on restart, `SerializationDeprecationException` + "Errors when loading 'Sandbox.Project'" (mixed-version DLLs), the scene deserializing with project components STRIPPED (MissingComponent — do NOT save the scene in that state, the on-disk file is still good), the package never mounting (type registry empty, nothing plays) -> "Could not find sbox-dev.exe" if relaunched while Steam stages that exact file. FIRST CHECK for any inexplicable editor-internal crash: `sbox/.version` mtime vs your session start (also `appmanifest_590830.acf` StateFlags — 4 + all BytesStaged = update complete). Recovery once the update finishes: relaunch, discard scene changes, verify headless `dotnet build --no-incremental` against the NEW engine assemblies before trusting anything.
- **s&box whitelist is only enforced by the IN-EDITOR compiler** - `dotnet build` passes code the game then rejects (e.g. `Environment.TickCount` -> SB1000 whitelist violation). Fix: engine surfaces only (`Time.Now`-derived entropy for seeds, `System.Random(int)` itself IS whitelisted). Sweep agent code for `Environment.*`, `System.IO`, `Process`, `Thread` before shipping.
- **Runtime reflection is ALSO whitelist-banned** in s&box game code - `Type.GetProperty`/`PropertyInfo.SetValue` -> SB1000, same as `Environment.*`. Never use reflection as a cross-file workaround for a property another agent owns; emit a HANDOFF instead.
- **Static input gates leak across play sessions — reset them in OnStart AND OnDestroy.** `static bool IsOpen` style gates (EscapeMenu, CeremonyScreen) survive Play→Stop→Play within one editor process. A session ending with the gate set freezes the next session (the character wouldn't move until the menu was toggled, resyncing the static). Rule: any static a gameplay loop reads must be hard-reset when its owning component spawns and cleared when it's destroyed. Grep `static bool` near input checks when adding UI overlays.
- **"Everything broke at once" during agent waves = check for a stale assembly FIRST.** When the package compile fails (an agent mid-edit), the s&box editor silently keeps running the LAST GOOD hotloaded assembly — so a playtest can exhibit "regressions" from code that predates hours of committed work (audio dead, features missing). Before debugging any multi-symptom regression reported during concurrent agent edits, grep the log for `Compile of ... Failed` / `Broken Reference` in the session window. If present, the fix is "finish/fix the broken edit + restart Play", not the symptoms.
- **Sounds/assets compiled AFTER a session started are invisible to that session — and a retry loop on them floods every frame.** The editor builds its asset registry at session start; .sound_c/.vsnd_c produced later (by another session, an agent's compile, or a git checkout) exist healthy on disk but "Couldn't find sound event" until an editor kick re-indexes. Two rules: (1) mtime staleness is a RED HERRING — prove it with a known-working control asset with identical mtimes before chasing it; (2) never per-frame-retry a failed loop: retry once after ~2s, then DISABLE with one warn (a permanently-invalid handle otherwise = one engine warning per frame per loop). A zero-volume canary probe of a few cross-bus events at first Sfx use turns "audio mysteriously dead" into one log line naming the editor kick.
- **.sbproj `Org` must be a valid lowercase package ident — a placeholder breaks engine BOOTSTRAP, not just publishing.** Setting Org to an uppercase TODO string made `Sandbox.PackageLoader.LoadAssemblyFromPackage` assert at editor startup ("File doesn't exist? Maybe a case sensitivity issue??") — the whole editor fails to boot, which reads as engine corruption, not a config typo. Keep `local` for dev; set the real org only when it exists, and never park a non-ident placeholder in that field (document the TODO in a doc instead).
- ****Fixing a generator whose bad emitted data a consumer already compensates for =**
- ****Shared editor play mode: "two idle polls N minutes apart" is NOT a claim — another agent can**
- ****The s&box editor MCP has NO open/load-scene tool, and `play_start` only plays the**
- **'s `WorldGen.Regenerate(Scene, WorldSpec)` really is a drop-in static library for another sbox project — the whole Code/Gen + Code/Mesh + WorldSpec/Tuning/ set copied into and built 0/0 after severing exactly THREE code-level deps and a global-namespace rename.** Verified reuse recipe: (1) the wb Gen/Mesh/Game files are all GLOBAL namespace (no `namespace` decl); the destination project mixed a `` namespace with many global-namespace files, so generic copied type names share the global namespace and can collide — grep the destination for every copied top-level class name first (here: no literal collisions, but `Tuning`/`Noise` are landmines), then whole-word rename the collision-prone ones (`Tuning`→``, `Noise`→``) rather than wrapping in a namespace, so the bodies stay 1:1 re-diffable against wb. (2) The ONLY real external deps to cut were `.Apply` (playground owns lighting), the LowPoly/Smooth `HeightfieldMesher` branches (not copied — voxel-only), and `PropScatter.Scatter` (+ its placement audit) — everything else was self-contained. (3) The FNV-1a grid-hash determinism probe (.GridHash, grid arrays + dims only, NO scene reads) ports straight across and is the one-line determinism proof: two `Regenerate` calls of the same seed on the ACTIVE play scene logged byte-identical `hash=7c74aee7accee1fe` (100 chunks / 101244 tris) either side of a clear. Bake the material-path strings into a const pointing at the copied `Assets/materials/<dst>/` (2 live: terrain atlas + water; the sky path is dead once is dropped).
- **Run exactly ONE s&box editor per project, on its assigned MCP port — and verify a port's IDENTITY by its scenes/toolset, not by a project-name probe.** When `list_scenes` persistently disagrees with what the owner sees, the cause is usually a second editor: the MCP identity probe can't disambiguate (two instances of the same project report the same Project), and a stale/second instance answers on a different port. Concretely (2026-07-12): ' port 7269 was DOWN while two other editors were live — 7290 (vehicle_proto, identified by its `vehicle_proto` scene) and 7200 (, identified by its `*` toolset). Detect with `Get-NetTCPConnection -State Listen` over the port range + `list_scenes`/`search_tools` on each reachable port to read its real identity; NEVER `play_start` a port you haven't identified (you could drive the wrong project's scene). If the target editor isn't up, do all headless work + commit and hand the owner a manual runbook.
- **Driving the s&box editor over MCP in an edit -> regenerate -> screenshot loop silently renders the PREVIOUS assembly: successful hot-compiles log NOTHING to the console (only failures print "Compile of ... Failed"), and `compile_status` reports the LAST completed build — so it can read green while your newest .cs save is still queued or was never picked up.** Symptom: the "after" screenshot is pixel-identical to the "before" and you chase a phantom non-fix (or worse, a transient mid-edit compile FAILURE leaves the last-good assembly live and green-looking). Protocol that held up: after the last file write, poll `compile_status` until `IsBuilding=false` AND `NeedsBuild=false` AND `Success=true` for BOTH local.<project> and local.<project>.editor, sustained across ~3 polls / >=12 s, and check the console for "Failed" lines timestamped after your write — only then regenerate. Multi-file edit bursts also trigger genuine transient failures per intermediate save; they are noise, but the LAST compile must postdate the LAST write. A `touch` on the file re-arms the watcher if it missed a save.
- **If the editor is IN PLAY MODE, `camera_screenshot` renders the PLAY session's scene clone (frozen at whatever the world was when play started) while ``/grid tools keep rebuilding the EDIT scene — so every "after" screenshot silently shows a world from N regenerations ago.** Symptom cascade observed: two different-style regenerations produced pixel-identical screenshots; a probe shot matched a style generated 20 minutes earlier; `delete_game_object` on the world root did not change the render (it deleted the play clone's root); scene queries showed 1 clean world root (the play scene's). The tell: `play_start` errors "Already playing" / `editor_status.IsPlaying == true`. Play mode can be entered outside your control (owner at the desk, leftover test session), so an MCP evidence loop must CHECK `editor_status.IsPlaying` before generate-and-screenshot evidence passes and `play_stop` (or report) if unexpected. Edit-scene state survives play/stop; in-play scene edits are discarded on stop.
- **The `NodeClimbOn` routine op dismounts by KICKING OFF the wall (a climb/wall jump away from the face), landing back on the FLOOR — it is NOT a top-mantle onto whatever sits above.** (TickNodeClimb phase 3: `_jumpLatch` then `return State==Ground`.) So an automated MULTI-STAGE climb (climb panel1 → stand on a ledge → climb panel2 off that ledge) is NOT achievable in a single routine: after stage 1 the piloted character is on the ground to the SIDE of the face, never on the ledge, and stage 2's field (based up at ledge height) is then unreachable from the floor. Build the staged structure as an OWNER-PLAYABLE layout feature, cover each stage's climb field with the climb-fields boot audit, and have the automated routine climb the reachable BASE stage only.
- **Lambdas stored in a STATIC collection get error-delegated on hotload — invoking one throws `NotImplementedException: Unable to find matching substitution for a lambda method` (`__error_delegate<NoMatchLambda>`), and because the static ctor does NOT re-run on hotload it stays broken for the whole editor session.** a project's `Buildables.All` holds `Action<GameObject> Attach` lambdas (`go => go.Components.Create<CropPlot>`). Editing Buildables.cs hotloaded and the editor log showed `[hotload/GameMenu] Warn: Unable to find matching substitution for a lambda method... Buildables::All[].Attach` + `Unable to find scope method (Name:.cctor, Ordinal: 4)`. Every spawn of a lambda-attached buildable (garden_bed -> CropPlot) then throws at `def.Attach.Invoke(go)` inside `Buildables.Spawn` — this is GAME-WIDE (BuildSystem.Place, SaveSystem load), not just the MCP path. Tell-tale: a buildable with NO Attach (fence/gate) spawns fine; a plain method call or a direct `go.Components.Create<T>` is fine — only the STORED delegate is stale. FIX (two levels): (1) a fresh editor restart re-runs the static ctor -> valid lambdas; (2) for a test harness that ALWAYS runs post-hotload, don't invoke the catalog lambda at all — add a `runAttach` flag so the tool spawns bare (`runAttach:false`) and re-attaches the domain component with a direct `Components.Create<T>` at the call site (JIT-resolved, never a stale delegate). Design lesson: prefer a switch/type-dispatch over lambdas stored in a static catalog if the path must survive hotload. (, Editor/SsTools.cs fence-first slice, 2026-07-12) UPDATE 2026-07-17 (TweakDials.All, dial Get/Set lambdas — the K tweak panel died session-wide with per-frame `Error calculating hash on <PanelComponent>... Unable to find matching substitution for a lambda method` warn spam after live-editing the dial table): fix level (3) — **`[SkipHotload]` on the static catalog field** makes every hotload skip the stale-value migration and re-run the initializer → fresh, valid lambdas, so the catalog survives live edits. Only safe when the catalog is STATELESS (rebuilt-from-code, e.g. dial rows reading/writing ConVar statics); state you want preserved must live in separate non-skipped statics. Already-wedged sessions still need the editor restart.
- **The s&box editor fires a one-time scene HOT-RELOAD ~15-20 s into a fresh Play session, which re-runs the scene bootstrap (rebuilds the world, RESPAWNS the player) and ORPHANS any suite that was armed by a console convar** — the fresh component instances start Idle, and a convar-armed-on-rising-edge suite never re-arms because the convar is already set (no new edge), so it wedges partway with the body frozen and no error. Symptom seen live: a battery consistently stalled right after the 5th metric, with "<pilot> attached" logged 3x + a character respawned at the spawn pad mid-run. WORKAROUND for a clean capture: play_start -> arm -> let the reload fire (~24 s) -> RE-ARM by toggling the convar 0 then 1 -> the re-armed suite runs to completion. It is an editor/session artefact (the very first post-build run and the shorter smoke suite both complete clean), not a harness or gameplay bug.
- **A STRUCTURAL `.razor` change (added markup/fields shifting the generated class + its `b__NN` lambda ordinals) can throw a live-editor HOTLOAD EXCEPTION that DEREGISTERS the whole project `[McpTool]` toolset while `compile_status` still reads Success=true - recover by RESTARTING the editor, not by more file edits.** Seen live ( M12.3, 26.07.08e): mid-edit the editor logged `[hotload/GameMenu] Error: Unable to resolve method definition ... GeneratorPanel.<TerrainStyleRow>b__74_0` + `Hotload exception`; afterwards `editor_status.ToolCount` dropped by exactly the project's tool count (the game assembly's failed hotload left the dependent `.editor` assembly unloaded, so every `*` tool answered `Unknown tool '...'`), yet `compile_status` reported `local.` AND `.editor` Success=true, NeedsBuild=false. DIFFERENT from the success=False compiler-wedge - here the COMPILE succeeded and the whitelist verdict is still trustworthy (Success=true, 0 SB1000 = the APIs passed), but the RUNTIME assembly swap failed. Neither `touch`, content edits, nor `console_command compile`/`hotload` (both `Unknown Command`) re-registered the tools - `NeedsBuild` stayed false. The only fix is an editor restart (owner-manual under a no-screen-control contract -> BLOCKED-MANUAL: do all headless work, commit, hand a runbook). Detect: `ToolCount` below the known-good number + `Unknown tool` on a tool that compiled clean. Minimize risk by not editing many `.razor` files against a LIVE editor in one burst. Companion: when the trigger was a **failed** compile (not a hotload exception after Success), see `g-tool-failed-compile-empties-typelibrary-missingcomponent`.
- **A failed in-editor compile of the game package empties TypeLibrary for project types — Play can still "start", but scene components become `MissingComponent` and the game never boots; `compile_status` may later lie Success=true while ToolCount stays at the stock editor-only count.** Observed 2026-07-14: razor `CS1501` (`HashCode.Combine` 9 args in `VehicleHud`) failed `local.` → editor package Broken Reference → session never re-loaded game/editor assemblies. Symptoms: (1) console `Missing Component: couldn't find Component type VehicleProto.GameBootstrap on GameObject:Game`; (2) `find_game_objects` shows `Components: ["MissingComponent"]` even though on-disk `.scene` still has the real `__type`; (3) `get_component_type` / `add_component` for project types fail ("No component type matches"); (4) `search_tools vp_` → 0 hits, `ToolCount` stuck at stock (~52) instead of known-good (~57); (5) no `[vp] boot started` after `play_start`. Headless `dotnet build` going green does NOT repopulate TypeLibrary. **DO NOT save the scene** while MissingComponent is showing — that would bake the strip into the asset; the on-disk file is still good (same law as `g-tool-steam-update-s-box-engine-underneath`). Recovery: fix the compile error on disk → **restart/reopen the editor** (no MCP recompile/reload tool exists) → confirm ToolCount / project tools / `get_component_type` before Play. Related wedge after a *successful* compile + failed hotload swap: `g-tool-structural-razor-edit-hotload-wedge-deregisters-toolset`.
- **The s&box editor PRESERVES static fields across a code hot-reload**, so any static CACHE goes stale when its backing DATA changes out-of-band and is never re-read until a full editor restart. Two bites in one session: (1) a statically-cached kit manifest (`PlaygroundKit._manifest`) kept serving the OLD prop set after the manifest was regenerated -- `SpawnThrowable` logged "unknown prop 'throw_ball'" and every dependent read failed; fix = invalidate the cache each build (`_manifest = null` at the top of the world Build) so a regen is picked up without a restart. (2) a `[ConVar]` bool backed by a static (`*`) whose setter early-outs on `value == _current` -- after a hot-reload leaves the static `true`, re-issuing ` 1` is a silent no-op that never re-arms; arm with a `0` -> `1` edge. Companion to g-tool-editor-onetime-hotreload-orphans-armed-suite.
- **A deterministic autopilot cannot steer a throw/aim that reads the CAMERA yaw.** The character's throw aim + spin read the player camera component's yaw, which is mouse-driven only; the pilot feeds move/jump/grab intents but never the mouse, and the climb camera-assist leaves the yaw wherever the last climb pointed -- so a piloted "throw at a target zone" lands in an uncontrolled direction. Test the yaw-INDEPENDENT invariant instead: throw DISTANCE (horizontal launch speed = base x scale, identical in any direction), not a fixed landing point.
- **A custom s&box McpTool that declares a SINGLE string parameter (e.g. `argsJson` / `opsJson` / `specJson` / `routeJson`) takes its real payload INSIDE that string — passing the logical fields as top-level MCP arguments fails with "Unknown argument '<field>' - this tool takes argsJson (string, optional)".** 's `` is called as `{"argsJson": "{\"op\":\"probe\",\"x\":480,\"y\":480}"}`, NOT `{"op":"probe","x":480,"y":480}`; same shape for ``/``/``. The failure mode is silent-ish in a poller: a wrapped status call returns the error string instead of a dict, so every field reads `None` and the loop looks hung. Check the tool's `inputSchema` (it lists exactly one string property) before scripting it, and wrap.
- **A C# `static` list/registry persists across editor Play STOP→START (scene reload is not an assembly reload) AND across a code hotload that s&box preserves — so a registry populated in a prior play session is still full at the next session's boot, pointing at GameObjects that were destroyed with the old scene.** Bit M5: a `CliffClimbPass.Placed` static (cliff patches + their `ClimbNodeField`s) populated by `pg_terrain 42` stayed at 30 entries after the terrain was cleared and after a fresh Play, so a terrain-gated pilot routine appended itself off the stale list and teleported the character to a destroyed wall → fall-through FAIL cascade (`passed=0`). FIX: gate on LIVE-OBJECT VALIDITY, not static contents — iterate the registry but skip any entry whose `Component.IsValid`/GameObject is dead; if none remain, treat as absent. Clearing the static on the teardown path (here: on `pg_terrain 0`) is good hygiene but is NOT sufficient alone, because the static also survives a fresh session that never called the clear.
- **A no-error (working→working) `.cs` edit does NOT reliably hotload into an already-RUNNING Play session via the MCP alone — `compile_status` reports `Success=true, NeedsBuild=false` (the editor compiled it to disk) while the running game keeps the assembly it loaded at the last real hotload, and Play stop→start does NOT pull the newer on-disk assembly either.** Only a genuine editor-side hotload (which the editor triggers on file-save when it has FOCUS — a blocked-manual step under the no-desktop-control directive) swaps the live assembly (and resets statics). M5: two `dotnet build` 0/0 fixes sat un-live for several Play cycles until a fresh `play_start` finally picked up the newer bin (`PLAYGROUND START routines=13`→`12` flipped only then). Practical loop: don't trust `compile_status` as proof the RUNNING game has your code — verify a code-path effect live (a changed log line / count); if it's stale, a full editor-focus hotload is the owner-manual reset. `dotnet build` remains the authoritative compile check; the editor's live assembly is a separate thing. Related: `play_pause` toggled via MCP could get stuck (`IsPaused` stayed true through repeated toggles) — a hard `play_stop`/`play_start` is the reliable un-stick.
- **The editor MCP `camera_screenshot {includeUi:true}` does NOT composite every `ScreenPanel` in a play-mode scene — it reliably captures the MAIN "ui" ScreenPanel and some later-created ones, but omits others, and right-anchored panels can clip off the right edge.** Verified (26.07.08e, 6+ ScreenPanel GameObjects): the generator "ui" panel and the SessionHud/HotkeyLegend panels appear, but `ui_minimap` and `ui_maplegend` (each their own `ScreenPanel` GO, confirmed `Enabled:true` via `find_game_objects`) never appear in the capture, and the right-anchored HotkeyLegend card clips past the right screen edge (the capture crops a layout laid out for a wider window — the LEFT-anchored generator captures perfectly at the same time). This is a CAPTURE artifact, not a game bug: all panels render in the real editor game viewport. Consequence for before/after UI evidence: you can screenshot the primary panel and the top/late panels, but NOT reliably the minimap/maplegend or right-edge content — fall back to `find_game_objects`/code review for those. No `includeUi`/camera flag fixed it.
- **`camera_screenshot {camera:"<CameraComponent id>", includeUi:true}` can drop the UI compositor ENTIRELY (zero ScreenPanels, not just secondary ones) even though `find_game_objects` shows the same camera as the scene's only non-editor `CameraComponent` and Enabled:true — passing the id explicitly is the difference, not gating state.** Verified (26.07.08e, `ui-segment-fit` session): with a fresh `play_start` and the scene's sole gameplay `Camera` GO's id passed explicitly, three separate `camera_screenshot` calls (immediately, +3s, and 2 minutes later after a hot-recompile) all rendered bare terrain with NO panel at all — not the generator panel, not RightRail, nothing — even though `get_game_object`/`find_game_objects` confirmed the `ui` GO's `ScreenPanel`/`GeneratorPanel` components Enabled:true and `` confirmed `networkingActive:false` (so `.IsAuthoring` should read true). Burned real time chasing `.HudHidden`/session-mode theories before trying the OBVIOUS alternative: **omit `camera` entirely** (schema says empty "uses the scene's main camera") — the very next call, same scene, same camera instance, composited the full styled panel correctly. Root cause unconfirmed (explicit-id path likely bypasses whatever hooks the UI compositor to `Camera.Main` even when the id resolves to that same camera) but the workaround is free: for UI-evidence screenshots, always call `camera_screenshot {includeUi:true}` with `camera` omitted/empty first, and only pass an explicit id if you specifically need a NON-main camera's view (which won't have UI anyway per the sibling gotcha above).
- **Two probe methods report a LIVE editor MCP server as dead — diagnose "unreachable" with the project's python client + the FULL `/mcp` URL before concluding anything.** Verified back-to-back against the same live editor (26.07.08e, port 7297): (1) ` --url http://127.0.0.1:7297` (bare origin, no path) fails "cannot reach … HTTP Error 404: Not Found" — looks like a dead/orphaned port, but the server was alive; `--url http://127.0.0.1:7297/mcp` answered instantly. The client does not append the `/mcp` path to a custom `--url`; pass the full endpoint. (2) PowerShell 5.1 `Invoke-RestMethod -Method Post` to the correct `…:7297/mcp` URL TIMED OUT (3s/8s/20s) while the stdlib-urllib python client succeeded on the identical URL in the same minute — and IRM simultaneously worked fine against a DIFFERENT editor's port (7298/), so a working IRM probe elsewhere proves nothing about the port you care about (mechanism unidentified; probably HTTP.sys queue/header interaction). Operational rule: the only trustworthy liveness+identity probe is `python editor_status --url http://127.0.0.1:<port>/mcp` — a netstat LISTEN (always PID 4/http.sys), the global `McpServerPort` cookie, an IRM timeout, and a bare-origin 404 are all noise.
- **`git worktree add` gives a headless `dotnet build` nothing to build — the `.csproj` files are gitignored (`*.csproj` in `.gitignore`, s&box regenerates them from the editor), so a fresh worktree's `Code/`/`Editor/` folders have no project file at all.** `dotnet build Code\.csproj` fails `MSB1009: Project file does not exist` even though the exact same command works in the main tree. Fix: copy the two `.csproj` files from the main working tree into the worktree before building — they are boilerplate SDK-style projects with `../../../../../../../Program Files (x86)/Steam/steamapps/ common/sbox/...`-relative references, so the copy resolves correctly as long as the worktree sits at the SAME path depth as the main tree (e.g. `dev/<project>//Code/` and `dev/<project>/wt-<slug>/Code/` are both 4 segments under `dev/`, so the relative reference count matches). Never commit the copied `.csproj` (still gitignored; verify with `git status` after copying). **If the worktree sits at a DIFFERENT (deeper) depth** — e.g. a scratch worktree under `.claude/scratch/worktrees/<slug>/Code/` — the relative `../…/Steam/...` refs resolve to the wrong root and the copied csproj fails to find the s&box DLLs; instead author a worktree-local `.csproj` with ABSOLUTE `C:/Program Files (x86)/Steam/steamapps/common/sbox/...` Reference/Analyzer/ ProjectReference paths (depth-independent). Also redirect `<OutputPath>` + `<DocumentationFile>` to an isolated in-worktree folder: the stock csproj writes to the SHARED `sbox/.vs/output/`, which the live editor uses — a verify build must not clobber it. Do NOT point `<BaseIntermediateOutputPath>` at a custom folder (warns MSB3539, and stale generated `*.AssemblyInfo.cs`/`AssemblyAttributes.cs` left under it get globbed into the compile → a wall of `CS0579` duplicate-attribute errors); leave `obj/` at its default (the SDK auto-excludes it from the compile glob) and `rm -rf` any stray intermediate/output dir before rebuilding. **With the isolated custom `<OutputPath>`, a second build run with `--no-incremental` can FAIL `MSB3030` ("Could not copy … deps.json / staticwebassets. endpoints.json … not found")** — the Razor SDK's `--no-incremental` clears the output then the copy-outputs step races the just-cleared isolated dir. It is a stale-state flake, not a code error: the FIRST build (or a plain `dotnet build` with NO `--no-incremental`) succeeds. Fix: `rm -rf` the custom output + `obj/` and run `dotnet build` WITHOUT `--no-incremental`; prefer a clean+plain build over `--no-incremental` for a deeper worktree with a redirected OutputPath.
- **An editor whose configured MCP port is already taken FAILS SILENTLY — no MCP endpoint at all, the only trace is one log line (`[MCP] Couldn't start MCP server on port X ... conflicts with an existing registration`) in sbox-dev.log — AND the shared `config/tools.json` `McpServerPort` value is re-stomped at runtime by whichever running editor last flushes its preferences, so a hand-edit of the config can be silently reverted minutes later.** Seen with 4 editors open ( 7298, 7297, assigned 7269): vp's editor wrote 7297 into the global cookie; every editor restart then inherited 7297, failed the bind against vp's live registration, and came up MCP-dead. Hand-editing tools.json to 7269 was reverted to 7297 within ~5 minutes by a running editor's preference flush. RECOVERY (the race is winnable): kill the target editor → write the wanted port into `config/tools.json` → relaunch the editor IMMEDIATELY (it reads the port within ~10 s of boot and binds for its lifetime; later stomps don't unbind it). Diagnose with `Get-NetTCPConnection -State Listen` over the 72xx range + the sbox-dev.log MCP line — the port table in project docs is a hint, never the truth.
- **An in-editor HOTLOAD migrates a `static readonly` field's VALUE from the old assembly instead of re-running the new initializer -- so a freshly-added entry in a static-init registry dict is INVISIBLE until a full assembly reload, even though the source (and `compile_status`) is green.** added `["crash"] = new CrashManeuver` to `ManeuverRegistry`'s `static readonly Dictionary _byName = new{...}`. The manifest-loader fix landed live in the SAME edit (attributes/methods come from the new assembly), but `ManeuverRegistry.Get("crash")` kept returning null -> `[vp] ... unknown maneuver 'crash'`, because the dict VALUE (built before "crash" existed) was migrated across the hotload and the field initializer never re-ran. `play_stop`/`play_start` does NOT fix it (statics are assembly-scoped, not scene-scoped -- same reason `VehicleBridge` needs an explicit session-reset). A red->green compile toggle also didn't (still a migration from the last-good assembly). What DID fix it in-session: RENAMING the field (`_byName`->`_maneuvers`) so the migration finds no old field to copy -> the new initializer runs fresh. A *fresh editor launch* is unaffected (first load runs the initializer), so committed static-registry code is correct -- this only bites live iteration. Tell-tale: a code path that reads TYPE metadata (a new method, attribute, switch arm) reflects your edit but a path that reads a migrated STATIC FIELD VALUE (dict/list/cache built in a field initializer) does not.
- **An editor with a `.scene` OPEN holds it in memory — editing that `.scene` on disk is invisible to the open tab, and any `save_scene` (MCP or manual Ctrl-S) serializes the STALE in-memory copy, silently CLOBBERING your on-disk edits.** B0: a `.scene` was rewritten on disk (removed the engine template's greybox plane+cubes, added a Bootstrap GameObject); `asset_info` even reported `IsCompiledAndUpToDate=true`, but `scene_tree` over the MCP showed the editor's open scene STILL had the old plane+cubes — the disk rewrite never reached the open tab, and `play_start` ran the in-memory (stale) version. There is no MCP open/reload-scene tool (`g-tool-editor-mcp-no-open-scene-play-active-tab`), so you cannot force the tab to re-read disk. Two safe reconciliations: (a) if the tab is open, mutate the LIVE scene through the MCP scene tools (`delete_game_object` the unwanted objects, `create_game_object` + `add_component` the new ones) then `save_scene` — the save writes an editor-canonical file that matches your intent and there is zero divergence; or (b) leave the disk file as the deliverable, DON'T save the stale tab, and have the owner revert/reopen the tab so disk loads (the on-disk `.scene` is authoritative on next project open). Do NOT hand-edit the `.scene` on disk AND then save from the editor — the editor wins and your edit is lost.
- **Editing WATCHED game source while a scripted play-mode battery is running dirties the in-editor compile mid-run — and on a VRAM-pressured machine the recompile's allocation spike can kill the editor outright.** feel round 2: a one-line log-format edit to `VehicleController.cs` landed while `vp_test.py --all` was mid-battery (13 files, stop/start play between entries); the editor began a hot recompile, the runner's next `play_stop` HTTP call hung to its 60 s timeout, and the editor died (MCP answered connection-refused; owner relaunch required). MEASURED terminal cause in the engine log (`sbox/logs/sbox-dev-*.log`, no shutdown footer): `OUT OF VIDEO MEMORY! ( vkAllocateMemory )` then `ToolsStallMonitor: Stall detected` — VRAM exhaustion with THREE+ concurrent s&box editors open (multi-project agent sessions), the mid-play recompile being the tipping allocation. Two rules: (1) treat a running battery as an exclusive lock on the source tree — queue every edit (even comments) until the runner exits; (2) on a machine running several editors, expect vkAllocateMemory deaths during long play-cycling batteries — check the log TAIL for the OOM line before blaming your code, and close spare editors before long batteries. The battery's completed entries stayed valid — the crash cost the session, not the data.
- **Vendoring a GLOBAL-namespace code slice into another project? Wrap the WHOLE slice in ONE sub-namespace — don't play per-type rename whack-a-mole.** 's generation code lives in the global namespace, where its `Noise` class collides with `Sandbox.Noise` once `global using Sandbox;` is in scope (the M5 copy solved this by renaming `Noise→`, `Tuning→` per-type). Vendoring the same slice into (B1), a cleaner move: prepend `namespace .Wb;` to every copied file (a mechanical byte-safe pass). All cross-file references resolve within the namespace unchanged; the one `using static ;` becomes `using static .Wb.;` (a using directive sits OUTSIDE the file-scoped namespace, so it needs the full path). Result: collision-proof by construction, zero per-type renames, and the vendored slice is quarantined from the consumer's own types. Headless build AND in-editor whitelist both 0/0. ( B1, Code/Wb/)
- **A `[Property]` on a scene component serializes its value INTO the `.scene` — editing the code default does NOT change a value the scene already saved.** Added `[Property] bool UseGeneratedTerrain = true;` to a bootstrap, played, later flipped the code default to `false` — but the running scene kept using `true` (the editor had written the property into the scene the moment it first saw the component). Cost real debugging time chasing a "car falls through the ground" that was actually "wrong world path taken". Fix: set it explicitly via MCP `set_component {id, properties:{Name:value}}` (or clear the scene override), don't rely on the code default once a scene has serialized the field. Related MCP trap: `read_console` returns a buffer that SPANS play sessions — filter by the newest boot line, or you'll read stale lines from a prior run and misdiagnose. ( B1)
- ****In PLAY mode, MCP `camera_screenshot` AND `get_game_object` return a STALE FROZEN CLONE — not**
- **A single-waypoint autopilot route TELEPORTS to the destination instead of walking to it, so a "walk into X" collision/traversal test silently proves nothing.** 's `` (.ConsumeCommands Walk case) teleports the character to `route[0]` (the FIRST waypoint) before starting the route — the intended "possess/spawn at the start" behavior. If your route has ONE waypoint, that waypoint IS `route[0]`, so the character teleports straight onto it and the run "reaches" instantly having moved zero distance. Fix: give TWO waypoints — the START as `route[0]` (teleport target) and the real DESTINATION as `route[1]` (the walked leg). Then the body actually traverses the intervening ground and hits whatever you placed in the path. Confirmed while proving M13b placed-prop collision: `[(0,0),(6,0)]` walked into a prop at (3,0) and was blocked at x≈2.2 with stuck events; the single-waypoint `[(6,0)]` had "reached" the goal on the first poll (a physical impossibility at 2.2 m/s over 6 m) — the tell that it teleported.
- **A physics-regression harness that re-runs a maneuver via back-to-back in-session respawns produces NON-deterministic results; a fresh `play_start` per run is required for byte-identical hashes.** Symptom: the same scripted crash driven 3x in ONE play session (spawn -> drive -> despawn -> respawn -> drive ...) gives DIVERGING dent/telemetry hashes, even though spawn pose is identical — the impact impulse `J = mass*|dv|` varied run-to-run (e.g. 12293 vs 12329 N-s) because the collision resolves at a different fixed-tick sub-phase when the world is never reset. Fix: stop-and-verify + `play_start` before EACH run so the session starts from t=0 with identical tick alignment (this is exactly what vp_test.py's robust_play_start-per-entry loop does). With fresh-play-per-run the same crash hashed byte-identical x3. Corollary: `read_console` spans play sessions, so filter by a per-run marker/newest-boot line, not the raw tail. ( B2 destruction migration / crash battery, 2026-07-13) **QUALIFIED ( D2 live gate, 2026-07-15):** fresh-play-per-run is NECESSARY but not SUFFICIENT for a byte-identical DENT hash on a COMPLEX car driven into the wall. The hatch (few parts, clean single-tick impact) hashed byte-identical x3/x4; the 29-part fitted-collider SEDAN, same fresh-play discipline, gave `5f196200b1fb8a21` x2 then `2832808f7956f975` — the ATTRIBUTION decided identically all 3 runs (`contactM=(0.690,2.199,0.201)`, always `bumper_f`) but the WORLD contact differed at sub-mm (`y=-27.185` vs `-27.170`), and the dent geometry is full-precision, so the hash diverged. Root cause is the DRIVEN bang-bang approach (see the new `g-tool-driven-approach-dent-hash-nondeterministic`): fresh-play fixes tick ALIGNMENT but not the float divergence a multi-tick run-up accumulates on a heavy body.
- **A DRIVEN crash approach (throttle the car into a wall) is not byte-identical tick-for-tick on a complex body, so a full-precision dent-hash gate flakes even with fresh-play-per-run — pin the IMPACT state instead of driving up to it.** D2: identical fresh-play crashes of the 29-part sedan decided attribution 100% deterministically (same part, same body-local contact to 3 decimals) yet produced a different combined dent-hash on 1 of 3 runs, because the world contact point varied by ~0.15 mm — the bang-bang run-up accumulates tiny float differences (and can cross the impact-trigger threshold one tick early/late), and the dent's LOCAL position is stored/hashed at full float precision. The DAMAGE code is deterministic (its offline pure-function gate is byte-identical, and the live attribution decision is stable); the non-determinism lives entirely in the physics run-up. Fixes for a byte-identical dent-hash regression: teleport the car to a fixed contact pose at a fixed velocity and let ONE impact resolve (remove the driven approach), or assert on the stable ATTRIBUTION output (which part / body-local contact rounded) rather than the full-precision geometry hash. Simpler cars (few parts, single-tick impact) happen to stay byte-identical, which hides this until you scale the part count. Cross-ref g-tool-fresh-play-per-run-determinism. ( D2 live gate, 2026-07-15)
- **A two-phase "reverse clear, then measure-drive FORWARD" crash-recovery maneuver drives straight back into the SAME wall unless backup distance > measured-drive distance, and a big backup/turn can FLIP a tall car — the offline dry-run can't catch either (it only validates spec schema).** `crashdrive` (approach→backup→drive) shipped with `backupM=12 < driveDistM=40`: after the frontal crash the car (still facing the wall) backs up 12 m then throttles FORWARD 40 m — re-hitting the wall at 12 m, so `driveDistM` is never covered and `driveTopSpeedMs`/`driftM` stay 0 forever (run ends on `maxRunS`). Correcting to `backupM=35 > driveDistM=15` let the drive phase run, but the sedan then FLIPPED during the long reverse (`[vp] AUDIT flips offenders=2, fallthroughs=1`) → a physics-glitch launch made `driveTopSpeedMs=75 m/s` (271 km/h) — garbage, not a real reading. Fixes: (1) require `backupM` to exceed `driveDistM` plus a wall-clearance margin, and add a wall-proximity cutoff to the drive phase; (2) measure the post-crash pull while driving AWAY from the wall (don't turn a tall wounded car around at speed); (3) add flips/fallThroughs==0 asserts so a glitched run FAILS loudly instead of returning a plausible-looking number. Validate any scripted recovery maneuver LIVE — a schema-valid spec can still be geometrically self-defeating. ( D2 live gate, 2026-07-15)
- **A previously-working Blender --background --python gen.py pipeline can start HANGING INDEFINITELY at full app init (near-zero CPU, no output, Blender --version still instant) — the culprit is Blender USER PREFS/addon state, not the script or the GPU.** Verified on a Windows machine after an s&box editor crash the same day: three consecutive 5-minute hangs with plain --background, then --background --factory-startup -noaudio ran the identical script to completion in seconds (Blender 5.1). --factory-startup skips loading the user preferences and addons, which is where the corrupted state lives; -noaudio removes the audio-device probe as a second known init staller. Fixes: (1) add --factory-startup -noaudio to every headless invocation in pipeline tooling — generator scripts should not depend on user prefs anyway; (2) if prefs are needed, delete/regenerate the Blender user config directory. Diagnostic that separates this from a busted install: --version returns instantly (binary fine) while --background --python stalls (init path).
- **The editor MCP port cannot be pinned per project: it is ONE engine-global preference (engine config/tools.json McpServerPort) and the editor AUTO-INCREMENTS past it when the configured port is already bound by another project editor — so with several editors open, each project ends up on a LAUNCH-ORDER-dependent port.** Verified with four editors live: the config said 7271; the first editor (one project) held 7271 and three others had bumped to neighbouring ports. Consequences: any hardcoded/remembered port goes stale on every restart or launch-order change, and a port that answered yesterday can be a DIFFERENT project today (mutating calls against the wrong editor are the failure mode). Fix: stop pinning and DISCOVER — scan the plausible range (e.g. 7200-7330) with a parallel raw JSON-RPC editor_status probe per port (read-only, safe against any project) and match the Project field; feed the found URL to all tooling via an env var. A ~130-port threaded scan completes in about a second. Implementation caveats: do not reuse an interactive MCP client helper that prints guidance and sys.exits on unreachable ports (SystemExit is not an Exception, and contextlib.redirect_stdout is process-global — NOT thread-safe); write the probe as a self-contained urllib POST. Reference implementation: .
- **The s&box editor compiles/verifies its ProjectRoot working tree — a branch that lives only in a git worktree is INVISIBLE to it, and `compile_status`/test-suite results are silently about the wrong code.** A live-verification run against a worktree-only branch produced green compile + 19/19 suite results that were actually MAIN's state (the branch's new `[ConVar]`s returned `Unknown Command`; its self-test log line never appeared — those absences were the tell). Fix: `git worktree remove` the worktree (confirm no other agent owns it), `git checkout <branch>` in the editor's ProjectRoot, then re-verify. Hot reload DOES pick up the checkout — new `[ConVar]`s registered without an editor restart (verified: the convar answered with its value/default ~1 min after checkout, compile 0/0). Evidence discipline: a suite pass without the branch-specific log marker is evidence of NOTHING — assert a marker unique to the branch code before trusting any result.
- **In an Editor-assembly type (an `[McpTool]`, `[Menu]`, etc.), a bare `FileSystem` is a CS0104 ambiguity between `Editor.FileSystem` and `Sandbox.FileSystem` — and headless `dotnet build` of the GAME csproj never sees it, because the editor assembly is compiled only in-editor.** A tool that read `FileSystem.Mounted.ReadAllText(path)` built green headlessly (0/0) but the in-editor `compile_status` reported 2× CS0104 on the `local.<project>.editor` compiler. Fix: fully-qualify `Sandbox.FileSystem.Mounted` (the mounted game content) — `Editor.FileSystem` is the editor's own FS and is almost never what you want. Broader rule this reinforces: for ANY code in the Editor assembly, the authority is the in-editor `compile_status`, not `dotnet build proj` (which compiles only the game assembly). Poll `compile_status` after the edit and inspect the per-compiler `Success/Errors` for the `.editor` compiler specifically.
- **Polling a POST-and-poll editor↔play-mode bridge for a run's result: the FIRST status poll after you post a new command can return the PREVIOUS run's terminal telemetry, because the play-mode consumer only flips `status` back to `running` on its NEXT fixed tick.** A loop that breaks on the first `status=="done"` then reads STALE metrics from the prior run — the symptom is every OTHER row of an A/B sweep silently duplicating the row before it. Two clean fixes: (a) snapshot the report run-counter before posting and wait for it to ADVANCE before accepting `done`; or (b) require you observe `status=="running"` at least once before honouring a `done`. Related trap in the same bridge: the maneuver-ack's `runId` is often the COMMAND token (`++_cmdToken`), a DIFFERENT counter from the telemetry report's run counter (bumped in the per-run reset) — matching one against the other never succeeds; snapshot the REPORT counter, not the ack's. The battery runner sidesteps all this by doing a full play_start per entry (fresh reset); a single-play-session A/B harness must handle it.
- **A hand-tune applied to a GENERATED asset's output file (a manifest JSON, an OBJ, a baked config) is silently reverted the next time the generator runs — regen is a clobber, not a merge.** Concrete instance: a live feel-tuning session moved a kit manifest's `driver_seat_author_m` sit point in the generated manifest.json; the Blender generator still carried the old tuple, and a routine 4-kit regen weeks of context later quietly reverted the tuned pose with zero warnings — caught only because the coordinator diffed the regen output against git HEAD before committing. The law: the generator is the SINGLE source of truth — any tune made against a generated file must be back-ported into the generator in the SAME work step, then proven by re-running the generator and byte-diffing its output against the tuned file (identical = parity; any diff = a value still living only downstream). Corollary: always run `git diff` on a regen before committing it — an unexpected changed line in a “should-be-idempotent” regen is exactly this trap firing.
- **A GREEN in-editor compile can still leave ONE MCP tool running phantom stale IL — fix is a SUBSTANTIVE body edit, not a recompile.** After an INTERMEDIATE broken compile (a multi-edit change that momentarily called a helper before its definition landed in a later edit — here `IsFinite(xM)` used before the `static bool IsFinite` was added), s&box can wedge a single tool's dynamic method. `compile_status` then reads success=True / 0 errors / needsBuild=False and MOST tools run fresh current code (/ reflected the new behaviour), yet the wedged tool's DMD executes OLD IL: threw `InvalidOperationException: An element of type 'String' cannot be converted to a 'System.Boolean'` at a `JsonValue.GetValue<bool>` that exists NOWHERE in the current source (running DMD != the file). Whitespace edits + forced recompiles did NOT clear it (the DMD was never rebuilt). A substantive edit to that method's body (changed `(string)(o["op"] ?? "")` to `GetString(o,"op","")`) forced a fresh DMD and it worked; an editor restart also clears it. Distinct from the success=False stale-assembly wedge (`g-tool-...`) — this one has a fully GREEN compile_status. Prevent the trigger: keep a helper's call and its definition in the SAME edit so no intermediate compile is ever broken.
- **The editor's MCP server does NOT reliably auto-bump off a taken port — it can fail the bind and simply NOT START, leaving a healthy editor with no MCP at all.** A freshly relaunched editor logged `[MCP] Couldn't start MCP server on port 7273 (Failed to listen on prefix 'http://localhost:7273/' because it conflicts with an existing registration on the machine.)` — 7273 was held by a running editor — and then gave up: no listener on ANY port (verified via `Get-NetTCPConnection` over the whole 7200–7330 scan range). This REFINES `g-tool-mcp-port-auto-bumps-scan-dont-pin`: auto-bump has been observed on some boots, but is not guaranteed — when `mcp_discover` finds nothing for a project whose editor is provably running, grep the engine log (`logs/sbox-dev.log`) for `[MCP]` before assuming a stolen/hung editor. AGENT-EXECUTABLE FIX (no owner needed, no screen control): kill the MCP-less editor first (it may flush prefs on exit), surgically edit the engine-global `<engine>/config/tools.json` `McpServerPort.Value` to a free port (byte-safe python edit, never PowerShell 5.1 text ops), relaunch the editor via CLI, re-discover. The MCP-server preference dialog is just a UI over this file. FLUSH-ON-EXIT TRAP (same day, second occurrence): a RUNNING editor holds prefs in memory and REWRITES tools.json on exit — an instance that booted before the edit silently REVERTED McpServerPort 7274->7273 when it died. Order matters: kill the target editor FIRST, wait for full exit, THEN edit the pref, verify the on-disk value, relaunch immediately; re-check the value after any other editor instance exits. RUNNING-REVERT (stronger than exit-only, observed 2026-07-14 second time): a sibling editor merely SITTING IDLE — not exiting — also periodically autosaves prefs and reverted a fresh 7274 edit back to its own in-memory 7271 within ~3 min, so a NEXT editor launched minutes later booted on the stale port and bind-failed. Because each MCP server keeps whatever port it BOUND AT BOOT (a later pref revert can't move an already-listening server), the reliable fix when you CAN'T kill the reverting sibling (e.g. an owner's watch window the kill-guard blocks) is to set the pref and RELAUNCH TIGHTLY as one step — kill your own portless editor, wait for exit, set the port, Start-Process, no gap — so the new editor reads the free port at boot before the sibling's next autosave. Winning the race once is enough (verify with the `[MCP] MCP server listening on http://127.0.0.1:<port>/mcp` log line).
- **An editor that stops answering MCP may be HUNG, not crashed — and a hung editor is agent-recoverable without screen control via kill + CLI relaunch.** Forensics that distinguish the states: `Get-Process sbox-dev` → the window title identifies WHICH project (`<scene> - s&box editor`); `Responding=False` + climbing CPU = wedged-but-working (a mass vmdl-compile backlog: ~4 back-to-back full-world export bakes ≈ 18k+ RegisterFile'd assets plus a 900-chunk regenerate produced a wedge that never cleared; one SMALLER backlog self-recovered in ~120 s — give it a bounded wait before acting). Port→PID mapping is useless here: the MCP server rides HTTP.sys, so every listener shows as PID 4 `System`. RECOVERY: capture the exact relaunch command with `(Get-CimInstance Win32_Process -Filter "ProcessId = <pid>").CommandLine` (shape: `sbox-dev.exe sbox-launcher.dll sbox-dev.dll -project <path>.sbproj`), verify the window title matches the target project, `Stop-Process -Force` ONLY that PID (never a sibling project's editor), relaunch via `Start-Process` with the captured args, and poll `mcp_discover` (boot + project compile ≈ 1–3 min). All committed work survives; only transient in-session scene state is lost. Check `McpServerPort` after relaunch — see g-tool-mcp-bind-fail-no-auto-bump-edit-pref-restart for the bind-fail follow-on trap.
- **A same-pose live-vs-export pixel-diff that hides the live world and spawns the exported block measures the ENVIRONMENT, not the export: the EnvmapProbe's ambient contribution is scene-state-dependent and flaps between captures — disable the skybox+probe GO for BOTH captures or the diff is garbage.** export-v2 §6.2 calibration: a faithful LowPoly export block diffed at interior mean 33/255 vs live with a UNIFORM ~1.30–1.36× per-channel brightness ratio on matched land patches — no UV/material/geometry fault at all. Two live-vs-live captures of the UNCHANGED world minutes apart differed by mean 28.5/255, proving the environment (not the bake) owned the whole diff; on this build the probe swing is ~4× the ~7% the older probe-flip gotcha measured, and the burst-guard (brightest-frame-with-a-twin) does NOT save you because an entire burst can land in one probe state while the other side's burst lands in the other. Fix that makes the gate exact: `set_game_object {id:<2D Skybox go>, enabled:false}` (kills SkyBox2D + EnvmapProbe together) before the LIVE capture, keep it off through the export-block capture, re-enable after. Result on the identical shots: live-vs-live mean 0.0000/255 (max 6), live-vs-export interior mean 0.102/255 p99=1.0 — the M11b ≈0 bar, recovered. Sun + locked Tonemapping exposure ( Min==Max) are stable and stay on; only the probe/sky pair is the noise source. Also check the clock isn't advancing ( paused/anchored) before blaming the bake.
- **A heavy export bake does not end when the MCP call returns: the editor then IMPORTS the fresh asset tree OUT-OF-BAND and goes unresponsive for MINUTES — stacking a second heavy MCP call into that grind is what WEDGES the editor, not the bake itself.** export-v2 calibration: a full-world Smooth bake writes ~2700 loadable assets (obj/mtl/vmdl/vmat/png/ prefab per land+water chunk); after returns its result JSON the editor asset-imports that tree and the MCP server answers slowly or not at all for ~15-20 min, then RECOVERS on its own. It survives ONE bake's import queue, but under 4 stacked bakes + a regenerate with no gap it wedged PERMANENTLY (needed the kill/relaunch recovery — see g-tool-hung-editor-kill-relaunch). Fix baked into the export runner (tools/.py): (a) give the MCP call a long ceiling (1800s) so the blocking bake write never times the client out; (b) after EVERY export — including the reexport-identical SECOND bake — DRAIN- WAIT: poll editor_status on a short (15s) timeout and only proceed once it answers promptly ~3 CONSECUTIVE times, so the next heavy step never lands on a digesting editor. Space all heavy ops; never poll-spam mid-bake (an ACTIVE, growing logs/sbox-dev.log means it IS working — wait, don't restart). Only treat it as hung if MCP is dead >25 min AND the log has stopped growing. ROOT CAUSE (nailed after a terminal wedge — 80+ min unresponsive, 10.2 GB working set, killed): every same-slot re-export DELETES + REWRITES the whole bake folder, which invalidates every compiled asset in it — the asset system then queues an on-demand recompile of the entire tree ("At least one out-of-date dependency: … (file stopped existing)" per asset in sbox-dev.log), so each re-export wave costs a full-tree recompile and STACKED waves in one warm session are UNBOUNDED churn; drain-wait spaces the waves but cannot shrink them. The durable fix is STRUCTURAL: standing regression suites bake SMALL worlds ( ruling 2026-07-14 — 192 cells / 36 chunks; bake + same-slot re-export + drains ≈ 25-40 s end-to-end, drains answer in 10-17 s) and full-scale bakes are ONE-TIME proofs recorded in the spec descriptions, never re-run as a standing gate.
- **The FIRST world-save of a freshly launched editor session can fail with 'write failed: Object reference not set to an instance of an object' — and then every later save (same slot, same code path) succeeds, so a full regression suite run on a FRESH editor can fail its save/load sequence while the identical sequence passes on re-run.** --all on a just-relaunched editor: save_load_roundtrip's (legacy slot, 144 planted props) returned the NRE from inside the Serialize/WriteAllText try; the sequence's loads then read a STALE world_slot1.json from a prior session (grid-hash mismatch + props=0 cascade, 5 failures). Minutes later in the SAME session, four other save sequences wrote both legacy and named slots clean, and a solo re-run of save_load_roundtrip passed 0-failures including the 144-prop round trip. Suspect class: a lazily initialized editor-side dependency (FileSystem.Data mount or serializer warm-up) not ready on first touch — unproven; needs a dedicated repro (relaunch editor -> save FIRST). Until root-caused: treat a save NRE in the FIRST save-bearing sequence of a fresh session as suspect, re-run that sequence solo before believing it, and consider a throwaway warm-up save before gate runs on fresh sessions.
- **A click-/hover-gated UI state (a dropdown, menu, or disclosure whose open flag is a PRIVATE field toggled only by an `onclick`/`onmouseover` on a runtime `ScreenPanel`) has NO editor-MCP drive path — you cannot open it to screenshot its contents.** The MCP surface has no UI-input tool (no click/type into a running ScreenPanel), no game `[ConCmd]` unless the project happens to expose one, and `set_component` can only reach `[Property]` fields — a plain `PanelComponent` shows `Properties: null` via `get_game_object`, so a private `bool _saveOpen` is unreachable. Verified (26.07.08e, GeneratorPanel's R2.4 Save ▾ dropdown, save-slot verify 2026-07-14): seeding the backing slots via `` populated the on-disk list, but the dropdown itself stayed closed and un-openable via MCP. Two capture facts that go with this: (1) `editor_camera_screenshot` (the free viewport camera) NEVER composites screen-space `ScreenPanel` UI in EITHER edit or play mode — it renders only the 3D scene; use `camera_screenshot {includeUi:true}` in **play-mode authoring** to capture the main "ui" ScreenPanel (see g-tool-mcp-camera-screenshot-misses-secondary-screenpanels for its own gaps). (2) When the app is UNDER the glyph-corruption threshold, that `camera_screenshot` renders the panel's base text CRISPLY, so it CAN verify layout/color and closed-state text — but the dropdown-OPEN text (the highest-risk, last-rendered run) still can't be reached. Consequence for a UI-verification agent: you can prove the razor compiles (`compile_status` + ToolCount stable), the panel renders + base text is legible, and the BACKEND the UI wraps works (``/`` round-trip the exact `WorldSave` path), but opening the dropdown to eyeball the slot rows / `●` marker / armed-delete / save-as flow is an OWNER real-window step — script the backend + closed-panel render, and route the click-gated interaction + its glyph eyeball to the owner. Don't burn cycles hunting an MCP click tool; there isn't one.
- **A NEW panel-button `onclick` effect (e.g. a toast) is still screenshot-verifiable without a click primitive or screen control — call the SAME game-assembly static method from any MCP-reachable editor tool that already shares the process, since the editor assembly references the game one.** Unlike a click-gated dropdown (g-tool-mcp-cant-open-click-gated-ui-state, which really has no bypass), a button handler that just calls a public static (here `GeneratorPanel.ExportWorld` → `TipsHud.ToastTop(...)`) can be exercised by temporarily adding ONE line to an already-callable editor tool (`.Export`, the `` MCP tool) that invokes the identical static call with the identical formatted string, screenshotting, then reverting the line before commit (confirm `git diff` is empty on that file). This proved the real `.toast-top` CSS/BuildHash/12s-timeout path (26.07.08e, export-toast top-center verify, 2026-07-14) — the button click itself was never simulated, but the rendered artifact is the same component. **Caveat — don't trust a null result:** the first two attempts silently ran the STALE pre-edit assembly (compile_status already read `Success:true`, but the temp `ToastTop`/`ToastNow` calls hadn't hot-loaded into the running Play session yet — see g-tool-editor-no-hotload-into-running-play-without-focus), so "no toast appeared" looked identical to a real gating bug. Fix: add a one-off `Log.Info` next to the temp call and confirm it via `read_console {filter:"..."}` (also note `read_console`'s real params are `limit`/`minimumLevel`/`filter`, not `lines`) BEFORE concluding the toast logic itself is broken — only trust a negative screenshot once the log line proves the new code actually ran.
- **A gitignored `Assets/exports/` bake tree whose SOURCE `.obj` were deleted but whose compiled `.vmdl` remain is a PERMANENT on-demand recompile loop that a FRESH editor re-enters and never drains — and its real damage is that it STARVES play-mode init, so it reads as "really low FPS in play" when the editor is actually just grinding.** Distinct from the in-session export churn (`g-tool-…export…recompile`, which drains in ~15-20 min): here the source is *permanently* gone (prior same-slot export-v2 re-export deleted the `.obj`), so the asset system logs `On-demand recompile of asset exports/…vmdl -> reason: … dependency: …obj (file stopped existing)` forever over a bounded ~20-asset working set (sampled: the last asset repeated every 6 s across 30 s; 1624 stale vmdls, 6.4 GB working set, console 100 % saturated so NO game/`[wb]` line is observable even in a 2000-line buffer). SYMPTOM MASQUERADE (the trap): with the storm live, `play_start` returns but `` shows `gameLive=false`/`frames=0` and the census falls back to the edit scene — the play world never builds — across 45 s. Looks like a play-mode perf/day-night problem; it is a stalled editor. RECOVERY (the live editor holds file handles, so `mv`/`rm` of `Assets/exports` fails `Permission denied`, and the auto-mode classifier blocks `rm -rf` of an unnamed repo dir): (1) `Stop-Process -Force` ONLY the editor PID (verify by window title `<scene> - s&box editor`; never a sibling project's), (2) `Move-Item` the gitignored `Assets/exports/` OUT of the tree (regenerable via ``; handles freed once the process is dead), (3) relaunch via the captured CLI — mind the `McpServerPort` bind-fail follow-on (`g-tool-mcp-bind-fail-no-auto-bump-edit-pref-restart`: the global pref may hold a sibling's port). After: play inits normally and holds a locked 60.
- **A `[vp] AUDIT <name> offenders=N target 0` line does NOT count cars or wheels — the word "offenders" is a shared template, and for the tick-counted invariants (`stuck`, `flippedTicks`) N is a fixed-TICK count, so a small nonzero like `stuck offenders=4` is a ~0.08 s transient, not four stuck cars/wheels.** `InvariantAuditAccumulator.Accumulate` increments `StuckTicks` once per fixed tick (50 Hz → 0.02 s/tick) whenever `Throttle>0.3 && SpeedMs<0.3 && grounded wheels>=3` on the single pilot `ActiveCar` — the `>=3` wheel test is a GATE, not the counted quantity. It is reset per run (`ResetRun`→`ClearRunMetrics`) and accumulates ONLY while a scripted maneuver runs (`_running`), never during genuine free-drive idle, and never for parked/undriven roster cars (they aren't ActiveCar and throttle≤0.3 anyway). So `offenders=4` = 4 ticks of flooring-it from a standstill (the launch transient) — BELOW the documented-normal spawn settle-freeze baseline of 6 (docs/part-kit-assembly.md, pickup-kit.md), and ~290x below a genuinely wedged car (measured `stuckTicks≈1160` for a coupe stuck in a slalom, docs/baseline-metrics.md). Reading rule: for tick-counted invariants treat N as `ticks × tickDt` of DURATION; only `flips`/`fallThroughs`/ `nanForces`/`sleepWhileDriving` are event counts. A real stuck detection reads in the hundreds+; single digits are launch/standstill-throttle transients, not a stuck car.
- **`[ConVar]` values PERSIST in the editor across sessions and code changes — a stale dev-override value silently defeats the new code default it was left pointing at.** 's `vp_engine_sound` was a v1 a/b/c pick dial; two audio reworks later it was repurposed as an optional override (`""` = per-class loops). The editor had persisted the old session's `"a"`, so every car kept playing the v1 loop through TWO owner listen tests — the code was right, the build green, the in-editor compile green, and the feature inaudible. Console proved it: the OnStart log printed the resolved event (`event=engine_a_mid_four`) while per-class pitch math was clearly the new code. Rules: (1) when repurposing a dev ConVar so that empty/default means "new behavior", assume machines that ever set it still carry the old value — clear it live (`<convar> ""`) or RENAME the ConVar (persistence is keyed by name); (2) make override state loud — log a WARNING at component start whenever a non-default override is in effect, or the next silent mask costs another test cycle; (3) when a listen/look test reports "nothing changed", grep the console for the resolved-asset log line BEFORE touching the assets — resolution beats regeneration.
- **The s&box console buffer holds ~2000 entries; any unconditional per-tick/per-instance telemetry silently destroys the playtest history an owner-plays -> agent-reads-telemetry loop depends on.** Two measured offenders in : a 2 Hz camera status line logged even with the player standing IDLE (byte-identical repeats -- ~3 min of idling flushed the whole buffer), and per-rope boot lines (2 per vine x ~330 tree vines = ~660 lines) flushed history on EVERY world rebuild. Fix pattern: (1) per-frame status streams log on CLASSIFICATION CHANGE only (mode/clamp/state signature), rate-capped, plus a slow heartbeat (~5 s) so a quiet log still proves liveness -- keep the line FORMAT unchanged so existing greps survive; (2) per-instance boot detail goes behind a debug convar (default off) while the per-pass COUNT summary line stays unconditional; (3) high-frequency failure loops (a reject/warn that retries every tick) get a TimeSince rate limiter at the log site from day one. Budget rule: telemetry someone must READ hours later competes for the same 2000 slots as the spam -- design log volume like a resource.
- ****A deterministic-asset regen "fails" its byte-identical check on Windows when the repo has**
- **The editor MCP `read_console` has a bounded buffer/line cap (~500 lines returned), so a verbose probe dump buries the lines you actually need — grepping the returned window finds NOTHING even though the lines were logged.** Live case: `` SNAP with a full aerial net up (2267 elems) emits hundreds of ROPE/THROWABLE rows per snapshot; the 2-line character summary (owner/proxy roles) scrolled past the cap instantly, and `read_console {"limit":2000}` still returned only ~500 lines. Don't raise `limit` and scroll — pass a server-side filter to pull the signal lines directly: `read_console {"limit":500,"filter":"<distinctive prefix>"}` returns exactly the matching rows (the `filter` arg matches substrings; `contains`/`search` are NOT accepted arg names). Same pattern works for any high-volume verifier: filter on the distinctive prefix of the line you need.
- **In an editor (`.editor.csproj`) assembly, an unqualified `FileSystem` is a CS0104 ambiguous reference between `Editor.FileSystem` and `Sandbox.FileSystem`** — so an `[McpTool]` that reaches the game's data filesystem (e.g. `FileSystem.Data.FileExists( SaveSystem.FileName )` to check for a save before starting a session) compiles fine in the game `Code` assembly yet fails to build in `Editor`. The game assembly only sees `Sandbox.FileSystem` (global `using Sandbox`), but the editor assembly ALSO globally imports the `Editor` namespace, which ships its own `FileSystem` type — same clash class as `Editor.Project` vs `Sandbox.Project`. Fix: fully-qualify `Sandbox.FileSystem.Data.…` in editor code. Caught headlessly by `dotnet build Editor/<proj>.editor.csproj` (the in-editor whitelist compiler would reject it too), so it's a compile-time trap, not a runtime one.
- **The s&box editor can get stuck running a STALE game assembly with `compile_status` fully green (Success=true, 0 errors, `NeedsBuild=false`) — and NONE of the standard recoveries clear it: a `touch`/mtime-bump, a dirty→clean syntax-error cycle, a fresh `play_stop`+`play_start`, and an in-play console-triggered world rebuild all keep executing the OLD code.** Isolate it from a merely-queued compile: add a unique `Log.Info("MARKER…")` at the top of a hot method, prove the editor DID compile it (inject a syntax error → `compile_status` flips Success=false with diagnostics within one poll, then remove it → green), boot, and grep the console — the MARKER never prints while the method's OTHER (old) log lines do, and a changed static default / new log wording stays at its pre-edit value. This differs from `g-tool-editor-silent-hotload-race` (there poll-green + touch fixes it; here that fully fails) and from the m7 hollow-wedge (there PlaygroundBootstrap/convars vanish; here the play session is HEALTHY — convars work, world builds, audits pass — it is just OLD code). Strong correlate: MULTIPLE `sbox-dev` instances of the SAME project open at once (`Get-Process sbox*` showed 3), so hotswap / obj-lock contention appears to freeze the live domain's code-load while each instance's own compiler still reports green. Recovery is an editor RESTART (blocked-manual owner step — agents have no MCP editor-restart and must not screen-control); close the duplicate instances first. Gate for any MCP verification work: a green compile is NOT proof the runtime runs your code — confirm with a unique runtime marker before trusting an in-editor behavior result.
- **A static registry populated by a static CONSTRUCTOR (or static field initializer) does NOT re-pick-up newly-added entries on hotload: s&box carries the OLD static state forward and never re-runs the static ctor, so a registry item added in source is ABSENT at runtime even though the source is correct AND `compile_status` is green.** Distinct from the whole-assembly-stuck-stale case (`g-tool-hotswap-stuck-green-compile-restart-only`): here METHOD code hotloads and runs fine — only the STATIC DATA is frozen at its first-load contents. Killer diagnostic: an entry added EARLIER (already in the last cold-load's ctor) is present while the NEWLY-added adjacent entry is missing — only a pre-addition static ctor produces that split. Verified (26.07.08e, build-hammer live-verify 2026-07-15): a `hammer` `ItemDef` added at `Items.cs` line 25 (`static Items` ctor, `All["hammer"]=…`) was ABSENT from the running `Items.All` (` hammer` → `{"error":"unknown item 'hammer'"}`) while the adjacent line-26 `watering_can` resolved fine — despite green compile and even a fresh editor session that DID pick up a new `[McpTool]`. Trap shape to watch: **registry-VALIDATED** ops fail (anything gated on `Registry.Get(id) != null`) while **raw-string** uses of the same id keep working — `Game.StartNew`'s `Inventory.Add("hammer")` seeded the hotbar, ` hammer` and the `SelectedId=="hammer"` build-mode gate all worked, but the hotbar icon/name (`Items.Get`) and `` (validates via `Items.Get`) were dead. So a feature can be fully playable while its registry-metadata lookups render blank / its validation verbs error. Fix: a full editor PROCESS restart (quit + relaunch) so the assembly cold-loads and the static ctor re-runs against current source — an mtime-bump/hotload/`play_stop`+`play_start` will NOT clear it (statics survive play restarts). Verification gate: after adding a registry entry, prove it live via a registry-validated path (a `Get`-backed harness verb) before trusting it; green compile + working gameplay is not proof the registry data is current.
- **`editor_status.ActiveScene` / `ActiveScenePath` can read `None` even when a scene IS loaded and even DURING play - do not gate scene-identity on them.** Verified (26.07.08e, 2026-07-15): with `homestead` loaded in edit mode `ActiveScene` was `None`; after `play_start` on the `worldgen_spike` scene `ActiveScene` was STILL `None` while the engine reported `IsPlaying=True`. The reliable scene-identity signals in the editor MCP are instead: the `play_start` RETURN payload's `Scene` field (carries the booted scene name, e.g. `{"IsPlaying":true,"Scene":"worldgen_spike"}`), and `list_scenes[].IsActive` (the entry flagged active is the loaded scene, both in edit and play mode). Use those; treat `editor_status.ActiveScene` as unreliable.
- **`camera_screenshot` with explicit `width`/`height` override renders a BLANK single-colour (clear/sky-colour) frame - the scene and UI are absent - while the SAME call with NO size args captures correctly at the native game-view size.** Verified (26.07.08e, 2026-07-15): `{"includeUi":true,"width":1920,"height":1080}` and even `{"includeUi":false,"width":1920,"height":1080}` both returned a uniform frame (1 distinct colour); repeated shots stayed blank (not a first-frame settle), and issuing the override also appeared to leave the play render-target wedged so the next default capture blanked until play was restarted. Default-size `camera_screenshot` and `editor_camera_screenshot` (no size args) work fine. Consequence + workaround: you cannot up-res a capture via the size args; to frame more of the world use `set_editor_camera` (position + angles) + `editor_camera_screenshot` at default size and reposition, rather than requesting a larger resolution. Related: a HUD authored at a fixed virtual render scale (e.g. 1920x1080) overflows a smaller game-view window - centre/right/bottom-anchored panels fall outside a 1280x720 default capture (top-left elements stay visible), so a full-HUD screenshot needs the editor game-view window itself sized up (an owner/manual step), not a width/height arg.
- **On a Windows OpenSSH server, an admin user's public key placed in ` may be SILENTLY IGNORED -- Windows' default `sshd_config` ships a `Match Group administrators` block that redirects admin-group keys to ` and sshd reads that file ONLY if its ACL is SYSTEM + Administrators with inheritance stripped.** Before installing a key for an admin account, check which regime the box is in: `Select-String 'administrators_authorized_keys' If that Match block is UNcommented (the default), write the key to the ProgramData file and lock it down: `icacls <file> /inheritance:r /grant Administrators:F SYSTEM:F`. If it is commented out (seen on some provisioned boxes), the normal user path `~/.ssh/authorized_keys` works (still set the strict ACL). Do NOT edit `sshd_config` to "fix" it -- that requires an `sshd` service restart, which is off-limits on a shared production box. Verified 2026-07-15 on a Windows Server 2022 box whose Match block was commented, so the user path worked.
- **A long-running process (e.g. a dedicated game server) started over a Windows OpenSSH session is KILLED the moment the ssh session closes -- `ssh box "start-server.bat"` looks like it worked, but the server dies as soon as the command returns.** sshd tears down the session's child processes on disconnect, and `Start-Process`/`-WindowStyle Hidden` does not escape it. Verified 2026-07-15 (Windows Server 2022, sbox-server.exe): the process was alive when probed from INSIDE the launching session (8 s in), and gone (`Get-Process` count 0) from a new session immediately after the first closed -- which also silently invalidated an earlier "guard didn't refuse" test result (the process the guard was supposed to detect was already dead, not missed). Two consequences: (1) the launch path for a live server on a remote Windows box is a SCHEDULED TASK (or service) -- task-launched processes are not tied to any ssh session; a flag-file-armed watchdog task that relaunches the server if it exits covers both boot and crash-recovery. (2) When TESTING anything against a "running" server over ssh, keep the launch, the test, and the assertion in ONE ssh session, or the server will be dead before the test runs.
- **To light a day/night play scene for screenshots when the clock field (`TimeOfDay`) is not an MCP-writable `[Property]`, race the writable day-length knob.** a project's `Game` exposes `DayLengthMinutes` (writable) but not `TimeOfDay`; set `DayLengthMinutes` small (e.g. 0.4 -> ~1.2 game-hours/sec) via `set_component`, poll `.clock` until midday, then set it large (e.g. 600) to nearly freeze while you capture. Watch the night-collapse path (sleeps you at 2 AM -> pauses at the wake hour); once paused, the iso follow-cam still runs `OnUpdate` and overrides any transform you set, so reframe by moving its TARGET (the player) instead -- and use a correct ground z (` height_at`) or the camera focus sinks under the terrain.
- **In PLAY mode, MCP `get_game_object`/`set_component` resolve an AUTHORED scene object to its EDIT-scene copy (stale/frozen at play_start), while a RUNTIME-spawned object reads LIVE — the split is by GUID, not "everything is frozen".** Refines the "play-mode reads are a frozen clone" note: it is not uniform. An authored object keeps its scene-file GUID in the edit→play clone, so that GUID exists in BOTH scenes and the MCP resolves it to the edit copy (which never runs the game's `OnUpdate`). A runtime-spawned object has a random GUID that exists ONLY in the play scene, so it resolves live. Evidence (freecam): after ` (1500,1500)`, the runtime **Player** (random GUID) read `WorldPosition=1500,1500` LIVE, but the authored **Camera** (deterministic bootstrap GUID `...106`) read its play_start pose and never moved, even though the live iso cam was really following the player. Consequence: you cannot or observe an authored component's live play-mode state (a toggle flag, a per-frame transform) via `set_component`/`get_game_object`. Fix: add a `[ConCmd("proj_thing")]` static that touches the live singleton (`Instance`) and `Log.Info`s the state, invoke it with the `console_command` MCP tool, and read the LOG — same console-parity pattern as vp_setworld /. A ConCmd registers on ordinary code hotload (unlike a new `[McpTool]`/ss_ tool, which needs an editor restart), so it is usable the same session.
- **In a SHARED multi-agent working tree, `dotnet build` as a personal verification gate fails on OTHER agents' uncommitted/untracked WIP that doesn't compile — their broken files break your build, even though your own changes are fine.** Confirmed independently by two agents on the same day (, 2026-07-15). Working recovery both converged on: `git worktree add --detach <path> HEAD` (or a clean `main`), copy in ONLY your own changed files, build there in isolation. Two s&box-specific traps in that recovery: (1) the worktree MUST sit at the SAME directory depth as the original repo clone — the editor-generated `.csproj` references the Steam s&box install via long relative paths (`../../../../../../…`) that only resolve at that depth, so a worktree created shallower or deeper breaks the reference; (2) that `.csproj` is gitignored (editor-generated, not tracked), so `git worktree add` does NOT bring it along — copy it in by hand too. Alternative to depth-matching: rewrite the copied `.csproj`'s relative paths to absolute paths instead (one agent did this and it also worked).
- **A crash-recovery test maneuver can TELEPORT a damaged car to a clean measure lane and the damage survives — because the damage state lives on components, not on world position.** D2 crashdrive replaced a reverse-then-drive recovery (which re-hit the wall and flipped the tall car, `g-tool-recovery-maneuver-reverse-then-drive-rehits-wall`) with: crash → settle → set `WorldPosition`/ `WorldRotation` to a FIXED clean-lane pose + zero `Rigidbody.Velocity`/`AngularVelocity` → settle a fixed window → hands-straight measure driving AWAY from the wall. Setting a Component's `WorldPosition`/`WorldRotation` DOES teleport the rigidbody in s&box; zeroing both velocities first stops the crash momentum carrying over. Verified live: post-teleport the crushed part still reads `dents=1 frac=1.000` and the derived steering pull applies during the measure. Fixed pose + fixed settle window = deterministic. This is the clean way to isolate a post-crash driving measurement from the messy crash kinematics (no re-hit, no flip).
- **To close a byte-identical dent-hash ×N gate that a DRIVEN run-up flakes (`g-tool-driven-approach-dent-hash-nondeterministic`), inject a SYNTHETIC impact with a FIXED BODY-LOCAL contact through the real damage path — the resulting dent is pose-invariant, so the hash is byte-identical regardless of where/settle the car is.** D2 `crashpin`: a harness hook builds the world contact as `bodyTx.PointToWorld(fixedContactBodyLocal)` and calls the same `ProcessImpact` a real collision uses (attribution + dent kernel + coupling). Because the dent is stored in PART-local space and the part rides the body rigidly, `partTx.PointToLocal(pointWorld)` depends only on the fixed body-local contact and the fixed body↔part relative transform — NOT on the car's world pose. Result: combined dent-hash byte-identical ×3 across fresh `play_start`s (vs the driven approach which drifted ~0.15 mm and flipped 1 of 3). It exercises the real damage CODE (only the physics collision detection — the actual nondeterminism source — is bypassed), so it is a legitimate determinism gate, not a stub. Pin the car pose too as belt-and-suspenders, but the pose-invariance is what actually guarantees the byte-identity.
- **A test maneuver that calls a just-spawned component's hook on its FIRST tick silently no-ops — the component's `OnStart`/bind has not run yet.** D2 crashdrive synthetic mode injected an impact on the first `PrepSynthetic` tick; `ImpactRouter.BindParts` (in `OnStart`) had not populated its part list, so `InjectSyntheticImpact` early-outed on `_parts.Count == 0` and NOTHING dented — three "different" runs came out byte-identical (all undamaged), which is the tell. Fix: gate the hook on the component reporting bound state (`router.DamageStates.Count > 0`) plus a small fixed settle-tick floor before firing. General rule for the pilot/maneuver pattern (spawn-then-drive): anything reading a freshly-spawned component's OnStart-populated state must wait a frame or two, not assume tick 1.
- **A data gate (hashes, fractions, telemetry bands) CANNOT see a render bug — visual features need a fixed-camera screenshot A/B with a pixel-diff as an acceptance step.** D2's mesh-deform "exit" passed on data alone and shipped an invisible dent. When you add the visual A/B, three editor-MCP traps bite (all hit live, engine 26.07): (1) **Auto-exposure/tonemap drift** — TWO STATIC frames of an unchanged scene, seconds apart, differ by ~767 k px (of 921 k) with `max≈196`; it is pure global brightness, not geometry. Defeat it by brightness-normalizing before diffing (scale one image so its mean luma matches the other; a real local change survives, the global shift cancels) — and confirm the change is LOCALIZED (bbox/centroid on the expected part), not scattered. A genuine deform diff is a few thousand px with `max` well above the ~200 exposure noise, clustered on the feature. (2) **A pinned/staged actor is only at its fixed pose DURING the maneuver's hold** — screenshot MID-HOLD (start the run non-blocking, sleep into the hold), not after it ends (the actor is released and drifts out of frame). (3) **`set_editor_camera` during active play fights the game camera** — set it BEFORE the run (it persists through), or the mid-run set loses the race; `get_editor_camera` confirms the value took but the render can still be the game camera's. Cleanest repeatable A/B: dent → shot → `sw_repair` IN PLACE (no respawn, same pose) → shot; repaired-vs-pristine should fall back to the exposure-noise floor. (, `` editor_camera_screenshot + PIL diff, 2026-07-16)
- **A `set`-only `[ConVar]` property (no getter) breaks the s&box ConVar source generator with a PHANTOM `CS0019` — "Operator '??=' cannot be applied to operands of type 'Func<int>' and 'lambda expression'" — attributed to the WRONG line in your file** (the generated-code mapping, tens of lines off from your property; the cited line contains no such code). Invisible to headless `dotnet build` (the generator runs only in the editor compile), so `dotnet build` is GREEN while the in-editor `compile_status` FAILs — the "editor is the whitelist authority" gate biting via the source generator, not the whitelist itself. Fix: give the ConVar property a getter backed by a field (`get => _arg; set { _arg = value; ... }`), like every other ConVar in the codebase.
- **s&box hotload PRESERVES `static` field values across recompiles AND play-mode cycles (state survival), so an edited `static readonly` initializer stays FROZEN at its first-load value — even after play_stop → play_start.** Symptom: you edit a `static readonly` data table (e.g. a test pilot's coordinate array), `compile_status` goes green, re-run the suite — and the OLD values are still used (byte-identical run output, the character teleports to the pre-edit spot). Method BODIES always hotload; only static-field RE-initialization is skipped. Fix for hotload-editable data: make it a property getter (`static T[] Table => new T[] {...};`) — a method body that re-evaluates the current code on each access. A full assembly reload (editor restart) also clears it, but that is a blocked-manual step.
- **A test-harness snapshot reads STALE per-frame-sim telemetry while the game sits at the title screen (or any modal), because the sim tick is gated on `!Paused`.** In `Game.Paused => UIState.AnyModalOpen`, and `AnyModalOpen` includes `StartMenuOpen` (true at boot), so `OnFixedUpdate` — and thus `PowerGrid.Simulate`/`WaterNetwork.Simulate` — never runs until a case calls `` to clear the title gate. Any metric derived from the per-frame sim (`wattsProduced`, battery-Wh accumulation) therefore reads its idle 0 in a case that never unpauses. The existing suite hid this because every case mutates state DIRECTLY (``) or via a direct daily-tick call (``) — none asserted a Simulate-derived field. Fix: ` {mode:new}` first (unpause → Simulate ticks each frame), or, more robust for determinism, assert on PURE-GETTER telemetry that sums live component state without a sim tick (the way the movement-invariant metrics are analytic).
- **A fresh `git worktree` of an s&box project cannot `dotnet build`: the editor-generated is GITIGNORED (so the worktree doesn't have it), and a straight copy from the main tree still fails because the csproj references the Steam engine DLLs via RELATIVE paths computed from the MAIN tree's directory depth.** MSB1009 "Project file does not exist" when the file is absent; MSB3245 unresolved `Sandbox.*.dll` references after a naive copy when the worktree nests one level deeper (e.g. `dev\\worktrees\<name>\` vs `dev\\ \`). Fix: copy from the main tree into the worktree, then byte-safe-patch every `../../../../../../../Program Files (x86)/Steam` reference one `../` deeper (7→8 for the standard worktrees layout here) — and never commit the copy (it stays gitignored). Hit independently by two parallel build agents in the same session. (, M14 items 2026-07-16) — **More robust than counting `../`:** write the csproj with ABSOLUTE engine paths (`C:/Program Files (x86)/Steam/steamapps/common/sbox/bin/managed/Sandbox.*.dll` and the `addons/base/code/Base Library.csproj` ProjectReference) — depth-independent, so the same file works from any worktree nesting. **AND give each worktree build a DISTINCT `<OutputPath>`** (e.g. `…/.vs/output-wt-<branch>/`): the default `…/.vs/output/` is shared, so two parallel worktree builds (or a worktree + the open editor) clobber each other's `.dll`. Verified a clean 0-warning/0-error build this way from a `--movement-loops` worktree.
- **`git worktree remove` fails with "Permission denied" on a repo that lives inside a One-synced folder — both on the worktree folder itself and on the `.git/worktrees/<name>` metadata dir.** One stamps read-only/sync attributes on files it manages, and git's delete gives up on them; `git worktree prune` then also fails to clear the metadata, leaving a stale `.git/worktrees/<name>` entry even after the folder is gone. Fix (PowerShell): `Remove-Item -Recurse -Force <worktree-path>` for the tree, then clear attributes on the metadata before deleting it — `Get-ChildItem <repo>\.git\worktrees\<name> -Recurse -Force | % { $_.Attributes = 'Normal' }` followed by `Remove-Item -Recurse -Force`. Verify with `git worktree list` and an `ls` of `.git\worktrees\`. Plan for this on any machine where `dev\` is under `One\Documents` — every agent-worktree cleanup hits it.
- **Never bind dev-tool toggles to F keys — the editor owns F1–F12 in play-in-editor, so a raw `Input.Keyboard.Pressed("F7")` never fires.** The F keys are consumed by editor functions before the game sees them, and the read fails SILENTLY: the code compiles, the whitelist is happy, the panel simply never opens — reads like a broken panel, not a dead key. Bind temp/dev UI to a free letter key instead (check the project's `Input.config` for what's taken; raw reads also bypass action registration, so any unbound letter works), and keep a convar fallback (` 1/0` style) so the tool stays reachable if the key choice ever collides.
- **A Python CLI that prints editor/game console output dies with `UnicodeEncodeError: 'charmap' codec can't encode character` the first time a log line contains an emoji** (s&box logs include them, e.g. the "👋 X has joined the game" line) — Windows Python defaults stdout/stderr to cp1252 when piped. Fix at the top of the script, right after `import sys`: `for s in (sys.stdout, sys.stderr): s.reconfigure(encoding="utf-8", errors="replace")` (or set `PYTHONIOENCODING=utf-8` in the caller). Hit live reading the project's editor console through the MCP client tool.
- **Regenerating a Blender-exported asset kit on a NEWER Blender than the one that built the committed assets rewrites EVERY exported `.obj`/`.mtl` with cosmetic-only deltas — an exporter version-header line and an unused `Ns` value in the `.mtl` — so a naive "commit the regen" sweeps ALL N parts (30 in sedan_kit) into a diff where only the target parts actually changed geometry.** Clean pattern: (1) run a baseline regen with the UNMODIFIED generator first and confirm the diff is cosmetic-only; (2) make the real change and regen; (3) selectively revert every non-target part before committing, so the commit carries only real geometry changes. JSON manifests are immune (no version string in them). Related gotcha: with `core.autocrlf=true`, editor-reserialized `.vmdl` files show `status=M` with an EMPTY git diff (line-ending-only) — verify with a content diff before assuming a `.vmdl` actually changed. ( sedan_kit regen, Blender 5.2 vs committed 5.1.2 assets, commit d78f772, 2026-07-16)
