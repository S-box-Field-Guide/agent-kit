# Building UI — skill pack

> Lane pack. Load `_core.md` first, then this file. Denser than the full articles by design;
> for a matching bullet's full write-up, follow that gotcha's article link in `coverage.md`
> (full articles live on the Field Guide website, not in this pack). Sanitized public
> advice; unverified material is held privately until verified. The sync appends new bullets here.

## C# & razor

- **`.razor` files do NOT go to the global namespace.** Plain `.cs` classes without a
 namespace do; razor classes get a RootNamespace/folder-derived one. Fix: `@namespace X`
 in every .razor + `global using X;` in Assembly.cs. Symptom: "type Hud not found"
 from C#, or razor resolving the WRONG type (see next).
- **Interface-based scene scans are unreliable.** `Scene.GetAllComponents<Component>
 .OfType<IMyInterface>` compiled but returned nothing at runtime. Enumerate concrete
 types explicitly (`GetAllComponents<CropPlot>`, etc.) and union them.
- **`PanelComponent` overrides `OnTreeBuilt()` / `OnTreeFirstBuilt()` — NOT
 `OnAfterTreeRender(bool)`.** `OnAfterTreeRender(bool)` is a `Panel`/razor-code-behind
 hook; putting it on a `PanelComponent` fails CS0115 "no suitable method found to
 override". Use `OnTreeBuilt()` to (re)touch `@ref`'d child panels after every rebuild —
 the `@ref` is a NEW Panel object on each BuildHash-triggered rebuild, so anything set
 imperatively on it must be re-applied there, not once.
- `[Sync]` = owner→proxies; `[Sync(SyncFlags.FromHost)]` = host→all; guard sim code with
 `if (IsProxy) return;`. Networked props are runtime-only, never saved to scenes.
- **`GameObject.NetworkMode` defaults to `Snapshot`** (per-object scene-snapshot
 networking), NOT `Never` — confirmed in `Sandbox.Engine.xml` — **and `[Sync]` does
 nothing on a `Snapshot`-mode object.** CORRECTED 2026-07-31 (doc and source verified,
 engine build 26.07.22): the earlier advice here, that a scene-wide singleton (world
 state everyone reads, only the host mutates: chaos meters, day/night clocks, run stats)
 does NOT need `NetworkSpawn`/`NetworkMode.Object` and can just build identically on
 every peer, is WRONG. `[Sync]` only works when the GameObject has the
 `NetworkMode.Object` mode; properties on `NetworkMode.Snapshot` objects are never synced
 after the initial snapshot to anyone, even if marked with `[Sync]`. The `NetworkObject`
 that carries the sync table is only constructed inside `NetworkSpawn(...)` or on receipt
 of a create message, so a `Snapshot`-mode object never gets one. Deterministically
 bootstrapped peers agreed on the starting value because each computed the same number,
 not because anything replicated; any later change silently diverges and stays diverged.
 So `NetworkSpawn` is mandatory for ANY object carrying a `[Sync]` field, shared
 host-owned singletons included: the host creates and `NetworkSpawn`s it host-owned, the
 joining client does NOT create a competing local copy, authoritative fields stay tagged
 `[Sync(SyncFlags.FromHost)]`, and every mutation is still manually gated with
 `if (Networking.IsActive && !Networking.IsHost) return;` — the engine does NOT stop a
 non-host from writing a FromHost field locally, it just gets steamrolled by the next
 incoming snapshot, so the manual guard is still load-bearing.

- `Json.Serialize` / `Json.Deserialize<T>` (Sandbox's) + `FileSystem.Data` for saves —
 missing JSON fields land as defaults, so append-only save evolution is safe. Keep
 enums append-only too (we extended `AnimalKind`/`WaterDeviceKind` safely).
- `Mouse.Visible = true` is obsolete but still functional — fine for cursor-driven games
 until the replacement API is obvious.
- **`TextEntry` (not a raw HTML `<input>`) is the house control for text entry**, and it
 already handles Enter internally: `OnButtonTyped` fires `CreateEvent("onsubmit", Text)`
 on Enter, so razor binds it the same way as `onclick` — `onsubmit=@Method` (plain
 no-arg Action), with live edits via `OnTextEdited(string)` (not a Blazor
 `ChangeEventArgs`). Reaching for standard Blazor `<input onchange=@(e=>...)>` compiles
 (the AspNetCore.Components types are real) but isn't how this engine's own UI works —
 grep `addons/base`/`addons/menu` for a live `onsubmit=`/`TextEntry` usage before
 guessing (e.g. `MainSearchBar.razor`, `ChatPanel.razor`).
- **A client-local screen effect (fullscreen fade/flash) driven from HOST-side game code
 must gate on the LOCAL player identity AND still run its side-effect when no overlay
 exists.** one project's catch→respawn fade: a `ScreenFade : PanelComponent` on the gameplay
 HUD registers a static `Instance` on OnEnabled (cleared on OnDisabled), exposing
 `FadeOutIn(legSeconds, hold, onMidpoint)`. The guard (host authority) calls it on a caught
 character, but the fade must only darken the CAUGHT player's screen — gate the visual on
 `Character.Instance == caughtCharacter` (Instance is the local non-proxy body), while the TELEPORT
 (WorldPosition/Velocity/BreakStance) runs unconditionally so the host moves the authoritative
 body and the transform replicates. TWO null-safety rules make it robust: (1) if
 `ScreenFade.Instance` is null (menu/headless — HUD not mounted), `FadeOutIn` must STILL invoke
 `onMidpoint` immediately (a missing overlay must never swallow the respawn side-effect); (2)
 fire the midpoint callback exactly once (`_midpointFired` latch) at full black, since the
 teleport is where the player-invisible jump happens. The overlay is `pointer-events: none` so
 it never eats input, and re-triggering mid-fade continues from current darkness (don't reset
 alpha to 0). HANDOFF cost: the HUD-owner must add `ui.Components.Create<ScreenFade>` to the
 gameplay HUD GameObject in Bootstrap — until then catch-respawn still teleports (no fade) via
 rule 1.
- **`@ref="_field"` on a bare private field silently never assigns** — only a `CS0649`
 build warning ("field never assigned"), easy to miss in a long log, and the ref stays
 null forever at runtime. Every working `@ref` in the engine's own source binds to an
 auto-property (`Foo { get; set; }`), not a field — use a property.
- **For a razor COLLECTION of stateful child panels, don't `@ref` them — make the child
 self-driving via markup-attribute properties.** A `@foreach` that needs N live child panels
 (character previews, tiles with per-item state) has no clean `@ref` form: `@ref="Method(id)"`
 isn't an assignable target (won't compile), and a per-id field hits the silent-no-assign trap
 above. Fix: give the child component public properties (`ModelPath`, `Selected`, …), set them
 as razor element attributes (`<CharacterPreview ModelPath=@c.Path Selected=@sel />` — razor
 maps attributes to public props, same as `<TextEntry MaxLength=.. />`), and have the child read
 them LIVE in its own `Tick` override. No `@ref`, no owner-side per-frame push loop, and state
 switches (e.g. selected→spin-faster) apply instantly regardless of razor re-render.
- **`ScenePanel` for an in-UI 3D model preview: `World` is settable but `Camera` is GET-ONLY**
 (the ScenePanel owns its own `SceneCamera`). Assigning `Camera = new SceneCamera{...}` (as
 editor Widget code does — there `Camera` is a settable field) fails CS0200 on a `ScenePanel`;
 configure the existing camera IN PLACE (`Camera.World = new SceneWorld; Camera.Position=..;
 Camera.Angles=..; Camera.BackgroundColor = Color.Transparent`). Preview world recipe (verified
 vs the install's `addons/tools/Code/Editor/VisemeEditor/Visemes.cs`, the canonical off-screen
 preview): `new SceneModel(world, model, Transform.Zero)` + `UseAnimGraph = false` +
 `CurrentSequence.Name = "idle"` for direct clip playback + `Update(dt)` each Tick to advance the
 anim; spin by setting `SceneObject.Rotation` each frame; light with 2× `ScenePointLight` + a
 `SceneCubemap(Texture.Load("textures/cubemaps/default.vtex"), BBox…)`. No sibling project used
 ScenePanel before — VisemeEditor is the reference.
- **Never write `@{ }` inside an already-open razor code block** (`@if(...){ }`, `@for(...){ }`,
 etc.) — once inside such a block you're already in C# code context, so a nested `@{ }` is
 invalid and fails `RZ1010: Unexpected "{" after "@" character`. Only use `@{ }` to transition
 IN from markup context. Fix: hoist any needed `@{ var x = ...; }` setup block to BEFORE/outside
 the new wrapping `@if`, or drop the `@` and just use a bare `{ }` once already in code context.
 Hit adding a menu-open visibility gate to an existing panel that had markup starting with
 `@{ ... }`.
- **Sibling `PanelComponent`s on one GameObject have NO implicit z-index/hiding** — DOM/creation
 order in `Bootstrap` is the only separation, so a gameplay HUD panel that should hide behind a
 full-screen menu overlay must actively gate its own render on the menu's `IsOpen`-style static
 flag (`@if (!MenuOpen) { ... }`, with `MenuOpen` folded into `BuildHash` too) — it will NOT
   automatically stay behind the modal just because the modal was created later.

- **A freshly-scaffolded project's `Code/Assembly.cs` can be MISSING `global using System;`
 even though `project-setup.md`'s own template includes it — don't assume the stock
 scaffold matches the documented template.** Copying vehicle code (`Math.Clamp`,
 `MathF.Abs`, etc. — used throughout without an explicit `System.` prefix, per house
 convention) into a project whose Assembly.cs only had
 `Sandbox`/`System.Collections.Generic`/`System.Linq` produced 57 `CS0103: The name
 'MathF'/'Math' does not exist in the current context` errors across every vehicle file
 in one build. Fix: always check the destination project's Assembly.cs against
 `project-setup.md`'s template (`Sandbox`, `System`, `System.Collections.Generic`,
 `System.Linq`, `System.Threading.Tasks`, plus the project's own razor namespace) before
 transplanting code from another project, not just after the first compile error.
- **Draggable slider tracks (click-to-jump + drag-to-scrub) on a hand-rolled `PanelComponent`
 slider — the engine's own `SliderControl`/`ColorHueControl` are the reference mechanic.** There
 is no native drag helper you can drop into a `RenderFragment`, but the base library ships the
 exact pattern at `addons/base/code/UI/Controls/SliderControl.razor` (+ `Color/ColorHueControl.cs`):
 value = mouse LOCAL x over the track width, clamped, snapped to step. Verified API: the razor
 `onmousedown`/`onmousemove` event binding's delegate type is **`Action<PanelEvent>`** — typing the
 handler/lambda parameter directly as `MousePanelEvent` fails **`CS1503`** ("cannot convert from
 `PanelEvent` to `MousePanelEvent`"). Accept `PanelEvent` and pattern-match to **`MousePanelEvent`**
 (`if (ev is not MousePanelEvent e) return;`) to reach **`.LocalPosition`** (Vector2, PIXELS relative
 to that panel) — `.This` (the listener panel — the track), `.PseudoClass.HasFlag(PseudoClass.Active)`
 (true while pressed), and `.Box.Rect.Width` (local px width) are all available directly off the base
 `PanelEvent`/`Panel`, no cast needed. There is **NO `.Type`/event-name field**, so to
 branch jump-vs-scrub in ONE shared handler you pass a `bool` per binding (onmousedown→always act,
 onmousemove→act only if Active), not by reading the event type. `frac = Clamp(LocalPosition.x /
 This.Box.Rect.Width, 0, 1)`; snap with `MathF.Round(v/step)*step` (or the engine's
 `float.SnapToGrid(step)`). SCSS: the track needs `pointer-events: all` and a taller (~14px) hit
 area to be grabbable, and set the fill child `pointer-events: none` so events always target the
 track (keeps `LocalPosition` track-relative); drop any `transition: width` on the fill or scrub
 lags. A bare click lands as jump-to-position for free with this design. Zero drag precedent in any
 house project. Note: MCP `editor_camera_screenshot`/`camera_screenshot` can VERIFY the panel
 renders + compiles but CANNOT synthesize a drag — the value-from-position math is a code-read +
 in-hand confirm.
- **scss `@import` of a shared token/mixin file IS supported engine-side, but is NOT compiled by
 `dotnet build` — so a broken import path can't be caught headlessly.** The engine menu addon uses it
 heavily (`@import "/styles/_theme.scss";` virtual root + relative `@import 'vars';` partials, plus
 `$vars` and `@mixin`/`@include`; `@use` has ZERO precedent — stick with `@import`). BUT: no GAME
 project has any `@import`/`$var`/`@font-face` precedent (they all inline `font-family: Poppins,
 sans-serif;` per file), and
 **`dotnet build` only compiles the C#/razor — scss is compiled by the editor/runtime**, so an
 `@import` that fails to resolve produces a broken-looking panel that a headless build reports as GREEN.
 When your only verification is headless, prefer an inline `$token` block copied identically per
 `*.razor.scss` (build-safe, visually proven) over a shared `@import`; reserve `@import` consolidation
 for when you can screenshot-verify.
- **JetBrains Mono (and Consolas) are NOT shipped with s&box — "Roboto Mono" is the
 engine's monospace.** The install ships a fixed font set and nothing else; "Consolas" only
 resolves on a dev box because it's a Windows system font. There is no `@font-face` —
 a font under an `Assets/fonts/` path is referenced by its internal family name, bare or
 quoted. For a mono look with zero shipping work, use `"Roboto Mono", monospace`.
 **CORRECTION (2026-07-31, engine 26.07.22): "auto-registers any font file under an
 `Assets/fonts/` path" is too broad — Roboto Condensed, though it physically ships at
 `addons/base/Assets/fonts/` alongside the others, is NOT reachable from UI CSS.** Verified
 live: `font-family: "Roboto Condensed"` (and unquoted) silently falls back rather than
 rendering condensed glyphs. The engine's own scss only registers Poppins, Roboto, Roboto
 Mono, Inter, and Material Icons as usable UI families — a font file being present in the
 base install's fonts folder is necessary but not sufficient; treat the confirmed-reachable
 list as exactly those five until a family is proven live by an actual capture, not by its
 presence on disk.
- **The editor-generated the project `.csproj` is gitignored (`*.csproj`), so a fresh worktree
 checkout has NO csproj to `dotnet build`.** The real one lives only in the editor's working tree with
 reference paths RELATIVE to that location (seven `../` to reach `Program Files`), so it won't resolve
 from a deep temp worktree. To build headlessly in an isolated worktree, write a throwaway
 `Code/<project>.csproj` (itself gitignored, never committed) with the SAME `Microsoft.NET.Sdk.Razor`
 SDK + `<Using .../>` block but ABSOLUTE reference/analyzer/`ProjectReference` paths into the sbox install
 and a scratch `OutputPath` (don't write into the install's `.vs/output` — the live editor may be using
 it). The Razor SDK auto-globs `**/*.cs` + `**/*.razor` under the csproj dir, so it picks up the
 worktree's own sources.
- **Loose `.json` under `Assets/` IS readable at runtime by the GAME assembly:
 `FileSystem.Mounted.ReadAllText("<path relative to Assets>")` + `Json.Deserialize<T>`
 (live-verified in-editor play mode — vehicle part-kit manifest; in a PACKAGED build the loose
 file must additionally be listed in the `.sbproj` `Resources` globs — loose files don't
 auto-publish — after which `FileSystem.Mounted` reads work in the package too, confirmed via the
 shipped part-kit manifest glob). Zero-attribute binding: name DTO properties byte-for-byte as the JSON's snake_case
 keys (`attach_author_m` is a legal C# identifier) — System.Text.Json matches case-sensitively
 and silently skips unbound JSON fields, so no serializer attributes are needed.
- **A flex-grow `.track` holding a normal-flow `.fill` sized by `width: N%` is UNSTABLE in this
 engine — dragging the fill wider grows the WHOLE TRACK, not just the fill.** The row's layout
 computes the flex-grow item's own content-basis FROM its child first; since the child's width is
 a percentage of that same item, the two feed back into each other and the entire slider balloons
 as the value rises (owner repro: "dragging right makes the whole slider grow wider"). The FIX is
 the engine's own ground-truth pattern, copied verbatim from
 `addons/base/code/UI/Controls/SliderControl.razor(.scss)`: `.track { position: relative; flex-grow: 1; }`
 sizes ONLY against its flex row, and the fill is taken OUT OF FLOW entirely —
 `.fill { position: absolute; left: 0px; height: 100%; }` with `width: N%` — so its percentage can
 never feed back into the track's own size. Verified live via `camera_screenshot` at 0% / 5% / 86%
 fill: constant track width, left-anchored bar at every value. **Do NOT reach for `flex-basis: 0` +
 `overflow: hidden` on the track as an alternative fix** — tried first, compiled fine, but produced
 a lime "diamond/lens" floating near the track's midpoint at EVERY value (including 100%) instead
 of a left-to-N% bar; the `justify-content: center` inherited from the old flow-fill layout (there
 to vertically balance a taller hit-area track) was still being applied to the horizontal axis of
 the now-absolute child, and/or the flex-basis:0 + absolute-child combination broke percentage
 resolution outright — not fully root-caused, just empirically worse. When a redesign restyles an
 existing working widget and something goes visually wrong, diff the STRUCTURE (not just colors)
 against the pre-redesign commit and, if the widget has an engine-shipped equivalent, `find` it
 under the sbox install and copy ITS position/flow pattern — don't invent a new one.
- **`camera_screenshot`'s UI overlay renders at the REAL game-window resolution, not the project's
 requested `width`/`height`.** Requesting a screenshot narrower than the actual editor/game window
 (e.g. 1600×900 while the monitor/window is 2560×1440) silently clips every `right:`/`bottom:`
 anchored absolute-positioned HUD element clean out of the captured frame — they aren't corrupted
 or hidden, they're laid out past the requested canvas edge. Left/top-anchored elements at the same
 time render perfectly fine at any requested size, which makes this look like a real rendering bug
 in the right-anchored panels specifically (it isn't). Fix: request `camera_screenshot` at the same
 resolution as the actual primary monitor/game window (`[System.Windows.Forms.Screen]::PrimaryScreen.Bounds`
 in PowerShell if unknown) before concluding a right/bottom-anchored panel failed to render.
- **`ScreenPanel.ZIndex` controls stacking BETWEEN separate ScreenPanel GameObjects** — set
  it high (e.g. `99999`) to force a full-screen overlay on top of other HUD panels. Each HUD
  component gets its own ScreenPanel GameObject (the "two PanelComponents under one
  ScreenPanel leaves the second unrendered" rule), and paint order between those roots is not
  guaranteed by creation order. The engine's own `SceneTransition` uses `ZIndex = 99999`.
- **To show a loading overlay before a synchronous blocking call, defer the blocking work a
  couple frames.** A razor `onclick` that flips an overlay flag and then immediately runs a
  multi-second synchronous job blocks the main thread before the render pass — the overlay is
  only visible after the freeze (i.e. never). Fix: the click shows the overlay and sets a
  small frame countdown; `OnUpdate` decrements it and runs the blocking work only when it hits
  0, guaranteeing the overlay painted first.
- **CSS `@keyframes` + `animation:` shorthand work in s&box razor SCSS** — a spinner needs no
  per-frame re-render. The engine's own menu addon uses them (`animation: rotation 1s linear
  infinite;` + `@keyframes rotation { … }`). Do NOT drive a loading spinner from a BuildHash
  frame counter; that re-renders the panel every frame. Caveat: SCSS is compiled by the
  editor/runtime, NOT by `dotnet build`, so a keyframes typo is invisible headlessly.
- **`border-style: solid` is a PARSE ERROR in s&box SCSS** — and because an invalid
  property aborts the whole stylesheet, one stray `border-style` makes the entire panel
  render with zero size (invisible, not just unbordered). s&box UI borders are always
  solid; there is no `border-style` property. Declare the border with `border-width` +
  `border-color` (or the `border: Npx solid <color>` shorthand) and never a separate
  `border-style`. `dotnet build` won't catch it; only the in-editor console shows the error.
- **`Sandbox.UI.Clipboard.SetText(string)` is reachable from game code and is
  whitelist-clean** (engine 26.07.08e). It lives in `Sandbox.UI` inside
  `Sandbox.Engine.dll`. A game-assembly component can call it directly — no editor
  bridge needed for copy-to-clipboard features.
- **A world-tracking label (name tag over a player's head) is best done as a
  screen-space Razor chip positioned each frame at
  `CameraComponent.PointToScreenNormal(worldPos, out behind)` — not a WorldPanel.**
  You must project through the active gameplay camera, not `Scene.Camera` (which
  resolves to a transient editor camera clone in play mode and mis-projects the label
  off-target).
- **`overflow-y: scroll` on a razor flex-column panel still corrupts child layout even with
  explicit min-height pins on every row.** Gap/height computation breaks: rows overlap and
  trailing children vanish. The engine's own menu UI uses overflow-y scroll successfully, so
  the property works in some contexts — but in a gap-spaced, scaled, absolute-column context
  it is reproducibly broken. Design panels to GROW with content (explicit column height) instead
  of scrolling.
- **A razor `background-image: url('ui/foo.png')` (any loose PNG/JPG a ScreenPanel references)
  renders in the editor but goes BLANK in the published package.** ScreenPanel CSS image URLs
  load loose image files from the mounted filesystem, so they work in-editor; but loose
  (non-compiled) images are NOT auto-shipped. Fix: add the disk glob to the sbproj `"Resources"`
  string (Assets-relative, no `assets/` prefix, `*` spans `/`): e.g. `"ui/cars/*.png"`. The
  wildcard matches the loose disk path, not the normalized asset path. Same mechanism as any
  other loose resource file — see the "Loose resource files don't auto-publish" article.
- **A runtime `Texture` for a UI panel must be built in C# and assigned to
 `Panel.Style.BackgroundImage` — scss `background-image` can't take a runtime texture.**
 Chain: `Texture.Create(w,h).WithFormat(ImageFormat.RGBA8888).WithDynamicUsage()
 .WithName("x").Finish()`, fill a `Color32[w*h]` (row-major), push with
 `tex.Update(colors, 0, 0, w, h)`. `.WithDynamicUsage()` makes repeated `Update` cheap
 (recreate only when dimensions change). Display: `@ref` a `Panel` and set
 `Style.BackgroundImage = tex` from code-behind in `OnTreeBuilt()`.
- **Editor-assembly code referencing a razor component's member fails CS0103 even though
  global-namespace game `.cs` types resolve fine** — the editor project has no
  `global using <RazorNs>`. The game project's Assembly.cs papers over the razor namespace
  split with `global using X;`, but the editor assembly doesn't import it. Fix: add the same
  `global using` to the editor assembly, or fully-qualify razor types.
- **`repeating-linear-gradient(...)` in razor SCSS renders nothing and logs `Unknown Image Type`
  to the console on EVERY render of that element (per-frame spam while visible).** Same root as
  `radial-gradient` unsupported: s&box's `background-image` supports only plain
  `linear-gradient( to <dir>, <stops> )`, not `repeating-`, radial, or conic variants. Emulate
  with a fixed-stop `linear-gradient` listing hard stops explicitly.
- **s&box panel CSS rejects `display: inline-flex` at RUNTIME** with a red "Code Error:
  inline-flex is not valid with display" toast. The panel engine's `display` accepts only
  `flex`/`none` (no inline variants). For "inline chip" behavior, give the parent row
  `flex-direction: row; flex-wrap: wrap` and the chip `display: flex; flex-shrink: 0`.
  Grep new .scss for `inline-` before shipping.
- **`[GameResource("Name","ext","desc")]` now raises CS0618: 'GameResourceAttribute' is
  obsolete: 'Use AssetType instead'.** Replace with
  `[AssetType(Extension = "ext")] public class Foo : GameResource`. The old attribute
  compiles as a warning, so it slips past a green build unless warnings-as-errors is on.

- **`SliderControl` near-min values produce scientific notation in CSS** — `SliderPosition` string-interpolated raw; float snap residue near Min formats as `4.9E-06%` (style parse error). Sanitize: snap values within `StepFine/2` of Min/Max to exactly Min/Max.
- **Never name classes after Sandbox built-ins.** A global-namespace `PlayerController` compiles, but razor resolves `Sandbox.PlayerController` first and you get baffling "no such member" errors. Check the name against the Sandbox API before creating player/camera/inventory-ish classes. (— renamed ours to `Farmer`) **Reconfirmed (, 2026-07-14, engine 26.07.08e):** a new UI PanelComponent named `LoadingScreen` (namespaced `.LoadingScreen`) hit `CS0104: 'LoadingScreen' is an ambiguous reference between '.LoadingScreen' and 'Sandbox.LoadingScreen'` in headless `dotnet build` — a clean, explicit error (nicer than the "baffling no-such- member" symptom above), but the SAME root trap. `Sandbox.LoadingScreen` is a real built-in type most agents wouldn't guess exists. Fix: prefix project-specific UI components (here, `GbLoadingScreen`) rather than the generic name that also happens to be a menu/HUD-ish concept — those are exactly the names Sandbox has already taken. **Reconfirmed, member-level variant (, 2026-07-16, engine 26.07.15a):** the collision isn't limited to top-level class names — naming a UI panel MEMBER `OnBack` collides with the engine's built-in `Sandbox.UI.Panel.OnBack(PanelEvent)` virtual hook, so a `public Action OnBack` property trips `CS0108` (member shadowing) instead of the ambiguous- reference error above; same family as `g-ui-component-member-shadows-base` but on `Panel` specifically. Fix: rename (e.g. `BackRequested`). Generalize: `Panel` exposes many virtual event hooks (`OnBack` among them) — check the base type before naming a public member `On*`.
- **`BuildHash` is the razor re-render trigger** — hash *everything* the markup reads, or the panel silently goes stale. **`HashCode.Combine` has no 9-arg overload** — a flat 9th argument is `CS1501` and fails the in-editor razor compile ( `VehicleHud.razor`, 2026-07-14: Play dead until nested). Nest: `HashCode.Combine( HashCode.Combine( a..h ), i )`.
- **`ScenePanel.RenderScene` preview, extended for a multi-part kit + auto-sized framing** ( , building on the single-model `RenderScene` pattern above / `CharacterPreview.cs`): (1) a kit-based subject doesn't get one renderer — build N plain `ModelRenderer`s under a parent `GameObject`, all inside the same `_scene.Push`, reading each part's transform from the SAME manifest data path the gameplay assembler reads (`PartKitManifest`/`PartKitPart.AttachLocalM`/`AttachRootM`), so the preview can never drift from what actually spawns in-game — and put NO physics components (no `Rigidbody`, no collider) in the preview scene, it only ever renders. (2) frame the camera ONCE per rebuild instead of hand-tuning a fixed camera per subject: walk every `ModelRenderer` under the root, transform each renderer's `Model.Bounds` 8 corners to world space via `renderer.WorldTransform`, union them into one `BBox`, then place the camera at `center + dir * (bounds.Size.Length*0.5f / tan(fov/2) * margin)` aimed at the bounds center. This makes differently-sized subjects (a kart vs. a pickup) fill their tile consistently with zero per-subject tuning — recompute after every rebuild (not once at startup), since a kit swap changes the bounds.
- **`Networking.QueryLobbies` + `LobbyConfig`/`LobbyPrivacy`/`LobbyInformation` are game-reachable and whitelist-clean; a Hidden+Public lobby is the code-gated-join recipe.** Verified surface (26.07.08e, M12.3, game-assembly compile Success=true, 0 SB1000): `Networking.CreateLobby(new Sandbox.Network.LobbyConfig{ Hidden=true, Privacy=LobbyPrivacy.Public, DestroyWhenHostLeaves=true, MaxPlayers=8, Name=... })` (`Hidden`=out of the server browser but still programmatically queryable; `Privacy` Public/Private/FriendsOnly); `Networking.SetData(key,val)` stamps lobby metadata; `await Networking.QueryLobbies(Dictionary<string,string> filters)` returns `List<Sandbox.Network.LobbyInformation>` where each has `.LobbyId` (string, feed `Networking.Connect(id)`), `.Data` (a `Dictionary<string,string>` - `lobby.Data?.TryGetValue("", out v)` reads YOUR stamped keys for a pre-connect version check), `.IsFull`, `.Members`/`.MaxMembers`, `.Name`, `.IsHidden`. The 3-arg overload is `QueryLobbies(Dictionary, bool includeServers, CancellationToken)`. REFERENCE: engine menu addon `addons/menu/` + `ServerRow.razor` + `MapModal.razor`. **CORRECTED 2026-07-17 (engine-source verified, `Networking.LobbyQuery.cs` / `LobbyConfig.cs` / `SteamLobbySocket.cs`):** two errors above. (1) The 3-arg `bool` is **`includeServers`** (merge dedicated servers), NOT "hidden-inclusion". Hidden-inclusion is a FILTER-DICT KEY: `QueryLobbies({..., ["hidden"]="1"})` opts hidden lobbies IN (the engine otherwise appends `hdn:0`). (2) A `Hidden=true` lobby is NOT returned by a default query — the earlier "still programmatically queryable" is only true WITH `["hidden"]="1"`. So the code-gated-join recipe is either **Public+`Hidden=false`** (listed) or **Public+`Hidden=true` queried with `["hidden"]="1"`** (unlisted but code-findable) — NOT the plain `Hidden=true` shown in the sample. Also: `LobbyPrivacy` (`Public`/`Private`/`FriendsOnly`) maps to the Steam `ELobbyType`; `Private`/`FriendsOnly` are excluded from ALL list queries, so keep `Privacy=Public` for any code-findable lobby. And the editor FORCES `Privacy=Private` on editor-host lobbies (`CreateLobbyAsync` → `EditorLobbyPrivacy`), so the old `query_self` "did it come back?" run could never pass in-editor regardless. Full matrix + Steam-invite plumbing in `methods/p2p-peer-hosted-servers.md §2b`.
- **A HUD that must stay visible while a networked player is auto-possessed needs its OWN `ScreenPanel` GameObject - the generator/tool "ui" GO is DISABLED on possess, taking every panel on it with it.** disables the whole `ui` GameObject (GeneratorPanel + BrushPanel) when entering character mode (`.SwapToCharacterView` -> `_uiGo.Enabled = false`), and D7 makes every networked player auto-possess - so anything the co-op session must SHOW while playing (invite code, player list, Stop/Leave) would vanish if parked on that GO. Fix: mount it as its own `ui_session` ScreenPanel GameObject in Bootstrap (exactly like the minimap, which survives possession the same way), root `pointer-events:none`, only the interactive card `pointer-events:all`. Bonus trap: character mode sets `Mouse.Visibility = Hidden` (CharacterCamera.OnEnabled, once - not per-frame), so a menu can re-show the cursor with a single flip, but you must also SUPPRESS the camera's mouse-look while the menu is open (a static `MenuOpen` flag the camera reads) or the view spins under the pointer, and re-Hide on close.
- **s&box razor text has three distinct "text is there in the DOM but not on screen" failure modes — verified while adding content to a dense panel ( GeneratorPanel, engine 26.07.08e).** (1) **A single long text line CLIPS TO BLANK.** A ~45-char string in a fixed-width (~268 px) card panel renders as an EMPTY box — not wrapped, not truncated, just gone; a ~12-char literal in the same box renders fine. Fix: split into short lines, and for label/value data reuse the `.census-line` idiom (a `flex-direction:row` div with two `<span>`s, `justify-content:space-between`) — free-stacked text `<div>`s in a `flex-direction:column` collapse to ~0 height and OVERLAP (glyph line-box exceeds the div height); the row-with-spans pattern stacks reliably. (2) **A narrow segmented row CLIPS multi-char strings.** Six `flex-grow:1` segments in that same panel width render a 4-char string (`"0.20"`) as blank but a 3-char string (`".20"`, via `ToString(".00")`) fine — the 4th glyph overflows the ~44 px segment and the whole run drops. Widen, reduce segment count, or shorten the string. (3) **Computed values render more reliably when RESOLVED TO A PLAIN LOCAL first** (`string s = Foo; … >@s<`) than referenced inline as `@Foo` / `@tuple.Field3` inside the `@<div>…` fragment expression — inline forms rendered blank in several cases while the local form worked. None of these throw or log; `compile_status` is Success=true.
- **SUPERSEDED (owner-settled, 26.07.22): the cumulative-text-count theory below does NOT hold. Do not trim, combine, or ration text runs to make a capture legible.** The glyph-block corruption is a CAPTURE-PATH artifact only; the mechanism is the root panel's fractional scale (see the superseded font-size bullet above for the full operative rules and the scale-1 capture recipe). The account below is kept as dated investigation record. The documented rule (style guide §0 / the ui-redesign post-merge note) is "beyond a low threshold of distinct font-size or letter-spacing declarations, ALL text on the panel becomes blocks." New observation ( GeneratorPanel, 26.07.08e, ZERO font-size/letter-spacing anywhere): after the extent×fidelity fold-in added ~19 extra mono text runs, the SINGLE last-rendered collapsible-section summary (`<span class="sec-sum">@Summary</span>` on the bottom "Advanced" header) rendered as solid blocks while three identical sibling section headers above it rendered as clean text. Content was irrelevant — a plain literal blocked too; expanding the section (which swaps the summary span for different markup) rendered fine. **Removing that one mono `.sec-sum` span brought the whole panel back under threshold and every header rendered.** So on a text-dense panel, treat mono text-run COUNT as a budget: trim the least-important text (a collapsed-section summary is a good sacrifice — the values reappear on expand) rather than assuming you're safe because you added no font-size. Same "survives" character as the font-size variant; verify on a play-mode `camera_screenshot`, never headless.
- **A computed getter-only property whose name CASE-collides with a snake_case JSON field on the same DTO makes `Sandbox.Json.Deserialize` throw and returns null for the WHOLE object — because s&box runs System.Text.Json with `PropertyNameCaseInsensitive=true`.** 's `PartKitPart` had JSON fields `stiffness`/`zone` (settable) AND convenience accessors `public float Stiffness => stiffness ?? default;` / `public string Zone => ...` (computed, code-only). STJ builds ONE type contract and, under case-insensitive matching, `Stiffness` collides with `stiffness` -> `InvalidOperationException: "The JSON property name for 'PartKitPart.Stiffness' collides with another property."` at contract-build time, so `TryLoad` returned null and the part-kit car silently fell back to its fused body (no damage components attached). This was LATENT: `dotnet build` is green, and the offline python validator never exercises the C# STJ binding, so it only fired the first time a manifest was loaded live in-editor. Snake<->Pascal pairs that ALSO differ by an underscore are safe (`dent_impulse`<->`DentImpulse` = "dentimpulse" != "dent_impulse"); only exact case-collisions (`stiffness`<->`Stiffness`) bite. Fix: `[System.Text.Json.Serialization.JsonIgnore]` on the computed accessors (they are derived, never serialized) -- or rename them. The whole accessor group got it, so a future accessor can't re-trip it. LESSON: any DTO that pairs a raw JSON field with a same-name computed accessor must ignore the accessor; verify manifest/DTO loads LIVE, not just via `dotnet build` + offline validators.
- **A member you add to a `Component` subclass can silently SHADOW a non-obvious inherited base member — `Sandbox.Component` has methods with plausible names like `EditLog(string, object)` (the editor undo/edit-logging hook), so a `public ... EditLog` property compiles with only a CS0108 "hides inherited member" WARNING, not an error, and quietly breaks the base member.** 's WorldSession gained `public IReadOnlyList<WorldEditRecord> EditLog => ...` for the M11 brush-edit save log; `dotnet build` was green, but the in-editor `compile_status` flagged `CS0108: 'WorldSession.EditLog' hides inherited member 'Component.EditLog(string, object)'`. This is the same family as "never name classes after Sandbox built-ins" (g-ui-name-classes-after-sandbox-built-ins) but one level subtler — it's a MEMBER on a Component-derived type colliding with an inherited METHOD, not a top-level class-name clash. Fix: rename the member (chose `EditRecords`). LESSON: on any `Component`/`GameObjectSystem`/etc subclass, treat generic member names (`EditLog`, `Log`, `Name`-adjacent, lifecycle-ish verbs) as suspect; the in-editor `compile_status` is the authority that catches the hide — headless `dotnet build` reports it too but it is easy to miss among warnings, so scan compile_status warnings after adding members to engine-derived types.
- **An agent CANNOT verify a font FACE (which TTF/weight resolved) or text legibility from an MCP `camera_screenshot` when the app is over the glyph-corruption threshold — the text renders as solid filled blocks, and you cannot screenshot your way out of it because the requested `width`/`height` does NOT change the UI render scale.** The UI overlay always renders at the REAL game-window resolution (see g-ui-camera-screenshot-s-ui-overlay-renders), so you can't raise the render height to push text under the glyph-corruption threshold — the scale is fixed by the editor's game-view size. Verified on (UI-0 spike, engine 26.07.08e, game-view ~1536×800): EVERY ScreenPanel's text rendered as blocks in `camera_screenshot` — not only the new text-dense style-kit card but the untouched, simple `StartMenu` (Poppins, ~4 distinct font-sizes) too, which proves the whole app was already over threshold at that render scale (this is the FONT-SIZE/text-run glyph-corruption law, g-ui-font-glyph-corruption-is-cumulative-text-count-not-just-font-size, manifesting globally at capture scale). SHAPES, fills, borders, box-shadows, tints, and swatch COLORS all render perfectly — only glyph runs corrupt — so `camera_screenshot` still verifies layout/color/shadow/animation-settled-state, just not typography. Consequence for a verification agent: font-family/weight resolution and text legibility are OWNER-CHECK items needing a real-game-window eyeball, not something an MCP screenshot loop can settle. Confirm the fonts at least LOAD via `read_console` (a missing TTF logs a resource error; 's Baloo 2 / Nunito logged none).
- **A flex container's `gap` and its thin flex-ROW children UNDER-MEASURE their height when rendered inside a `@<div>…` RenderFragment EXPRESSION (a `RenderFragment Foo => @<div>…` / `return @<div>…`), so the rows collapse on top of each other AND the container reports a shorter height than its content — the NEXT sibling then overlaps the fragment's lower rows.** The IDENTICAL markup renders perfectly when placed directly in the component's `<root>` (main) markup. Verified in GeneratorPanel (engine 26.07.08e, UI player pass): a 4-row `.ef-readout` (thin `.census-line` flex rows in a `gap`-ed column) lived inside `RenderFragment ExtentFidelityBlock` and rendered all four rows piled on one line while the container measured ~2 rows tall, so the disclosure row below overlapped rows 3–4 — but the SAME `.census-line` block used inline in `<root>` (the dev-stats census) rendered clean. The reliable fix is a combination: **(1) render such rows in the main component markup, not inside a fragment expression; (2) put a `min-height` on the thin rows (e.g. `.census-line { min-height: 19px }`) so a row can never collapse to ~0; (3) give the CONTAINER an explicit `min-height` covering all its rows (`.ef-readout { min-height: 111px }`) so it can't under-report height to following siblings.** Two adjacent traps found the same pass, worth knowing: a `min-height` on a flex CHILD that has a sibling (e.g. a `.slabel` label row above a `.segments` row inside one `.preset-row`) COLLAPSED the sibling `.segments` row — so do NOT blanket-`min-height` flex children that share a parent; and `overflow-y: scroll` on a `gap`-ed flex COLUMN broke gap/height computation for its direct children (they collapsed) — never put scroll on a gapped flex column. Single-root fragments (`RenderFragment Row => @<div class="preset-row">…`) rendered inline are FINE; it is the gap-and-thin-rows-inside-a-fragment case that breaks.
- **'border-style: dashed' raises an on-screen Code Error toast at load ('dashed is not valid with border-style').** The engine's css parser supports only a subset of border-style values - 'solid' works; 'dashed' (and likely 'dotted') are rejected at STYLESHEET PARSE time, surfacing as a red Code Error popup in-game rather than a build error, so headless build AND in-editor compile_status both stay green - the first signal is the runtime toast. Fix: use a faint solid border (+ ghost text / no hover) for disabled treatments.
- **To persist a heterogeneous record log (e.g. a save file's `objects[]` of prop-place / prop-remove / density records) DON'T reach for a System.Text.Json polymorphic tagged union (a discriminator + custom `JsonConverter`) — use ONE flat concrete class with a `kind` int and the UNION of every kind's fields.** STJ polymorphism needs a custom converter (a whitelist/ portability risk this engine's "boring serialization" posture dodges); the flat class round-trips trivially with plain `[JsonPropertyName]` props. Each record carries its `kind` (validated with `Enum.IsDefined` on load) plus a monotonic `seq`; per-kind static factories (`PropPlace(...)`, `Wildlife(...)`) keep construction honest. APPEND-ONLY field law: never rename/reorder/remove a field — an older file that omits a newer field deserializes it to its default, no throw. Verified whitelist-clean + full round-trip (mixed log) in-editor.
- **The engine's text-line measure that feeds auto-height flex layout DRIFTS ACROSS EDITOR BOOTS — the same committed SCSS rendered a segmented control's option boxes at 16px INSIDE a 25px track in one editor session and at 28px POKING OUT of a 15px track after an editor restart (same capture size, same scene).** So any control whose vertical geometry relies on text-derived auto heights (a padded label box inside a padded auto-height track — the classic segmented-row recipe) is unstable: it can look correct today and visibly broken (asymmetric or overflowing) after the next editor boot, which reads like a regression nobody caused. Two-part remedy, verified in GeneratorPanel `.segments`/`.seg`: (1) `align-items: center` on the track so any measure deficit splits symmetrically instead of the stretch default top-anchoring the child and eating the BOTTOM inset only (the visible symptom was the active pill sitting ~2px low in its track); (2) pin the geometry EXPLICITLY — fixed `height` on the option child (drop its vertical padding; the height owns the box, glyphs still paint centered via align/justify center, the proven `.step` button pattern) plus a matching `min-height` on the track (child + 2×padding, border-box accounting) — after which all rows measured an exact uniform inset (3px top AND bottom at 1440p) across boots. Related but distinct from `g-ui-razor-fragment-flex-gap-undermeasures-height` (that is fragment-context under-measure; this is boot-to-boot drift that hits MAIN-markup rows too).
- **`radial-gradient(<size> at <pos>, ...)` — the modern CSS `at <position>` syntax — raises an on-screen red Code Error toast ("Cannot read a color from '60% 100% at 50% 100%'") at load; it is NOT a headless-build or in-editor `compile_status` error, so both stay green and the first (only) signal is the runtime toast, same failure shape as `border-style: dashed` (see `g-ui-border-style-dashed-code-error` above) — the engine's UI CSS parser accepts only a small legacy subset of `background-image` functions. Fix: s&box has no radial-gradient-with- position replacement, so drop to `linear-gradient( to <dir>, <stop>, ... )` (optionally with `deg` angles and %-offset stops) — every confirmed-working gradient in this codebase (HomeScreen.razor.scss) is a plain `linear-gradient`, never `radial-gradient`. For a "glow emanating from one edge/corner" look, a `linear-gradient( to top, rgba(...) 0%, rgba(..., 0) 100% )` reads close enough when the glow element is already positioned/sized to sit at that edge. Likely same-bucket-broken (untested but same parser family): `oklch`, `color-mix`, and `conic-gradient` — treat any non-`to <dir>` gradient argument and any non-`rgb`/`rgba`/hex color function as suspect.
- **MCP `camera_screenshot(includeUi:true)` can capture only the first live `ScreenPanel`, silently omitting every later ScreenPanel even though the game scene contains and renders them.** Verified with 's eight live UI GameObjects: the first generator panel appeared, while separate brush/right-rail/session/tips panels did not; disabling the first UI GameObject yielded a capture with no UI rather than promoting the next panel. This is a capture-tool limit, not proof that the later panels are absent in the real game window. Fix the runtime layout with one PanelComponent per ScreenPanel as usual, but treat a multi-ScreenPanel parity montage as an owner/manual game-window capture unless the MCP compositor gains multi-panel support. Do not mutate play-clone UI GameObjects to isolate layers: `set_game_object enabled=false` dropped the play clone during this verification. ( Onyx restyle, 2026-07-15) **UPDATE ( ui-polish r2, 2026-07-14 session):** a later play-mode `camera_screenshot(1920x1080, includeUi:true)` DID composite panels from at least two ScreenPanel GameObjects at once (the generator panel AND the right-rail minimap, plus a toast). So the single-panel limit is not universal — captures may include several ScreenPanels, but which ones appear is not agent-controllable, so the rule stands: use captures for the GEOMETRY of whatever panels do appear; whole-HUD parity and all text remain owner/manual checks.
- **Trailing children of a tall absolutely-positioned flex column silently don't render at all — no overflow paint, no compression, just missing UI — once the column's AUTO height under-measures its content.** 's generator card (position:absolute column, `transform: scale(0.75)`, ~24 direct rows): with every disclosure expanded, everything after the Features section (the Advanced section, Regenerate, Play, the join row) vanished while the card background ended at the last child that "fit" the mis-measured extent — even though the full content demonstrably fits on screen. This is the same engine auto-height measure drift that hits thin mono rows (the ef-readout precedent), scaled up to a whole panel. THE FIX LADDER, each variant play-mode-capture-verified: `max-height` on the column = flex-compression (children paint dim past the shortened card bg); removing the cap = the silent truncation above; `min-height` = NO effect (does not lift the clamp); an EXPLICIT `height` (e.g. `height: 1400px`, sized as painted-space ÷ transform-scale) = everything lays out and renders. Give Yoga a definite size; the unused remainder is harmless under a pointer-events:none root.
- **A Razor panel mounted right before a synchronous multi-second call never paints — one consumed tick is NOT enough; hold several real frames before the block.** ' staged production boot mounted its loading screen, deliberately consumed one Update tick, then ran the ~6 s terrain build — the log shows the build starting ~70 ms after mount, and the owner saw no loading screen at all, just a frozen editor he read as a crash ("pressing play bombs out"). A fresh ScreenPanel + Razor tree needs mount → style/layout → paint spread across MULTIPLE engine frames, so one tick of headroom paints nothing. Fix (verified live): gate the armed stage on a frame COUNTER — each OnUpdate call is one real rendered frame (no frames render inside the sync block, so counting calls counts paints) — and only start the heavy call after ~6 frames (~0.1 s, imperceptible next to the build it fronts). General rule for any loading/progress UI fronting synchronous work: budget N frames of pure idle between mount and block, not "next tick".
- **A razor `PanelComponent` that lazily caches a list of control closures over a retargetable Target property keeps editing the OLD target after the property is re-pointed — the UI displays and MUTATES orphaned state.** Pattern: `_params ??= BuildParams` where `BuildParams` does `var def = Target.Definition;` and each control captures `def` in its `Get`/`Set` lambda. When a live swap re-points the property via a plain auto-property setter (e.g. `UiRig.Retarget` runs `panel.Car = newController` on every car switch, and the new controller carries a FRESH definition instance), the setter runs no code, so the cached closures still bind the destroyed object's def. The panel then shows and writes a def that no live object uses ( tester: the Redline dial did nothing after a Tab/menu car switch — it was editing the orphaned previous car). Fix: make the target a full property whose setter, only on an actual reference change (`if ( !ReferenceEquals(_x, value) )`), nulls the cached closure list and re-derives any per-target baseline/tuning state, so the next access rebuilds `BuildParams` against the new instance. Prefer this to live `Target.X` closures so per-target dirty-tracking and baselines still work. Same family as `g-cl-*` retarget bugs but UI-specific: the cache, not the reference, is the stale thing.
- **Unquoted numeric component attributes compile headless but FAIL the in-editor razor codegen.** `<TextEntry MaxLength=22 />` builds green under headless `dotnet build`, but the editor's razor generator emits the value as a STRING literal -> `CS0029: cannot implicitly convert type 'string' to 'int?'` in the `_gen_*.razor_*.cs` file (compile_status errors; the panel never loads). Fix: force a C# expression -- `MaxLength=@(22)` (or bind a field). This is a concrete instance of the standing law that headless green does not prove editor green: the two razor codegens differ on attribute-literal typing. Caught only by in-editor `compile_status` after merging a menu rewrite whose headless gates were all green.
- **The in-editor razor codegen DROPS whitespace between literal text and `@expressions` in a text node — "Begin on @cur.Name" renders as "Begin onTwin Currents".** Headless build accepts it and simple single-expression cases ("Seed @w.Seed") can render fine, but any node mixing several literals and expressions loses spaces unpredictably ("Seed @cur.Seed - @cur.Biome" -> "Seed 918Temperate"). Same family: a mixed inline STYLE attribute (`style="width:@(pct)%"`) silently never applies (a progress bar stuck at 0 width while its label ticks). Fix for both: collapse the whole thing into ONE interpolated expression -- text nodes become `@($"Seed {cur.Seed} - {cur.Biome}")`, style attrs become `style=@($"width:{pct}%")`. Owner-screenshot-verified before/after on the StartMenu map picker.
- **Grid thumbnails (a `background-image` on a fixed-size `.thumb`, `background-size: cover`) render inconsistently — some cropped at the edges, one over-zoomed, one tiny in a sea of backdrop — even though every source PNG is the same dimensions.** Two compounding causes: (1) `cover` scales the image to FILL the tile and crops the overflow, so a 1:1 (256x256) source in a ~1.8:1 landscape tile silently loses ~22% off the top and bottom; (2) if the source crops frame their subject at different sizes/offsets, `cover` amplifies that inconsistency. Fix is the SOURCE-IMAGE layer, not CSS: normalize each PNG to the tile's EXACT aspect ratio — output an integer multiple of the tile's logical px (a 130x72 tile -> 390x216 = 3x) so `cover` crops nothing — with the subject centered, filling a uniform fraction, and ~10% margin baked in so `cover`'s fill absorbs sub-pixel tile-aspect drift from `flex-grow` without ever clipping the subject. Then `background-size: cover; background-position: center` is correct and needs no change (design-lock friendly). OVERWRITE the existing globbed png paths — don't add new `Resources` paths. Scripted normalization: measure each subject's pixel bbox, recompose onto the aspect-matched canvas, edge-replicate padding (valid when the render backdrop is a horizontally-uniform sky/ground gradient). Verify without the engine by emulating cover in PIL (scale by `max(tw/iw, th/ih)`, center-crop to tile) — the session-menu modal card can't be camera-screenshot (see g-tool-camera-screenshot-misses-modal-card).
- **The engine styler hard-errors on a standalone `border-style: dashed` property ("dashed is not valid with border-style", red Code Error card at load) — while the SHORTHAND `border: 1px dashed <color>` parses silently.** The asymmetry is the trap: a style refactor that splits a working dashed shorthand into base-rule `border` + per-state `border-style`/`border-color` overrides (e.g. applying the constant-box-size pattern to stateful chips) converts a silently-tolerated declaration into a hard error. Whether the shorthand's dash ever actually RENDERED dashed is unconfirmed — treat engine border styling as solid-only and express states via `border-color` alpha/tint instead.
- **A bare top-level class selector in a razor `.scss` matches EVERY descendant with that class — so reusing a state-vocabulary word as a layout/card class silently applies the card's box rules (padding, gap) to any child element that happens to be in that state, ballooning it.** In 's TelemetryPanel the grip CARD was `<div class="card grip">` with a top-level `.grip { padding: 24px 26px; gap: 10px; … }` rule; the per-wheel status chips used the house state vocabulary `grip/working/sliding/air` as their state class (`<div class="wchip grip">`). The engine styler compiles the card rule to `TelemetryPanel .grip`, which is an ANY-descendant match — so a chip in the GRIP state (`.wchip.grip`) ALSO matched it and inherited the card's 24×26 padding, rendering FL/FR (gripping) as big squares while working/sliding/air chips (no bare card selector) stayed at their real 22×36. Symptom read as "chips resize with state" and survived a wrong fix aimed at borders, because the real cause was selector collision, not the border. FIX: never let a layout/container class share a word with a state class — rename the structural one (`.grip` card → `.gripcard`). More generally: scss nesting does NOT scope a bare class to its parent; `.foo { .bar { &.foo {…} } }` still lets the outer `.foo` hit `.bar.foo`. When a design system has a fixed state vocabulary (grip/working/sliding/air, pass/fail/warn, on/off), treat those words as RESERVED and never reuse them as container names.
- **To put a working `TextEntry` on a HUD panel that rebuilds every frame, don't fight the rebuild — move the field into its OWN sibling `PanelComponent` whose `BuildHash` excludes all live/per-frame data, so it never rebuilds while the user types and the field keeps focus by construction.** A `TextEntry` inside a panel whose `BuildHash` folds live gameplay values (suspension load, rpm, timers) is destroyed+recreated on every rebuild → focus/caret lost every frame → typing impossible. The `_wantsFocus` + `OnTreeBuilt` re-`Focus` idiom (ChatBox) only survives a ~1 Hz rebuild; at 60 Hz it drops keystrokes. Robust recipe, verified in 's tune-rename modal: (1) a dedicated `PanelComponent` (mounted beside the others on the same ScreenPanel — each PanelComponent has its OWN independent BuildHash, so the busy panel rebuilding does NOT force this one to); (2) drive it from static state (`TuneRenameState.Active` + an integer `Revision` bumped only on open/close/commit); (3) `BuildHash => Combine(Active, Revision)` — constant while typing, so ZERO rebuilds mid-edit and the TextEntry object persists. Seed the field text + `Focus` once in `OnTreeBuilt` guarded by a "have I seeded this target yet" ref so a later build can't clobber in-progress typing. This overturns the earlier verdict that an in-HUD rename field is "impractical" — it is practical, just not in the same component. Esc ownership: consume `Input.EscapePressed` in the overlay AND guard any other panel's global Esc-closer with `!TuneRenameState.Active` so the modal owns the key while open.
- **`protected override int BuildHash` compiles ONLY on `PanelComponent`, not on a plain `Component`.** `BuildHash` is a `PanelComponent` member; a logic-only `Component` has no such method, so the override fails headless with `CS0115: no suitable method found to override`. Easy to hit when you split a UI feature into a logic `Component` (state + per-frame) that lives beside its `PanelComponent` view on one `ScreenPanel`, then copy the "keep the tree stable" `BuildHash => 0` idiom every PanelComponent in the codebase uses onto the logic component too. Fix: delete the override from the plain `Component` — it has no render tree to hash; only the PanelComponent view needs a BuildHash.
- **A `static [ConVar]` property named `Enabled` on a `Component` subclass hides the inherited instance `Component.Enabled`** (`CS0108: 'X.Enabled' hides inherited member`). The convar property name is free (the convar string is its own argument, e.g. `[ConVar("rpg_tips")]`), so the collision is gratuitous. Beyond the warning it is a real footgun: unqualified `Enabled` inside the class now resolves to your static convar, not the component's enable state. Fix: name the backing property distinctly (e.g. `TipsEnabled`) and keep the convar string as-is. General rule: never name a static member after a `Component`/`GameObject` instance member.
- **A `position: absolute; right: Npx` flex COLUMN with no explicit `width` and `align-items: flex-end`, holding MUTUALLY-EXCLUSIVE conditional children of different widths, is suspected to right-anchor inconsistently depending on which child is currently mounted** — reasoned fix, NOT yet confirmed live in-editor (this pass had no editor/MCP access; confirm with `camera_screenshot` toggling between states before treating as settled). Repro context: `RightRail.razor.scss` `.rail` (owner screenshot: toggling H between the compact vertical `.key-legend` (168px) and the expanded `.card` Hotkeys panel (280px) — both plain `align-items: flex-end` children of the same `.rail`, no other positioning — showed the expanded card sitting noticeably LEFT of the compact list's edge). By CSS spec an unset-width `right`-anchored box should shrink-to-fit and keep its right edge pinned regardless of content width, so flex-end children should never move; if the engine's shrink-to-fit for this case does NOT hold that invariant (plausible given this engine's other flex quirks — see `g-ui-flex-grow-track-holding` and `g-ui-text-measure-drifts-across-editor-boots` in this same file), the container's own width — and therefore every flex-end child's anchor — would drift with whichever conditional child happens to be the widest at render time. **Fix applied (pending confirmation): give the container an explicit fixed `width` equal to its widest possible child** (here `.rail { width: 280px; }`, matching `.card`) so the anchor point is deterministic instead of shrink-to-fit-dependent. If a future in-editor check finds the CSS-spec behavior actually DID hold (i.e. the fixed width was a no-op) and the real cause was something else (scale-transform origin interaction, stale BuildHash on toggle, etc.), update this entry with the real root cause — don't let the "fix" stand unexamined once verified.
- **An undefined scss variable is a runtime "malformed rule" that can kill the WHOLE panel — and no compile gate catches it.** Referencing an scss variable that the stylesheet never defines (e.g. using `$text-hi` in a sheet that only defines `$text`) passes `dotnet build` AND the in-editor `compile_status` clean (0/0). At RUNTIME the style parser logs `Warn: Skipping malformed rule in /ui/<sheet>.scss: Unknown variable '$x'` — and the panel using that sheet can stop rendering entirely ('s hotbar disappeared outright). Each razor.scss has its OWN variable header — variables do NOT cross sheets, so copying a rule between sheets silently drops its variables. **Diagnosis:** a panel vanishes with a green compile → `read_console minimumLevel=Warn` and grep for `malformed rule`. **Fix:** define the variable in THAT sheet's header (or use a defined one). **Related trap seen the same day:** heavy hotload churn can FAIL lambda substitution ("Unable to find matching substitution for a lambda method") leaving a MIXED old/new assembly — stale keybinds/UI despite green compiles. A play-session restart does NOT clear it; a full editor restart does. If UI behaves like an old build after big merges, check the console for hotload substitution warnings before debugging "bugs" that do not exist in the current code. verified: 2026-07-17 (hotbar vanished in play; console: "Skipping malformed rule ... Unknown variable '$text-hi'"; defining the variable restored the panel after editor restart)
- **The classic CSS border-trick triangle (zero-size element + transparent side borders + one solid border) does NOT render in s&box — the "triangle" draws as small dark fragments.** The engine's border renderer does not reproduce the browser behavior the hack relies on (transparent borders meeting at a miter); a `width/height: 0` element with `border-top/bottom: transparent` + `border-left: solid` comes out as garbled dark blocks ('s time-pause pill "play" icon, owner screenshot 2026-07-17). **Engine-safe replacement — the clipped rotated square:** a fixed-size wrapper with `overflow: hidden`, containing a square child `transform: rotate(45deg)` shifted half out of the window with a negative margin — only the diamond's point shows through = a clean triangle, any direction by which half you clip. Both ingredients (overflow clip, transform rotate) render reliably. Bars/rects for other media glyphs (pause, etc.) work as plain filled divs — it is only the border-miter triangle hack that fails.
- **`DateTime.TryParse` with `DateTimeStyles.RoundtripKind | DateTimeStyles.AdjustToUniversal` throws `ArgumentException` at RUNTIME, not compile time** — `RoundtripKind` is mutually exclusive with `AdjustToUniversal`, and the invalid combination passes compilation silently, then crashes on the first call. For round-trip `"o"`-format UTC stamps (trailing `Z`), use `RoundtripKind` ALONE — the `Z` already yields a UTC kind.

- **A player's Steam avatar renders via the engine `avatar:<steamid64>` texture-URL scheme.** Razor/SCSS: `background-image: url( avatar:<steamid64> );` — the CSS setter calls `Texture.Load(url)`, which dispatches `avatar:`/`avatarsmall:`/`avatarbig:` (32/64/128px) to the Steam avatar loader. Returns a transparent placeholder immediately and swaps the real avatar in place once Steam responds — no panel re-render needed. C# equivalent: `Texture.LoadAvatar(long steamid, int size)`.

- **In s&box Yoga, an explicit `flex-basis` (notably `flex-basis: 0` inherited from a base rule) takes precedence over `width` for a flex item's main-axis size** — giving a column `width: Npx; flex-grow: 0` WITHOUT overriding an inherited `flex-basis: 0` collapses it to ~0 (min-content not honored, text renders one character per line). Fix: set `flex-basis: Npx` (plus `width: Npx` belt-and-suspenders). The failure is invisible headlessly since `dotnet build` never compiles SCSS.

- **Razor component TAG resolution does NOT consult `global using` directives — a library component tag silently renders as a plain HTML element while its class resolves fine in code-behind.** The Razor compiler triggers `RZ10012` as a WARNING, not an error; the tag falls back to an inert element. C# `@code { }` blocks DO see global usings, creating a confusing split. Fix: add an explicit `@using <Namespace>` in every `.razor` file that renders external components as tags. Treat `RZ10012` as an error in review.

- **When a namespace segment and a class share the same simple name, code in a sibling namespace binds the simple name to the NAMESPACE — member access fails CS0234 even though a correct `using` for the class is present.** C# resolves unqualified names by walking enclosing namespaces first; file-level `using` directives are consulted only after that walk finds nothing. Adding more `using`s will not help. Fix: fully qualify through the namespace down to the class, or introduce an alias (`using Wb = Some.Ns.ClassName;`). Keep type names distinct from any namespace segment when you control the naming.

- **A `global using` ALIAS in the consumer's `Assembly.cs` cleanly resolves a simple-name collision between a vendored-library type and a game type of the same name, WITHOUT editing the vendored copy.** The alias makes game code bind the correct type, while the library's own files are untouched (enclosing-namespace members beat compilation-unit aliases inside library compilation units). This keeps vendored code pristine for byte-identity with canonical upstream.

- **When a game keeps a type whose simple name collides with a library type of COMPATIBLE shape, a green compile does NOT prove the intended binding — both resolutions are legal, so the compiler happily binds whichever the lookup rules pick and never warns.** This is the dangerous sibling of CS0104/CS0234: when same-named types have compatible members, NOTHING fails and code silently runs against the wrong type. Fix: pin intent with a `global using` alias, even when the build is clean — the build being clean proves nothing here.

- **A runtime-written `FileSystem.Data` image (a per-save thumbnail, a user screenshot) is NOT addressable via a CSS `background-image` URL** — those resolve against mounted content/asset filesystems, not the user Data FS. Turn it into a `Texture` in code and assign `Style.BackgroundImage`: decode pixels via `Texture.Create().WithFormat(RGBA8888).WithDynamicUsage().Finish().Update(Color32[])` (whitelist-clean). Storage tip: `FileSystem.Data` write surface is TEXT-only (`WriteAllText`); a palette-indexed text thumbnail (one char per cell) is ~4 KB at 64x64 and needs no binary/PNG API.
- **An undefined SCSS variable in a `.razor.scss` fails at RUNTIME with only a per-render console warning — no Code Error toast, no compile error — and the affected panels can render fully INVISIBLE.** The sheet silently drops all rules referencing the undefined `$var`. SCSS sheets share nothing; a sweep/refactor that copies a token name from another sheet plants this bug. Defenses: define every `$var` in the sheet that uses it, lint used-vs-declared variables per sheet pre-push, and smoke-test that overlays render a non-trivial rect. Same silent-parser family as `border-style: dashed` and `radial-gradient(... at ...)`, but WORSE — those raise a red toast; this one only whispers to the console. → [fix article](/fix/scss-undefined-variable-invisible-panel)

- **`ScreenPanel.ZIndex` orders PAINT only — pointer input is NOT routed by cross-root stacking.** A lower-ZIndex root whose panel is `pointer-events: all` keeps swallowing clicks even when a higher-ZIndex root draws a full-screen modal over it. The symptom reads like a broken modal: it renders on top, some controls work, but clicks in regions where the lower root also has hit area trigger the underlying screen's buttons. Fix: cooperative de-arming — the lower root must drop its own hit surface while any modal that can cover it is open (a computed class like `under-modal` setting `pointer-events: none`, keyed on every such modal's open flag, and those flags must be part of the lower panel's `BuildHash` or the class never re-evaluates). Corollary: every new full-screen modal added over an armed surface must join that surface's de-arm condition; the modal's own scrim cannot protect it. → [fix article](/fix/screenpanel-zindex-paint-only-input)

- **A child-combinator rule targeting the universal selector (`.parent > *`, including `> * + *`) is silently dead in s&box razor SCSS — no error, no warning, the rule simply never applies.** `dotnet build` and in-editor `compile_status` both stay green. Flex-shrink/spacing protections written as `.scroll > * { flex-shrink: 0 }` do nothing, so once content exceeds the container's max-height the flex layout squashes the children and elements paint over each other. Same silent-parser family as the undefined-SCSS-variable and border/gradient traps. Fix: skip the universal child combinator; write direct descendant rules naming each child class explicitly (`.scroll .row`, `.scroll .divider`). → [fix article](/fix/scss-child-combinator-universal-selector-never-matches)

- **SUPERSEDED (owner-settled, 26.07.22): "a single `font-size` VALUE declared uniformly across every text element still triggers the glyph-corruption bug" does NOT hold, and neither does the strip-every-declaration recipe it came with.** Owner screenshots of sized panels (font-size scale 12/13/14/16 with px line-heights) confirm those panels render PIN-SHARP on the physical screen — font-size declarations are not a live-render bug at all. The glyph-block corruption is a CAPTURE-PATH artifact ONLY, produced somewhere in the MCP `camera_screenshot` pipeline, not by the UI, the font, or any stylesheet property. Operative rules: (a) NEVER strip font-size or letter-spacing from a shipping stylesheet to "fix" captures — there is nothing to fix; (b) verify GEOMETRY from a capture, never text fidelity; (c) the owner's eyeball on the real game window is the only valid typography route; (d) capture-evidence conclusions about glyph rendering are unreliable by demonstrated history (three sessions, three different wrong generalizations), so do not draw a fourth. **MECHANISM FOUND (26.07.22, 2026-07-31, controlled single-variable A/B in one live scene):** the trigger is the ROOT PANEL'S SCALE. A `ScreenPanel` on its default `AutoScreenScale` renders at a fractional scale, and at a fractional scale the capture path rasterizes glyph runs as solid grey blocks; set `AutoScreenScale = false, Scale = 1` on that same panel and every glyph is pin sharp. (e) To verify text from a capture at all, pin the panel under test to `AutoScreenScale = false, Scale = 1` at runtime (an MCP `set_component`, no code change, gone at play-stop) before capturing; (f) the owner's eyeball is still the acceptance route; (g) never SHIP a panel pinned to scale 1 to work around this. → [fix article](/fix/font-glyph-corruption-shared-font-size-value-still-triggers)

- **A CSS shape/size/position keyword inside `radial-gradient` (`circle`, `ellipse`, `at 50% 50%`, `closest-side`) errors as a color and drops the rule.** The engine's `radial-gradient` parser accepts a color-stop list ONLY; it reads the first prelude token (`circle`) as the first color stop and aborts with a red "Cannot read a color from 'circle'" Code Error at sheet compile, then the failed rule silently vanishes (lazy compile — the card appears at load, then goes away). Fix: write color stops only — `radial-gradient( rgba(...), rgba(...) )` gives the same centred fill; for a shaped or off-centre gradient, paint a plain gradient on a sized, absolutely-positioned child. Same legacy-subset parser family as `border-style: dashed` and `radial-gradient(<size> at <pos>)`. → [fix article](/fix/radial-gradient-shape-keyword-parses-as-color)

- **`Sandbox.Json.Serialize` always writes INDENTED JSON, and its serializer options are an internal get-only property with no public override.** Every byte of a payload built through it pays for newlines + indentation on top of the data, so any per-tick or bulk network channel that Json-serializes pays a size tax with no built-in opt-out (fine for one-off save files). Fix: re-drive the SAME serializer (same converters + naming policy) through a compact/non-indented writer so the wire format is identical except whitespace — measured 41–56% smaller on bulk channels. Prove it: parse the compact output back, re-serialize through the ORIGINAL indented path, and diff for byte-identical output before shipping. → [fix article](/fix/json-serialize-indents-by-default)

- **A C# property initializer that reads another property (`public int X { get; init; } = SomeClass.SomeProperty;`) compiles clean headless but breaks the EDITOR compile with CS0182 ("attribute argument must be a constant expression").** The editor's code generator embeds initializers where only compile-time constants are legal; a headless `dotnet build` never runs that generator, so green headless ≠ editor-safe. Same headless-green/editor-red split as the razor codegen traps, but on a plain C# property. Fix: give the property a constant sentinel default (`0`/`-1` = "unset") and resolve it to the live value at the validation/normalization boundary, not at declaration. → [fix article](/fix/editor-codegen-needs-constant-property-initializer)

- **A struct's `public static Config Default => new();` does NOT call a declared all-optional-parameter constructor — `new()` binds to the implicit parameterless value-type constructor, so `Default` returns `default(T)` with every field zeroed and the intended defaults silently discarded.** It compiles clean and reads correctly in review; nothing hints the constructor was skipped. Fix: pass every argument explicitly (`new(argA: ..., argB: ...)`) or add an explicit parameterless constructor that chains to the parameterized one, and pin it with a test asserting the populated field VALUES, not just that construction succeeded. → [fix article](/fix/struct-default-property-new-skips-constructor)
- **A text element's line box measures `max(declared line-height, the font's own natural box)`, so a column of rows built to a browser mockup's arithmetic overflows its fixed parent.** Measured on a scale-1 capture: `16px/22px` renders a 33px line box, `13px/20px` renders 30px. Padding, borders, margins, gaps and explicit sizes all measure EXACTLY as declared — only the line box inflates, which is why it hides until you total a tall stack (a 13px button with 9px padding + 1px border stands 50, not 40). **CORRECTED MECHANISM (2026-08-01, engine 26.07.22, live measurement of seven elements via a dev ConCmd reading `Panel.Box.Rect`):** the "1.5x the declared line-height" figure is a symptom, not the law. The natural box runs about 0.95x font-size for a body face and about 1.08x font-size for a Poppins-weight display face; across the seven elements the ratio to the DECLARED line-height ranged 1.00 to 1.57 while the ratio to the FONT SIZE held steady within each face, and the one element declaring a line-height above its natural box (23px font, 30px declared) measured exactly 30.0. So declaring a line-height below the font size does nothing, the natural box wins regardless — there is no way to shrink a line box below its glyphs by lowering the declared number, only by lowering the font size. Size rows off the font size, not off an assumed 1.5x, reserve an explicit `line-height` for asking the engine for MORE leading than the glyph needs, and verify on a `Scale = 1` capture, not by construction. → [fix article](/fix/text-line-box-taller-than-line-height)
- **s&box composites UI alpha in LINEAR color space while browser mockups blend in sRGB, so every low-alpha white/light surface renders substantially LIGHTER in-engine than the same `rgba()` looks in a browser.** `rgba(255,255,255,0.05)` over a near-black card measured `#434445` in-engine vs `#1C1E21` in a browser. It's isolated to the alpha step — opaque colors round-trip bit-for-bit and a 0.92 alpha matched a linear-blend prediction to 1/255 — and worst at low alpha. Fix: match TARGET RENDERED PIXELS, never authored alpha numbers; sample both surfaces and dial s&box alpha until they agree, per backdrop (the corrective factor is backdrop-dependent). → [fix article](/fix/ui-alpha-composites-linear-not-srgb)

- **`ScreenPanel.ZIndex` DEFAULTS TO 100, not 0 — and two roots with EQUAL ZIndex have NO defined paint order.** Engine source: `[Property] public int ZIndex { get; set; } = 100;` (`ScreenPanel.cs`), so every ScreenPanel that never sets a tier sits at 100 — exactly where most projects author their "modal above the HUD" tier, so a menu "at 100" is actually TIED with every unset base root. Cross-root compositing is `Scene.GetAll<ScreenPanel>().OrderBy(x => x.ZIndex)` (`CameraComponent.OnCameraRenderUI`): a stable sort over the scene object index's HashSet enumeration order, which has no contract. It matches creation order on a fresh boot (so the bug hides for months) and re-rolls on any ScreenPanel disable/enable interleave (freed HashSet slots reused) or editor hotload (index rebuilt from an unordered `HashSet<object>`). Symptom: a full-screen surface that painted a perfectly healthy tree is invisible because a tied root composites over it — every painter-side check reads green. Fix: author an explicit tier for EVERY root (set the bottom surface to a real 0, never leave it unset), keep any two coexisting surfaces on distinct tiers, and when debugging "painted but invisible" dump the roots in `OrderBy(ZIndex)` order and look at what composites AFTER the victim. → [fix article](/fix/screenpanel-zindex-default-100-tie-order-unordered)

- **A weight-named font FILE registers as its own separate font FAMILY, not as a weight variant of its base family — `Poppins-Black.ttf` becomes the family `"Poppins Black"`, it is NOT `Poppins` at `font-weight: 900`.** Because s&box auto-registers every font file under `Assets/fonts/` by its internal family name (see `g-ui-jetbrains-mono-consolas-shipped-s-box`), declaring `font-family: Poppins; font-weight: 900;` does not reach the Black weight at all — the engine silently serves whatever the nearest weight it actually HAS registered for the `Poppins` family is (observed: Bold), with no warning that the requested weight was substituted. This applies to every weight-suffixed variant shipped as its own file (ExtraBold, SemiBold, Medium, Light, Thin, etc.) — each one is only reachable by its own family NAME (`font-family: "Poppins Black";`), never by pairing the base family with a numeric `font-weight`. Verify which weight actually rendered on a live capture before trusting a `font-weight` value did anything.

- **Inline `<svg>...</svg>` markup written directly into a `.razor` file renders as an empty panel — no error, no console warning, just nothing drawn — while a `.svg` FILE loaded through CSS `background-image: url(...)` renders fine.** The engine has precedent for the working path, and no precedent anywhere for inline svg markup succeeding. Fix: never author inline `<svg>` in a razor template — save the graphic as a `.svg` asset and reference it the same way any other background image is referenced (`background-image: url("/path/to/icon.svg");`), which also gets you the normal scss sizing/positioning rules for free.

- **A razor `class` attribute built with direct string interpolation (`class="x@i"`) emits NO class at all — silently, with no compile error and no console warning — collapsing the element as if it had no styling.** The razor compiler does not splice `@i` into the surrounding string the way it does inside element content; inside an attribute value the whole thing needs to be one expression. Fix: build the class name as a single C# expression instead of interpolating into the attribute string — `class="@("x" + i)"` (or a computed property/local holding the full string) — never `class="x@i"` or similar bare interpolation directly in an attribute.

- **Razor's email-address heuristic silently swallows an `@` used as a literal-to-expression boundary: `word@Word.Word` (e.g. `P@Race.Position`) parses as a LITERAL STRING, not an expression, because the shape matches `foo@bar.com` and razor treats that as text needing no escape.** The element renders the literal source text (`P@Race.Position`) instead of the expression's value, with no compile error, no console warning, and a `camera_screenshot` capture that looks plausible because the engine rasterizes any sized text element as a solid grey block regardless of what it actually says. Four instances shipped this way in one project, one of them since the very first HUD build, all invisible through every offline gate and a full design review (a review compares the mockup to the MARKUP, and the markup reads correctly to a human). The only thing that caught it was measuring a cell's rendered box WIDTH against its expected character count: a value cell rendering 408.5px at ~28px/glyph is fifteen characters wide, and `P@Race.Position` is exactly fifteen characters. **Fix: never glue a literal directly to the front of an expression** (`P@x`, `l@i`, `x@a.B` are all the same trap) — put the literal INSIDE the expression instead, `@($"P{x}")`, which has no adjacency for the heuristic to misread and cannot be reintroduced later by editing the prefix. Same root as `g-ui-razor-class-attribute-interpolation-emits-no-class` above (opposite symptom: that one vanishes, this one becomes text) — a note at one call site did not stop the same mistake three files away, so once the mechanism is understood, grep the SHAPE (`[A-Za-z0-9]@[A-Za-z_][A-Za-z0-9_.]*`) across every `.razor` file rather than just fixing the one instance under investigation.

- **A unitless `line-height` (e.g. `line-height: 1.4;`) misrenders — the glyph raster overflows its box — where the same value declared in `px` renders clean.** This is a more basic break than the already-documented line-box inflation (`g-ui-text-line-box-is-one-and-a-half-times-the-declared-line-height`, which covers a `px` value rendering 1.5x its declared height): a unitless value is not merely scaled wrong, it visibly corrupts the glyph rendering. Always declare `line-height` in `px`, never unitless, and budget the extra ~1.5x box height documented in the entry above on top of that.

- **Never write a focus-cursor field from `onmouseover` when that field participates in the panel's `BuildHash` — the mouse arriving over a row rebuilds the panel tree on the spot, destroying and recreating the very element under the cursor, and a press/release that straddles that rebuild is swallowed by the engine: no `onclick` fires.** The bug reads as "this button doesn't work, it just doesn't respond" with a perfectly clean console and a perfectly correct `onclick` handler sitting right there in the markup — nothing about the symptom points at `BuildHash` unless you already know the mechanism. Root-caused independently on two different panels in two different projects (a menu row list, a landing-page card picker), same mechanism both times. **Fix: focus is a KEYBOARD/gamepad-cursor concept only — never write it from a mouse hover handler.** Give the mouse its own affordance from CSS `:hover` (which needs no rebuild), styled identically to the `.focused` class so both input methods look the same to the player. If a panel genuinely needs both a keyboard focus index AND a way to show which row the mouse is currently over, keep them as two separate fields and fold only the keyboard one into `BuildHash`.

- **A panel's declared `width`/`height` IS the outer number: padding and borders sit inside it, never added on top.** Measured live with a dev ConCmd reading `Panel.Box.Rect`: a row declared `width: 512px` with 24px padding on each side measured 512.4px outer, not 512 + 48 = 560; a `width: 30px` glyph with a 2px border measured 30.5px outer. There is no `box-sizing` declaration involved anywhere in either sheet, the untouched default already behaves as border-box. Practical rule: author every mockup width and height as the OUTER number the mockup shows, never as a content number with padding/border added by hand, that hand arithmetic under-sizes every single box by exactly its own padding (in the case measured here, every row in a launch menu shipped 48px too narrow until re-authored). Caution for anyone reasoning from a design-review claim of the shape "no engine precedent for `box-sizing: border-box`": that statement can be true and still say nothing about what the untouched default does. A missing precedent for a DECLARATION is not evidence the property defaults to the opposite value, test the default directly before designing a whole sheet's arithmetic around its assumed absence.

- **The logical UI canvas a `ScreenPanel` lays out against is HEIGHT-fixed, not a fixed 1920x1080: width follows the game window's aspect ratio instead.** Measured live at a 1920x1080 window: the logical canvas is 2060.7 x 1080, not 1920 x 1080. Consequences for any mockup authored against a 1920-wide canvas: every VERTICAL measurement and every LEFT-anchored value transfers 1:1 (a footer's top edge and height both landed within a pixel of the mockup's numbers). Anything RIGHT-anchored drifts wider than the mockup draws it, one panel that was meant to clear a neighbour by 60px cleared it by 198px instead, wider but not wrong. Anything HARD-CENTRED on an assumed canvas width is silently off-centre: a sheet positioned with `left: (1920 - width) / 2` sat 70px left of true centre once the real canvas was 2060.7 wide. **Centre with flex (`align-items: center` / `justify-content: center` on the parent), never with arithmetic against an assumed canvas width** -- flex centering is correct at any width the window produces, hard-coded centring is only ever correct at the one aspect it was computed against.

- **An absolutely positioned child with a percent-based size resolves against the nearest ANCESTOR THAT ESTABLISHES A POSITIONING CONTEXT, and a styled track/container `div` does NOT establish one just by having other layout properties (flex, padding, a background, a fixed size) on it -- it needs an explicit `position` declaration.** Symptom observed live: a stat-bar track (`.bar-track`) held an absolutely positioned fill sized `width: N%; height: 100%`, and because the track itself declared no `position`, every fill resolved against the nearest ancestor that DID have one, the panel root, so `height: 100%` meant 100% of the whole screen. Twenty-five bars across five cards rendered as a solid amber flood covering the entire panel, with the cards themselves buried underneath it. Fix: `position: relative` on the track (or any intended containing element) makes it the resolution reference and the percent sizes come back to the track's own box. **When one absolute child misbehaves this way, sweep every sibling pattern in the same stylesheet rather than fixing only the element that shouted** -- the same missing-`position` defect was sitting silently on two other elements in the same file whose absolute children happened to be small (corner-flag triangles), which merely rendered in the wrong corner instead of flooding the screen, and would have shipped unnoticed without the sweep.

- **`transition-delay`, `&:intro` and `&:outro` all parse cleanly in the engine's razor-scss pipeline (tested isolated, engine build 26.07.22) — an earlier "zero precedent, assume unsupported" caution for this selector family can relax.** An earlier build shipped without an arrive/leave cascade specifically because none of the three had a known-working precedent anywhere in the codebase, and an unparsed selector aborts the WHOLE stylesheet to a 0x0 render (see `g-ui-scss-undefined-variable-console-warn-only-invisible-panel` and the `border-style`/`radial-gradient` entries above for that failure family) — a caution that made sense given the blast radius, but stopped being necessary once actually tested. Isolated test on `MainMenuPanel`'s `.row`: measured 560.9 x 450.3 with a bare `transition-delay: 60ms`, and 560.9 x 450.3 again with `&:intro` plus `&:outro` added — same box both times, meaning the sheet parsed and rendered rather than aborting. Same answer this shop's `skewX` got after previously being assumed dangerous for the same reason. **Rule: a novel/unprecedented selector is worth one isolated test against a throwaway element before it gets written off** — the caution is warranted (a wrong guess costs the whole sheet), but so is spending the five minutes to find out, rather than carrying the caution forward indefinitely. Per-row stagger (`:nth-child` with a delay) remains untested.

- **A single-line XML doc comment sharing the SAME line as the field it documents swallows the declaration — the field silently stops existing.** `/// <summary>X.</summary> public float MinX;` parses as one triple-slash comment line; the compiler sees no `MinX` at all. No error at the declaration — it surfaces downstream as a confusing `'Foo' does not contain a definition for 'MinX'` cascade that reads like a typo or missing-using. Rule: XML doc comments go on their OWN line, directly above the member, never sharing a line with the declaration.

- **`box-shadow: inset` paints OUTSIDE the element in s&box Razor panels — `inset` is effectively ignored, not weakly supported.** An inset vignette meant to darken a panel's inner edges renders as a halo AROUND it; scaled to a full-screen inset vignette it renders NOTHING AT ALL, silently, no error. Build vignettes/inner glows from `linear-gradient` bands (a radial or directional gradient layered as a background/overlay) instead — those render reliably at any size, including full-screen.

- **An absolutely positioned child resolves against the nearest ancestor that ESTABLISHES a positioning context, and a styled `div` does NOT establish one just by carrying flex/size/border/background — it needs an explicit `position`.** Without it the child skips its visual parent and resolves against the panel root/screen (a 180px card's fill texture rendered full-bleed with the frame chrome drawn on top). Fix: `position: relative` on the intended container. Trap: a sibling screen using the identical absolute-child pattern can render correctly only because ITS own box already was the positioning root — copying that pattern into a plain host box reproduces the bug, so sweep every sibling absolute child in the stylesheet, not just the one that shouted.

- **`#if DEBUG` inside a `.razor` file is ALWAYS FALSE in every configuration (editor included) — a preprocessor guard in razor markup strips nothing.** Razor compiles through a generator that parses with `CSharpSyntaxTree.ParseText(text, null, path)`; the null `CSharpParseOptions` means `Default`, which carries no preprocessor symbols (unlike a `.cs` file, which gets the project's real set). So `#if DEBUG` deletes everywhere and `#if !DEBUG` keeps everywhere, with no warning. Worse, the `.razor` SOURCE and any sibling `.scss` ship into the uploaded package verbatim regardless of the guard. Fix: build anything that must be genuinely absent from a release as a plain `.cs` class (real symbols + `StripDisabledTextTrivia` actually delete it); prove exclusion by building Release and byte-scanning the assembly in BOTH UTF-8 and UTF-16. -> [fix article](/fix/razor-if-debug-always-false)
